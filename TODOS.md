# TODOs

Tracker unique des TODO en attente, conformément à la règle "aucun TODO non tracké en fin de phase".

## P0 — Infrastructure & squelette

- [ ] Remplacer les icônes/favicon placeholder (`public/pwa-192x192.png`, `public/pwa-512x512.png` — carrés unis `#0b0f14` ; `public/favicon.svg` — cercle teal minimal, remplacé début P1 car c'était encore le logo par défaut du template Vite) par de vraies icônes de marque lors de la phase design UI (`ui-ux-pro-max` + `frontend-design`). Repéré par `graphify` P1 (communauté isolée "Leftover Vite Favicon Branding").

## P1 — Auth & multi-tenant

- [ ] Avant le go-live (P13) : configurer un provider SMTP dédié (Resend/Postmark/SendGrid) pour les emails d'auth. Le service email intégré de Supabase est limité à quelques emails/heure sur ce projet (confirmé pendant les tests E2E — `over_email_send_rate_limit`), pas prévu pour de l'usage réel au-delà de tests ponctuels.
- [ ] Avant le go-live (P13) : revoir la config auth hébergée (`site_url`, `additional_redirect_urls`, `minimum_password_length`) — actuellement les valeurs par défaut Supabase, le `supabase/config.toml` local pointe encore vers `127.0.0.1` et ne doit pas être poussé tel quel (`supabase config push`) sans revue complète.
- [ ] Playwright : n'a que Chromium pour l'instant. Ajouter WebKit (proxy iOS Safari) en phase Polish (P11) quand le mobile-first sera vérifié visuellement.

## P2a — Programmes & blocs

- [ ] Le projet Supabase gratuit a un cold-start perceptible (une requête peut prendre plusieurs secondes après une période d'inactivité) — les tests E2E tournent en 1 seul worker (`playwright.config.ts`) pour éviter la contention, et l'assertion de suppression a un timeout étendu à 20s. À revisiter si on passe sur un tier payant ou une DB locale (Docker).
- [ ] Les types Supabase (`src/lib/database.types.ts`) sont générés une fois via `supabase gen types typescript` — à régénérer après chaque nouvelle migration (P2b et suivantes), sinon les nouvelles tables ne seront pas typées.

## P2b — Catalogue d'exercices & génération de séances

- [ ] `session_template_exercises.exercise_id` référence `exercises` par FK sans vérifier que l'exercice est accessible (catalogue système ou exercice perso de l'utilisateur) au moment de l'insert — un utilisateur pourrait créer une référence "orpheline" vers l'exercice privé d'un autre utilisateur (deviné par UUID). Aucune fuite de données possible (RLS bloque toujours la lecture), juste une référence morte théorique. Pas prioritaire, mais à durcir avec une contrainte/trigger si on ouvre l'app à plusieurs utilisateurs réels.

## Architecture pivot (2026-07-21, post P2a/P2b) — voir mémoire "P2 architecture pivot"

- Cette phase a supprimé `blocks` (périodisation), `sessions` + `generate_block_sessions()` (pré-génération de séances). `focus` est passé du Bloc au Programme. Le vrai logging de séance (remplaçant `sessions`) est repoussé à P3 — c'est sa place naturelle, pas la peine de le tracker séparément ici.
- [ ] `pnpm audit` : 1 vulnérabilité modérée acceptée en connaissance de cause — `shadcn` (devDependency, requis pour `src/index.css` → `@import 'shadcn/tailwind.css'`) dépend transitivement de `@modelcontextprotocol/sdk` → `@hono/node-server` (CVE path traversal Windows-only dans `serve-static`, sert uniquement le registry MCP local de la CLI shadcn, jamais invoqué par cette app, jamais bundlé côté client). Pas de version corrigée disponible en amont (4.13.1 = dernière version au 2026-07-21). À revérifier périodiquement (`pnpm audit` + `npm view shadcn versions`).

## Jours fixes Lundi-Dimanche (2026-07-21)

- Remplacé la création manuelle de "jours" par une structure fixe : chaque programme a toujours exactement 7 `session_templates` (1=Lundi...7=Dimanche), auto-créées par le trigger `on_program_created` (même pattern que `handle_new_user` en P0). L'utilisateur bascule juste chaque jour entre "Repos" et "Entraînement" au lieu de nommer/ordonner des jours.
- RLS durcie : policies INSERT/DELETE retirées sur `session_templates` (seuls SELECT/UPDATE restent) — un utilisateur ne peut plus casser l'invariant "toujours 7 jours" en ajoutant/supprimant une ligne. Vérifié en live.
- `duplicateProgram()` adapté en conséquence : les 7 jours de la copie existent déjà (trigger), donc on les met à jour (`day_type` + exercices copiés) au lieu de les insérer.
- RPE : définition ajoutée en texte d'aide sous le champ (pas de tooltip — mobile-first, le hover ne fonctionne pas au toucher).

## Phase Profil (2026-07-21/22)

- Le Profil (mensurations, objectifs) est conçu pour permettre, plus tard, des recommandations d'entraînement pilotées par IA en fonction des objectifs déclarés (le user veut que "tout soit lié" — profil → programmes → IA). L'IA elle-même reste backlog (P9), ce qui a été fait ici : étendre `profiles` (P0) plutôt que créer une nouvelle table, avec des colonnes explicitement typées (`goal`, `sex` en check constraints) faciles à référencer depuis un futur prompt IA.
- Table `weight_entries` créée (time-series, une entrée par jour via `unique(user_id, recorded_at)` + upsert) — pas de graphique pour l'instant, c'est le scope de P5 (analytics). RLS complète (select/insert/update/delete, `auth.uid() = user_id`) vérifiée en live avec deux comptes jetables (isolation croisée testée : lecture, update, insert tous bloqués sur le profil/les pesées d'autrui).
- `src/pages/ProfilePage.tsx` : formulaire profil (`ProfileForm`, state initialisé directement depuis les props — pas de `useEffect` de synchro, pour éviter le anti-pattern "setState dans un effect" détecté par `eslint-plugin-react-hooks`) + section pesée (log du jour + liste + suppression via `ConfirmDialog`). Testé en live à 390px (mobile) : sauvegarde profil persistée après reload, log de pesée, suppression de pesée avec confirmation — tout fonctionne.
- Aucune nouvelle dépendance ajoutée ; `pnpm audit` toujours à 1 vulnérabilité modérée acceptée (voir note P2a/P2b ci-dessus, inchangée).

## Sélecteur d'exercice — search bar + visuels (2026-07-22)

- Remplacé le `<Select>` groupé par muscle group dans `ExerciseSlotFormDialog` par `src/components/ExercisePicker.tsx` : recherche live (filtrage local, pas le filtre interne de cmdk — `shouldFilter={false}`) + groupes par muscle avec une icône `lucide-react` par groupe (Dumbbell/Shirt/PersonStanding/BicepsFlexed/Footprints/Target/Activity pour pectoraux/dos/épaules/bras/jambes/core/full_body). "Visuels" = ces icônes de groupe, pas de vraies photos d'exercices — ça reste backlog tant que le user n'a pas envoyé ses exemples 21st.dev.
- Nouvelle dépendance : composant shadcn `command` (ajoute `cmdk` — maintenu, ~40k dl/semaine) + `input-group` (dépendance du composant `command`). `shadcn add` a de nouveau créé le dossier `@/` parasite à la racine (bug CLI connu, cf. notes P0/P1) — seuls `command.tsx`/`input-group.tsx` étaient réellement nouveaux, déplacés manuellement ; `button.tsx`/`input.tsx`/`textarea.tsx`/`dialog.tsx` régénérés étaient identiques (diff = guillemets simples vs doubles uniquement), donc ignorés.
- Bug mobile réel découvert en testant ce picker à 390px : `DialogContent` (`src/components/ui/dialog.tsx`) n'avait pas de `max-height`/`overflow-y-auto` — un formulaire un peu long (comme celui-ci une fois la liste d'exercices ajoutée) poussait son propre titre et son bouton de soumission hors viewport, sans aucun moyen de scroller pour les atteindre. Corrigé au niveau du composant de base (`max-h-[calc(100vh-2rem)] overflow-y-auto`) donc ça bénéficie à toutes les dialogs de l'app, pas seulement celle-ci.
- `e2e/session-structure.spec.ts` mis à jour : `#exercise-select` (Radix Select, supprimé) → `getByPlaceholder('Rechercher un exercice…')` + `getByRole('option', ...)` (cmdk rend ses items avec `role="option"`, confirmé dans le bundle `cmdk`).
- Testé en live à 390px : recherche/filtrage, sélection (coche + résumé "Sélection : X"), pré-remplissage en mode édition, scroll interne de la liste + scroll du dialog, soumission — tout fonctionne. Suite Playwright complète (7 tests) + Vitest (13 tests) verts après la modification.

## P3a — Log de séance, boucle de base (2026-07-22)

- Le "Log de séance" complet du brief (timer de repos, mode silencieux gym, saisie vocale, calculateur de plaques, offline-first) est découpé en sous-phases (voir échange avec le user) : **P3a** = boucle de base en ligne (ce qui est fait ici) ; backlog : P3b (timer + mode gym), P3c (offline-first IndexedDB/Dexie + sync — le brief le dit "non négociable", volontairement fait après pour ne pas construire la sync sur un modèle de données pas encore prouvé), P3d (calculateur de plaques + saisie vocale).
- Nouvelles tables `session_logs` (une instance réelle d'un jour de `session_templates`, statut in_progress/completed) et `session_log_sets` (séries réellement faites, rattachées au `session_template_exercise_id` planifié pour comparer prévu/réalisé). RLS identique au pattern déjà utilisé (`auth.uid() = user_id`, CRUD complet — pas de restriction comme sur `session_templates`, rien à protéger structurellement ici).
- ⚠️ **RLS non re-vérifiée en live pour cette phase** — le rate limit d'emails Supabase (`over_email_send_rate_limit`, déjà documenté en P1) a bloqué la création de comptes jetables au moment de tester. Vérification faite par relecture de code à la place (policies strictement identiques à celles de `weight_entries`/`profiles`, déjà vérifiées en live). À refaire en live dès que le SMTP dédié (tracké en P1) est en place, ou plus tôt si un service_role key est fourni.
- UI : bouton "Démarrer la séance" sur `SessionTemplateCard` (visible seulement si le jour a au moins un exercice planifié) → crée le log et navigue vers `/sessions/:id` (`SessionLogPage.tsx`). Par exercice : séries déjà loggées (avec suppression) + formulaire d'ajout rapide (charge/reps/RPE, reps pré-rempli avec la cible). "Terminer la séance" fige la vue en lecture seule (plus de formulaire ni de suppression de série). `ProgramDetailPage` affiche un historique des séances passées (jour + date + statut + suppression).
- Testé en live (flux complet) : démarrage, 2 séries loggées avec numérotation automatique, fin de séance, historique affiché correctement, suppression cascade propre. Nouveau test Playwright `e2e/session-log.spec.ts` couvre ce parcours de bout en bout.
- Bug rencontré pendant la génération des types : `supabase gen types typescript --linked > ...` a capturé des lignes de statut de la CLI ("Initialising login role...", un tag `<claude-code-hint>`, une notice de mise à jour) au début/fin du fichier généré, cassant le parsing TS. Nettoyé manuellement ; à surveiller si ça se reproduit (rediriger uniquement stdout ne suffit pas ici, la CLI semble mélanger les flux).

## Retours post-gate P3a (2026-07-22)

- Catalogue : ajout de "Romanian Deadlift (RDL)" et "Single Leg Press" (groupe `jambes`, cohérent avec "Stiff-Leg Deadlift"/"Single-Leg Romanian Deadlift" déjà présents dans ce groupe). Le sigle "(RDL)" est resté dans le nom à la demande explicite du user (migration de rename séparée après un premier essai sans sigle).
- Superset : nouvelle colonne `session_template_exercises.superset_group` (texte libre, nullable — ex "A"). Champ optionnel dans `ExerciseSlotFormDialog`, affiché en badge "Superset A" sur `SessionTemplateCard` (vue planification) et `SessionLogPage` (vue log). Portée volontairement limitée à un **tag visuel** pour cette passe — ça ne change pas le flux de saisie (toujours une carte par exercice, une série à la fois). Un flux de saisie alterné (A1 puis A2 puis A1...) resterait à faire si le user le demande, plus naturel à construire avec le mode gym de P3b.
- Dupliquer une série : bouton "Copy" à côté de chaque série déjà loggée dans `SessionLogPage`, ré-utilise `createSessionLogSet` avec les mêmes `actual_reps`/`actual_weight_kg`/`actual_rpe` que la série copiée, au prochain `set_number`.
- Vérifié en live (390px et desktop) : superset badge affiché correctement, duplication de série fonctionnelle. Confirmé par le user (gate 1✅ 2✅).

## P3b — Timer de repos & mode gym (2026-07-22)

- Nouvelle colonne `session_template_exercises.target_rest_seconds` (smallint, nullable, > 0) — même pattern optionnel que `target_rpe`. Champ "Repos (secondes)" dans `ExerciseSlotFormDialog`.
- `src/components/RestTimer.tsx` : décompte visuel (`font-mono text-5xl`), vibration (`navigator.vibrate`, no-op silencieux si absent — pas de support iOS Safari, limitation de plateforme documentée dans le composant, pas un bug) + bip sonore (Web Audio API, `try/catch` défensif pour ne jamais casser le flux de log si l'audio échoue). Boutons "+15s"/"-15s"/"Passer", tous en `h-12` (48px) — les tailles `sm`/`lg` du design system ne montent qu'à 28-36px, en dessous du minimum 44px que `design-system/MASTER.md` impose explicitement "critique en mode gym" ; overrides via `h-12!` plutôt que d'élargir tous les boutons de l'app.
- Le timer démarre automatiquement après chaque série loggée (ajout normal ou duplication), avec une `key` React sur l'id de la série créée pour forcer un vrai reset à chaque nouvelle série plutôt que de réutiliser l'état du timer précédent.
- Mode gym : Charge/Reps/RPE (`SessionLogPage`) et le bouton "Série N" passés à une taille plus grande (`h-12`, `text-lg`) spécifiquement sur cette page — pas un changement de thème global, l'app est déjà sombre par défaut partout.
- Bug de lint évité : `hasRung` gardé en `useRef` plutôt que `useState` dans `RestTimer` — c'est un simple garde anti-double-son, pas un état qui doit re-render, donc `setState` dans l'effect (anti-pattern déjà rencontré en Phase Profil) ne s'applique pas ici avec un ref.
- Testé en live : décompte jusqu'à 0, pas d'erreur console (vibration/audio), ajustement +15/-15, duplication qui relance le timer proprement. Nouvelle assertion Playwright dans `e2e/session-log.spec.ts` (apparition du timer + bouton "Passer" après une série).
- Backlog restant du "Log de séance" complet du brief : **P3c** (offline-first IndexedDB/Dexie + sync), **P3d** (calculateur de plaques + saisie vocale).

## Repos par défaut selon le focus du programme (2026-07-22)

- Retour du user : le repos de base ne devrait pas être une valeur unique (90s) mais dépendre du focus du programme (force / hypertrophie / endurance). Remplacé la constante `DEFAULT_REST_SECONDS = 90` par `DEFAULT_REST_SECONDS_BY_FOCUS: Record<ProgramFocus, number>` dans `sessions-api.ts` :
  - **Force (180s / 3min)** : charges quasi-maximales, récupération ATP-PCr plus complète nécessaire. Repères NSCA (Essentials of Strength Training and Conditioning) et de Salles et al. (2009, Sports Medicine, "Rest Interval between Sets in Strength Training") : ~3-5 min pour le travail force/puissance.
  - **Hypertrophie (90s)** : Schoenfeld, Pope et al. (2016, J Strength Cond Res, "Longer Interset Rest Periods Enhance Muscle Strength and Hypertrophy in Resistance-Trained Men") montrent qu'un repos de 3 min donne des gains de force ET d'hypertrophie égaux ou supérieurs à 1 min, probablement via un meilleur maintien de la charge/du volume sur les séries suivantes — l'ancien réflexe "60s pour la congestion" n'est plus bien soutenu par la littérature récente. 90s reste un compromis raisonnable pour des séries de travail courantes (un repos plus long serait défendable mais moins nécessaire qu'en force pure).
  - **Endurance (45s)** : repères classiques NSCA pour l'endurance musculaire (repos courts, largement sous la minute) pour maintenir le stress métabolique.
  - Ces valeurs restent des **points de départ éditables** — `target_rest_seconds` sur chaque exercice prime toujours sur le défaut du focus.
- Le focus doit maintenant descendre jusqu'à trois endroits : `ProgramDetailPage` (a déjà `program.focus`) → `SessionTemplateCard` (nouvelle prop `focus`) → `ExerciseSlotFormDialog` (nouvelle prop `focus`, utilisée pour le placeholder et le texte d'aide) ; côté log, `SessionLogPage` le lisait via `useProgram(log.program_id)` — depuis le passage offline-first (P3c), il vient du plan mis en cache (`plan.focus`), avec le même repli sur `'hypertrophie'` si le plan n'est pas encore disponible.
- Bug corrigé au passage : `duplicateProgram()` (`programs-api.ts`) copiait les slots d'exercice sans `target_rest_seconds` ni `superset_group` — la liste de champs de l'insert n'avait pas été mise à jour lors de l'ajout de ces deux colonnes dans les phases précédentes. Un programme dupliqué perdait silencieusement ces deux valeurs. Corrigé ; pas de test de duplication existant pour attraper ce genre de régression (`e2e/programs.spec.ts` ne teste que la création) — à surveiller si on ajoute d'autres champs aux exercices planifiés.
- Vérifié en live sur le programme réel "FORCE 2026" (focus Force) : tous les exercices sans repos explicite affichent maintenant "repos 180s" au lieu de l'ancien "repos 90s" fixe.

## P3c — Offline-first avec Dexie (2026-07-22)

- Portée : le domaine "log de séance" (`session_logs`, `session_log_sets`, plus le plan du jour nécessaire pour l'afficher) devient **local-first** — Dexie (IndexedDB) est maintenant la source de vérité pour la lecture ET l'écriture de cet écran, Supabase n'étant plus qu'une cible de synchronisation en arrière-plan. C'est le seul domaine de l'app concerné : la planification (programmes/exercices/profil) reste 100% en ligne, le brief n'exige l'offline que pour "la séance" elle-même.
- Nouveaux fichiers :
  - `src/lib/offline-db.ts` — schéma Dexie : `sessionPlanCache` (plan du jour mis en cache : nom du programme, focus, jour, exercices avec toutes leurs cibles), `sessionLogs`/`sessionLogSets` (miroir local, avec un champ `dirty: 'create'|'update'|'delete'|null`).
  - `src/lib/offline-sync.ts` — moteur de sync : `syncPendingChanges()` pousse chaque ligne `dirty` vers Supabase (logs avant sets, car un set ne peut pas être inséré avant que son log parent existe côté serveur), déclenché après chaque écriture locale + sur l'évènement `window online`. `refreshSessionLog(s)Cache`/`refreshSessionLogSetsCache` font l'inverse (lecture à travers le cache) sans jamais écraser une ligne qui a des changements locaux en attente.
  - Résilience ajoutée après coup : si le push échoue avec une violation de clé étrangère Postgres (code `23503` — le programme/log parent a été supprimé avant d'avoir synchronisé), la ligne est abandonnée plutôt que retentée indéfiniment (sinon elle boucle pour toujours sans jamais pouvoir réussir).
- Identifiants générés côté client (`crypto.randomUUID()`) dès la création, pas `gen_random_uuid()` côté Postgres — indispensable pour écrire hors-ligne, et rend la sync idempotente via `upsert` (un retry après succès non confirmé ne crée pas de doublon).
- `requireUserIdOffline()` lit `useAuthStore.getState().session?.user.id` (store Zustand en mémoire, peuplé une fois au démarrage) plutôt que `supabase.auth.getUser()`, qui fait un aller-retour réseau et échouerait hors-ligne. Limite connue et acceptée : si l'app reste hors-ligne assez longtemps pour que le JWT expire complètement (access + refresh), une réauthentification pourrait être nécessaire au retour — cas extrême pour une séance de sport, pas traité dans cette passe.
- `pnpm add dexie dexie-react-hooks` (déjà prévus dans le brief initial section 3) + `pnpm add -D fake-indexeddb` (shim IndexedDB pour les tests Vitest/jsdom, câblé dans `src/test/setup.ts`).
- Bug TanStack Query découvert et corrigé : `networkMode` par défaut (`'online'`) met les mutations en pause tant que `navigator.onLine` est faux — ça interceptait mes appels AVANT qu'ils atteignent mon propre code offline-safe (le bouton "Série N" restait bloqué en "pending" indéfiniment hors-ligne). Corrigé en ajoutant `networkMode: 'always'` à toutes les mutations de `useSessionLogs.ts` (elles n'ont plus besoin du réseau, seulement de Dexie).
- Deux corrections de lint dans le même esprit que les précédentes (Phase Profil, RestTimer) : `setState` dans un effect évité dans `useSessionLog` en trackant les ids déjà rafraîchis (`Set<string>`) plutôt qu'un booléen réinitialisé ; `varsIgnorePattern: '^_'` ajouté à `eslint.config.js` pour permettre de "jeter" un champ via destructuring (`const {dirty: _dirty, ...rest} = row`) sans déclencher `no-unused-vars`.
- Tests : `src/lib/offline-sync.test.ts` (9 cas — push réussi, échec laissé dirty, hors-ligne = no-op, ordre logs-avant-sets, sync groupée, suppression après delete réussi, delete gardé dirty si échec, abandon sur violation FK pour logs et sets) avec `@/lib/supabase` mocké. `e2e/offline-session-log.spec.ts` : coupe le réseau (`page.context().setOffline(true)`), logge une série, vérifie qu'elle s'affiche sans erreur ; remet le réseau, attend la sync, **vide IndexedDB puis recharge la page** — la série doit réapparaître uniquement si elle a vraiment atteint Supabase (preuve qu'un cache local périmé ne peut pas donner un faux positif).
- Ajout de `"DOM"` à `lib` dans `tsconfig.e2e.json` — nécessaire pour typer `indexedDB` dans le callback `page.evaluate()` du nouveau test.
- Backlog restant du "Log de séance" complet du brief : **P3d** (calculateur de plaques + saisie vocale). Offline-first et timer/mode gym (P3b) sont maintenant faits.

## Retour post-gate P3c : retry périodique (2026-07-22)

- Retour du user : pas pu tester sur téléphone (app en localhost, pas accessible depuis le mobile pour l'instant) — test physique reporté à une passe de fin. Point produit important : l'app sera utilisée à ~99% du temps avec la 4G active, pas en vrai mode avion — le cas réaliste du quotidien est plutôt "signal faible/qui coupe par intermittence" que "zéro réseau prolongé".
- Ça a révélé un vrai trou dans la stratégie de retry : `navigator.onLine` reflète seulement si la radio est allumée, pas si les requêtes aboutissent réellement — en 4G faible il peut rester `true` alors que tout timeout. Mon code gérait déjà correctement l'échec ponctuel (toute erreur laisse la ligne `dirty` pour un retry ultérieur), mais les seuls déclencheurs de retry étaient "juste après une écriture" et l'évènement `online` — une coupure de quelques secondes sans nouvelle saisie après pouvait laisser une ligne non synchronisée en attente indéfiniment.
- Ajouté un retry périodique (`setInterval`, 20s) dans `offline-sync.ts`, au niveau du module (tourne tant que l'onglet est chargé, peu importe la page affichée) plutôt que scopé à `SessionLogPage` — plus robuste si l'utilisateur navigue ailleurs pendant qu'une sync est en attente. `syncPendingChanges()` est déjà un no-op bon marché s'il n'y a rien à synchroniser, donc pas de coût réel à cette fréquence.
- Pas de test automatisé dédié à ce timer (mécanique triviale, la logique de sync elle-même est déjà couverte par les 9 tests unitaires existants) — mais à garder à l'esprit si le comportement de retry doit un jour être ajusté (fréquence, backoff, etc.) une fois testé en conditions réelles de 4G.

## P3d — Calculateur de plaques & saisie vocale (2026-07-22)

Ferme le "Log de séance" complet du brief (P3a boucle de base, P3b timer/mode gym, P3c offline-first, P3d ici) — tout dans `SessionLogPage`, là où le brief place ces deux features.

- **Calculateur de plaques** : `src/lib/plate-calculator.ts` (`calculatePlateBreakdown`, pur, testé — 7 cas) — algorithme glouton (plus grosse plaque en premier, par côté) sur un jeu de plaques standard `[25, 20, 15, 10, 5, 2.5, 1.25]` kg, barre par défaut 20kg. Le glouton est optimal ici (pas juste une heuristique) parce qu'un jeu de plaques de salle standard a la même propriété qu'un système de pièces "canonique" (chaque dénomination divise proprement dans les suivantes) — pas besoin d'une résolution par programmation dynamique. Si la charge cible n'est pas atteignable exactement (granularité), retourne le plus proche possible en dessous plutôt que d'échouer, et `PlateCalculatorDialog` l'affiche clairement ("le plus proche : Xkg").
  - Jeu de plaques et poids de barre pas encore configurables par l'utilisateur (barre éditable dans le dialogue à chaque usage, mais pas persistée comme préférence, et le jeu de plaques est fixe) — backlog si le user veut une salle avec un inventaire différent.
  - `PlateCalculatorDialog.tsx` : bouton "Calculateur de plaques" dans le formulaire de saisie de série, pré-rempli avec la charge déjà tapée dans "Charge (kg)" à l'ouverture (pas de sync automatique dans l'autre sens — modifier la charge cible dans le dialogue n'affecte pas le champ du formulaire, ce sont deux valeurs indépendantes une fois le dialogue ouvert).
- **Saisie vocale** : Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`), pas dans les types DOM standards (spec pas finalisée) — interfaces minimales déclarées dans `useVoiceSetInput.ts` plutôt que `any`. Pas de support iOS Safari connu à ce jour (support Web Speech absent/inégal) — détection de fonctionnalité (`isSupported`) fait disparaître le bouton "Dicter la série" plutôt que d'afficher un contrôle qui ne marcherait jamais, même logique que pour l'API Vibration en P3b.
  - `src/lib/voice-set-parser.ts` (`parseVoiceSetInput`, pur, testé — 8 cas) : extrait charge/reps/RPE d'une transcription via mots-clés ("kilos", "reps"/"répétitions", "rpe") ; repli sur "premier nombre = charge, deuxième = reps" si aucun mot-clé n'est dit (ordre naturel à l'oral : "quatre-vingts, huit"). Parseur "best effort", pas du NLP complet — suppose que le moteur de reconnaissance transcrit déjà les nombres dits en chiffres (comportement réel de Chrome/Android), gère la virgule décimale française.
  - Le résultat vocal **remplit les champs sans soumettre automatiquement** — l'utilisateur voit et confirme avant de valider "Série N", pour éviter qu'une mauvaise reconnaissance loggue une série silencieusement fausse.
- Tests : 15 tests unitaires purs (7 calculateur + 8 parseur vocal) ; `e2e/plate-calculator.spec.ts` (calcul + changement du poids de barre recalculant en direct). Pas de test e2e pour la saisie vocale — Playwright n'a pas de moyen pratique de simuler l'API Web Speech, et le microphone déclencherait une demande de permission navigateur bloquante en test automatisé ; couverture via les tests unitaires du parseur + vérification manuelle en direct (boutons affichés, dialogue calculateur fonctionnel, valeurs correctes).

## Analytics — 1RM, progression, régularité (2026-07-22)

- Ferme la section "Analytics et progression" du brief. Nouvelle page `/analytics` (lien dans `AppLayout`, icône `ChartLine`), **en ligne uniquement** — comme Programmes/Profil, pas dans le périmètre offline-first de P3c (c'est une feature de reporting/réflexion, pas quelque chose utilisé en pleine série).
- `src/lib/one-rep-max.ts` : formules Epley et Brzycki, moyennées pour le nombre affiché. Fiables seulement jusqu'à ~12 répétitions (au-delà les deux formules divergent significativement) — `estimateOneRepMax` retourne `null` hors de cette plage plutôt qu'un nombre trompeur ; la charge max réelle reste affichée même quand le 1RM ne l'est pas.
- `src/lib/analytics-api.ts` (`fetchSetHistory`) : une seule requête Supabase avec double embed (`session_log_sets` → `session_template_exercises` → `exercises`, et → `session_logs` pour la date) ramène tout l'historique de séries de l'utilisateur, toutes séances/programmes confondus — nécessaire car la progression "par exercice" doit grouper par `exercise_id` (le catalogue partagé), pas par `session_template_exercise_id` (qui est spécifique à un jour d'un programme donné).
- `src/lib/analytics.ts` (pur, testé — 15 cas) : `getLoggedExercises` (liste pour le sélecteur, seulement les exercices avec de l'historique réel), `computeOneRepMaxProgression` (meilleur 1RM estimé par jour, par exercice), `computeWeeklyTonnage` (tonnage par semaine ISO, lundi comme premier jour — cohérent avec `WEEKDAY_LABELS`), `computeDailyVolume` (alimente la heatmap).
- `src/components/ContributionHeatmap.tsx` : grille façon GitHub, faite maison (CSS grid `grid-flow-col` + `grid-rows-7`, pas de librairie dédiée — juste une grille de cases colorées, pas besoin d'une dépendance de plus pour ça). Niveaux d'intensité calculés en tiers relatifs au jour le plus chargé de la fenêtre affichée. Logique de dates extraite dans `buildHeatmapDays` (paramètre `now` injectable) pour être testable sans mocker `Date` globalement — 5 tests couvrant l'alignement sur lundi et le cas où "aujourd'hui" est un dimanche.
- **Bug réel trouvé et corrigé en vérifiant en direct** : le graphe de tonnage hebdomadaire affichait "19 juil." pour une donnée dont la vraie date (lundi) était le 20. Cause : `toLocaleDateString('fr-FR', {...})` sans `timeZone` explicite utilise le fuseau local du navigateur pour formater une date-calendrier (pas un instant) — sur une machine en UTC-10, minuit UTC du 20 juillet devient 14h locale le 19. Corrigé avec `timeZone: 'UTC'` dans `formatShortDate` (`AnalyticsPage.tsx`). Le même bug préexistait dans `ProfilePage.tsx` (date des pesées, `recorded_at` — une colonne `date` pure, pas un timestamptz) : corrigé au passage. Distinction importante retenue : `session_logs.started_at` (un vrai instant, timestamptz) doit au contraire s'afficher en heure locale du visiteur — c'est correct tel quel dans `ProgramDetailPage`, pas touché.
- `recharts` ajouté (suggéré dans le brief section 3, actif/maintenu). Chargement paresseux (`React.lazy` + `Suspense`) pour la route `/analytics` — la lib pèse à elle seule ~110kB gzippés, ne vaut pas la peine d'alourdir le bundle initial de chaque autre page. Sans ce découpage, le chunk principal serait passé de ~273kB à ~382kB gzippés.
- Testé en live sur les vraies données du user (programme réel avec des séries "Hack Squat" loggées) : heatmap, tonnage hebdo et progression 1RM s'affichent correctement après la correction du fuseau horaire.
- Historique "par séance" déjà couvert par `ProgramDetailPage` (P3a) ; "par semaine" couvert par le graphe de tonnage hebdo. Pas de vue "historique global toutes séances confondues" séparée construite pour cette phase — backlog si le besoin se précise.

## Retour post-gate Analytics : contraste des graphiques (2026-07-22)

- Retour du user (au survol/tap sur "Tonnage hebdomadaire") : le rectangle de survol Recharts par défaut est un gris clair pensé pour un thème clair — jarrant sur le thème sombre de l'app, avec en plus un contour noir disgracieux sur la barre active.
- Corrigé sur les deux graphiques (`Tonnage hebdomadaire` et `Progression par exercice`) : `Tooltip.cursor` remplacé par une teinte primary/border très subtile au lieu du gris clair par défaut ; `labelStyle`/`itemStyle` du tooltip fixés explicitement en `--popover-foreground` (ne plus dépendre des couleurs par défaut de Recharts, pensées pour un fond clair) ; `activeBar`/`activeDot` stylés pour rester dans la palette du thème plutôt que d'hériter du contour noir par défaut.
- Vérifié en live (survol + zoom sur la tooltip) : plus de rectangle gris, contraste correct, cohérent avec la philosophie d'élévation du design system ("un ton de surface plus clair plutôt que des ombres lourdes").

## Dashboard — HomePage condense les données (2026-07-22)

- `HomePage.tsx` réécrit (était un placeholder quasi-vide depuis P0). Quatre cartes, dans cet ordre : **Aujourd'hui** (jour planifié du programme actif + démarrer/continuer/revoir la séance, ou statut repos, ou invite à activer un programme) → **Dernière séance** (recap : exercice/charge/reps + 1RM estimé du dernier jour loggé, réutilise `getMostRecentHighlight`) → **Cette semaine** (nombre de séances + heatmap compacte 8 semaines, réutilise `ContributionHeatmap`) → **Poids** (dernière pesée + tendance ↑/↓ + distance à l'objectif). Carte "Mes programmes" existante conservée en bas comme nav secondaire.
- **Bug réel trouvé en construisant "Aujourd'hui" — corrigé dans la même passe** : aucune UI ne permettait de passer un programme en statut `active` depuis sa création (toujours `draft` par défaut, jamais changé nulle part). Le dashboard n'aurait donc jamais pu afficher la séance du jour pour un utilisateur réel. Ajouté un contrôle segmenté (Brouillon/Actif/Archivé) sur `ProgramDetailPage`, même pattern que le toggle jour de repos/entraînement déjà utilisé sur `SessionTemplateCard`. Vérifié en live sur "FORCE 2026" (programme réel) : activé → "Aujourd'hui" affiche "FORCE 2026 — Mardi" avec le bouton Démarrer.
- Simplification assumée : plusieurs programmes peuvent techniquement être `active` en même temps (pas de contrainte d'unicité en base) — le dashboard prend le plus récemment créé parmi les actifs. Pas de désactivation automatique des autres programmes à l'activation d'un nouveau ; à revoir si ça crée de la confusion en usage réel.
- "Aujourd'hui" compare la date du jour en **heure locale** (pas UTC) — contrairement au bucketing des graphes Analytics (qui reste en UTC pour la cohérence entre lignes de données historiques), ici on veut vraiment "quel jour est-ce pour l'utilisateur maintenant", donc heure locale est le bon choix. Deux préoccupations différentes, résolues différemment, documenté en commentaire dans `HomePage.tsx`.
- `countTrainingDaysThisWeek`/`getMostRecentHighlight` ajoutés à `analytics.ts` (purs, testés — 5 cas), réutilisent `getIsoWeekStart` (maintenant exporté) pour rester cohérents avec le graphe de tonnage hebdo.
- Nouveau test e2e `e2e/dashboard.spec.ts` : calcule le jour de la semaine réel au moment du test (pas de jour codé en dur), crée un programme, ajoute un exercice sur le jour du jour, l'active via le nouveau contrôle, vérifie que le dashboard propose de démarrer, démarre, revient sur le dashboard et vérifie "Continuer la séance".
- En creusant un échec de ce test (une race condition sur le cache TanStack Query après avoir changé le statut), vérification directe en base via REST a révélé 3 programmes de test orphelins (comptes e2e-fixture uniquement, jamais la donnée réelle) laissés par d'anciennes itérations de debug qui avaient échoué avant d'atteindre leur étape de nettoyage — pas un bug de l'app (le `deleteProgram` fonctionne correctement, vérifié directement), juste de la debris de session nettoyée au passage.

## Backlog — idées futures (2026-07-21)

Notées par le user après le gate Dashboard, explicitement "à réfléchir par la suite" — pas scopées, pas planifiées.

- [ ] **Assistant IA sur le dashboard** : chat pour lancer un entraînement, créer un programme, etc. de façon conversationnelle. Probablement la matérialisation naturelle de la "couche IA" du brief (section 4) une fois construite — voir aussi la note P2 sur le lien profil → recommandations IA.
- [ ] **Curseurs de progression sur le dashboard** : indicateurs visuels pour l'atteinte d'objectif, l'obtention d'un nouveau PR, etc. — distinct des graphiques Analytics existants, plus dans l'esprit "coup d'œil" du dashboard.
- [ ] **Aspect social** : partage d'évolution entre utilisateurs, feed avec PR et photos d'évolution. Le user a été explicite : **à construire en tout dernier**, après tout le reste (implique déjà un modèle multi-tenant, mais des considérations produit/privacy supplémentaires vu que ça expose des données à d'autres utilisateurs — à cadrer sérieusement le moment venu, pas juste "ajouter du partage").
- [ ] Sélecteur d'exercice avec visuels réels (photos) — toujours en attente des exemples 21st.dev du user (rappel, déjà noté depuis P2).

## P4a — BYOK : gestion des clés API (2026-07-21)

Démarre la "couche IA" du brief (section 4). Contrainte produit explicite du user : chaque utilisateur fournit sa propre clé API (pas de clé partagée côté app) sur Anthropic et/ou OpenAI. Architecture décidée avec le user avant implémentation : clé chiffrée côté serveur (pas 100% client) + relai par Edge Function, les deux providers dès le départ, `Suggestion de deload/recommandation` comme première feature IA prévue pour P4b.

- **Stockage** : `supabase_vault` (pgsodium), déjà activé sur le projet hébergé (`create extension if not exists` a juste confirmé l'existant — pas eu besoin de l'activer manuellement). Nouvelle table `ai_provider_keys` (`user_id`, `provider` check `in ('anthropic','openai')`, `vault_secret_id` → `vault.secrets(id)` `on delete cascade`, `is_valid`, `last_validated_at`) — la ligne ne contient jamais la clé, seulement une référence opaque vers Vault. RLS : policy `select` uniquement pour `authenticated` (`auth.uid() = user_id`) ; volontairement **aucune** policy insert/update/delete — ces écritures passent exclusivement par les Edge Functions via `service_role`, qui gèrent le secret Vault et la ligne de façon atomique.
- **Wrappers Vault** (`public.ai_vault_create_secret/update_secret/read_secret/delete_secret`, `security definer`, `search_path = ''`) : nécessaires car le schéma `vault` n'est pas exposé par PostgREST (seul `public` l'est) — les Edge Functions appellent ces wrappers en RPC via `supabase-js` plutôt que d'ouvrir une connexion Postgres directe. `revoke all ... from public, anon, authenticated` + `grant execute ... to service_role` : vérifié après coup via `information_schema.routine_privileges` que seuls `postgres`/`service_role` ont l'EXECUTE — un `authenticated` ne peut ni lire ni écrire une clé même en appelant les wrappers directement. Les 4 wrappers testés individuellement en direct (`supabase db query --linked`, create → read → update → read → delete → confirmation que la ligne `vault.secrets` a bien disparu) avant d'écrire les Edge Functions par-dessus.
- **Trois Edge Functions** (premières du projet — `supabase/functions/` n'existait pas avant P4a) : `ai-key-save` (valide la clé auprès du provider avant de la stocker, upsert si une clé existait déjà pour ce provider), `ai-key-test` (revalide une clé déjà stockée sans que le user ait à la ressaisir — déchiffre côté serveur via le wrapper, jamais renvoyé au client), `ai-key-delete` (supprime le secret Vault ; la ligne `ai_provider_keys` disparaît par cascade FK, pas de delete explicite sur la table). Toutes vérifient le JWT de l'appelant via un client anon-key avant de créer un second client `service_role` pour les écritures — le `user_id` vient systématiquement du JWT vérifié, jamais du corps de la requête.
- **Validation de clé à coût nul** : les deux providers exposent un endpoint `GET /v1/models` qui ne vérifie que l'auth sans consommer de tokens de complétion — utilisé pour valider une clé à la saisie (P4a) et lors du "retester" sans dépenser un seul appel payant. `AbortController` avec timeout 10s sur ces appels (un provider qui ne répond pas ne doit pas laisser la fonction bloquée indéfiniment).
- **Vérifié en direct** (curl avec un vrai JWT du compte e2e-fixture) : clé invalide → 422 avec message clair par provider, rien n'est persisté (confirmé par lecture directe de la table après coup) ; provider inconnu → 400 ; pas d'Authorization header → 401 ; tentative d'insert direct sur `ai_provider_keys` avec le anon/authenticated JWT → 403 RLS (confirme qu'un client ne peut pas contourner les Edge Functions). Reproduit aussi dans le navigateur (Paramètres IA, section Profil) : clé Anthropic invalide affiche "Clé Anthropic invalide ou révoquée." en direct sous le champ, le badge reste "Non configurée".
- **Non vérifié cette session, à faire par le user** : le chemin "clé valide" de bout en bout (P4a suppose que `GET /v1/models` réussit avec une vraie clé Anthropic/OpenAI — mécaniquement identique au chemin déjà testé côté erreur, mais pas rejoué avec un vrai secret) ; isolation RLS testée uniquement via écriture directe bloquée (403), pas via un second compte réel lisant les clés du premier — la policy est le même pattern `auth.uid() = user_id` que `weight_entries`/`programs`/etc., déjà éprouvé ailleurs dans le projet, mais pas re-testé en croisé ici faute d'un second compte de test.
- **UI** : nouvelle section "Paramètres IA" dans `ProfilePage` (`AiSettingsSection.tsx`), une carte par provider avec statut (badge "Non configurée"/"Validée ✓"/"Invalide"), champ mot de passe masqué, bouton retester, suppression via `ConfirmDialog` existant. Les deux champs "Clé API" avaient initialement le même label accessible ("Clé API") — corrigé en "Clé API — {provider}" (ex. "Clé API — Anthropic (Claude)") avant même d'écrire le test e2e, un vrai souci d'accessibilité (deux champs de formulaire indiscernables au lecteur d'écran), pas juste une commodité de test.
- **Nouveauté d'outillage** : `eslint.config.js` ignore désormais `supabase/functions/**` — runtime Deno (globals `Deno.*`, imports `jsr:`), pas dans le tsconfig de l'app Vite/Node, donc hors du projet TypeScript qu'ESLint utilise pour le reste du repo.
- Test e2e (`e2e/ai-settings.spec.ts`) : couvre le chemin clé invalide (le seul rejouable sans vrai secret provider). Pas de test pour le chemin clé valide, pour la même raison que les tests API n'existent pas encore dans ce repo (aucun précédent `*-api.test.ts`) — jugé cohérent avec la convention existante plutôt que d'introduire un nouveau style de test pour ce module seul.

## Retour post-P4a : la couche IA passe en toute fin de projet (2026-07-21)

- Retour du user juste après le gate P4a (BYOK terminé et vérifié, hors test avec vraie clé) : la suite de la couche IA (P4b — recommandations/deload, puis l'assistant conversationnel) est **explicitement reportée à la toute fin du projet**, au même rang que l'aspect social déjà noté "à construire en tout dernier" plus haut dans ce fichier.
- P4a lui-même reste committé et en prod tel quel (migration + Edge Functions déjà live sur Supabase, code testé) — rien à défaire. C'est la suite (branchement d'une vraie feature IA sur le pipe BYOK) qui est reportée, pas l'infrastructure de gestion des clés.
- Prochaine reprise de ce fil : revoir `TODOS.md` section P4a pour le contexte technique, puis reprendre au choix du provider par défaut / première feature (voir discussion P4b dans l'historique de conversation) une fois le reste du brief traité.

## P5a — Data ownership : export JSON et suppression de compte (2026-07-22)

Ferme la première moitié de la section "data ownership" du brief (l'import, plus complexe — remapping d'IDs sur un graphe imbriqué programme→séances→séries — reste en P5b, non commencé).

- **Piège trouvé avant même d'écrire le code** (vérifié en lisant toutes les migrations) : `ai_provider_keys.vault_secret_id` référence `vault.secrets(id) on delete cascade`, mais dans le sens "suppression du secret Vault → suppression de la ligne", pas l'inverse. Une suppression de compte qui se contenterait de `auth.admin.deleteUser()` aurait laissé les secrets Vault (clés Anthropic/OpenAI) orphelins indéfiniment — la cascade FK ne les touche pas. La nouvelle Edge Function `account-delete` nettoie donc explicitement Vault en premier (boucle sur les `ai_provider_keys` du user, `ai_vault_delete_secret` par ligne), puis appelle `admin.deleteUser()` qui cascade tout le reste (`profiles`, `programs`, `session_templates`, `session_template_exercises`, `session_logs`, `session_log_sets`, `weight_entries` — confirmé par lecture de **toutes** les migrations avant d'écrire le code : chaque table a `on delete cascade` vers `auth.users`, directement ou transitivement).
- **Export 100% client** (`src/lib/data-export-api.ts`) : pas d'Edge Function nécessaire, RLS scope déjà toutes les lectures à l'utilisateur courant — même pattern que le reste de l'app. Un objet JSON plat, un tableau par table (`weight_entries`, `exercises` custom uniquement, `programs`, `session_templates`, `session_template_exercises`, `session_logs`, `session_log_sets`) plus `profile`, avec `schema_version`/`exported_at`. `user_id` retiré de chaque ligne (implicite — c'est déjà "les données de cet utilisateur"), `id` conservé pour permettre un remapping de relations lors d'un futur import (P5b). `ai_provider_keys` volontairement absent de l'export — rien d'exportable (juste une référence Vault opaque), et pas une "donnée utilisateur" au sens où on l'entend ici.
- Les types exportés sont dérivés de `Database['public']['Tables'][T]['Row']` via `Omit`/`Pick` plutôt que redéfinis à la main — reste synchronisé automatiquement si une colonne est ajoutée plus tard, cohérent avec l'intention "exporter vraiment tout".
- Téléchargement via `Blob` + `<a download>` synthétique (`fitness-export-YYYY-MM-DD.json`), aucun backend impliqué.
- **Edge Function `account-delete`** (nouvelle) : réutilise directement `_shared/http.ts` de P4a (`requireAuthedContext`) sans modification — le helper était déjà générique, pas spécifique aux clés IA malgré son emplacement.
- Après suppression côté serveur, le client vide aussi le cache Dexie local (`sessionPlanCache`, `sessionLogs`, `sessionLogSets` — offline-first depuis P3c, données qui existent en dehors de Supabase) puis `supabase.auth.signOut()` et redirection vers `/login`.
- Refactor de passage : extrait `invokeEdgeFunction` (avec son extraction de message d'erreur depuis la réponse de la Edge Function) de `ai-keys-api.ts` vers un nouveau `src/lib/edge-function.ts` partagé — la logique était identique à ce qu'il fallait pour `deleteAccount()`, dupliquer aurait été le seul autre choix.
- **Vérifié en direct, pas juste par lecture du code** : création d'un compte jetable via l'API Admin (`POST /auth/v1/admin/users`, évite le rate-limit d'emails déjà rencontré sur ce projet — voir note plus haut dans ce fichier sur `over_email_send_rate_limit`), ajout d'une vraie pesée + d'un vrai secret Vault avec sa ligne `ai_provider_keys` associée (créés directement en SQL, hors flux `ai-key-save`, pour isoler le test du piège Vault), appel de `account-delete` avec le JWT du compte jetable, puis confirmation en base que `auth.users`, `profiles`, `weight_entries`, `ai_provider_keys` **et** `vault.secrets` sont tous à zéro pour cet id — le piège identifié plus haut est bien corrigé, pas seulement en théorie.
- Test e2e (`e2e/data-export.spec.ts`) : couvre l'export (téléchargement intercepté via `page.waitForEvent('download')`, contenu JSON vérifié — présence de tous les tableaux attendus, absence de `ai_provider_keys`). Pas de test e2e pour la suppression de compte — trop destructif pour tourner dans la suite partagée (supprimerait potentiellement le compte fixture e2e si mal isolé) ; vérifié à la place manuellement avec un compte jetable dédié, comme décrit ci-dessus.

## P5b — Import JSON (2026-07-22)

Ferme la section "data ownership" du brief. Import ajouté dans `src/lib/data-export-api.ts` (`parseUserDataExport` + `importUserData`), même fichier que l'export, réutilise le type `UserDataExport`.

- **`session_templates` n'a pas de policy INSERT — piège trouvé en écrivant le premier jet, corrigé avant même de tester** : chaque programme reçoit ses 7 jours fixes (lundi-dimanche) via un trigger `on_program_created` à la création (`20260721220320_fixed_weekdays_session_templates.sql`), et la policy insert a été explicitement supprimée à l'époque pour préserver l'invariant "toujours exactement 7 jours". Importer un jour ne peut donc jamais être un insert — le code récupère les 7 jours déjà auto-créés sur le nouveau programme (matchés par `day_of_week`, la vraie clé naturelle de la table) et fait un `update` de leur `day_type` plutôt qu'un insert. Repéré en lisant la policy RLS live (`pg_policies`) avant d'écrire le code, pas après un échec — mais la suite a quand même révélé deux bugs réels, tous deux trouvés en testant le scénario "exporter puis réimporter dans le même compte" via e2e (`e2e/data-import.spec.ts`), un cas que rien d'autre dans le repo n'exerçait jusqu'ici :
  1. **Collision de tri sur `created_at`** : la première version préservait `created_at`/`updated_at` de l'export sur les lignes recréées (programmes, exercices, `session_template_exercises`, `session_logs`, `session_log_sets`) — cohérent en apparence avec la philosophie "restaurer les vraies données", mais un programme réimporté dans le même compte se retrouvait avec le **même `created_at`, à la microseconde près**, que l'original. Comme `fetchPrograms()` trie par `order by created_at desc` sans tie-breaker, Postgres ne garantit aucun ordre stable entre deux lignes à égalité — la liste changeait d'ordre de façon non déterministe entre deux refetch, un vrai bug produit (pas juste un souci de test) pour quiconque réimporte dans un compte non-vide. Corrigé : seules les dates réellement porteuses de sens métier restent préservées (`recorded_at` des pesées, `started_at`/`completed_at` des séances loguées — celles dont dépendent les graphes Analytics) ; `created_at`/`updated_at`, simples métadonnées de ligne sans lecteur applicatif, ne sont plus reportés — chaque ligne importée reçoit un timestamp frais.
  2. **`invalidateQueries()` sans filtre dans `useImportUserData`, combiné à un bug préexistant dans `usePrograms.ts`** : `useDeleteProgram`/`useCreateProgram`/`useDuplicateProgram` avaient `onSuccess: () => queryClient.invalidateQueries(...)` (corps d'expression, donc la promesse est **retournée**, pas juste déclenchée) — `mutateAsync()` attend cette promesse avant de se résoudre. Supprimer un programme depuis sa page de détail invalide `['programs']`, qui matche en préfixe la query `['programs', id]` **encore montée** (on est toujours sur cette page) ; son refetch échoue (la ligne vient de disparaître) et retry avec backoff avant que l'invalidation ne se résolve — le bouton de suppression restait désactivé plusieurs secondes, parfois au-delà du timeout de test. `useUpdateProgram` avait déjà le bon pattern (`void queryClient.invalidateQueries(...)`, non bloquant) ; aligné `useCreateProgram`/`useDeleteProgram`/`useDuplicateProgram` dessus. Bug préexistant, pas introduit par l'import — mais l'invalidation globale (`invalidateQueries()` sans clé) de la première version de `useImportUserData` ajoutait une charge de fond qui le rendait beaucoup plus visible ; corrigé aussi en scopant l'invalidation de l'import aux 5 clés réellement concernées (`profile`, `weight-entries`, `programs`, `exercises`, `set-history`) plutôt qu'une invalidation globale.
  - **Backlog** : le même pattern bloquant (`onSuccess` sans `void`) existe dans `useAiProviderKeys.ts`, `useExercises.ts`, `useSessionTemplates.ts`, `useWeightEntries.ts` — pas corrigé ici faute de preuve concrète d'impact (aucun test ne l'a fait apparaître), mais candidat à un passage de nettoyage dédié.
- **Sémantique d'import** : chaque ligne recréée avec un nouvel id (remapping en mémoire, `Map<ancien_id, nouvel_id>` par table) — réimporter ou importer dans un compte non-vide crée des doublons visibles plutôt qu'une fusion ou une corruption silencieuse. Seule exception : `weight_entries`, upsert sur `(user_id, recorded_at)` (vraie clé naturelle de la table). `exercise_id` sur `session_template_exercises` : remappé si l'exercice est un exercice custom présent dans l'export, laissé tel quel sinon (catalogue partagé, supposé stable sur le même projet).
- Pas de transaction Postgres — insertions séquentielles, erreurs accumulées ligne par ligne dans un tableau retourné à l'UI (`ImportResult { imported, errors }`), cohérent avec le reste du code où la logique métier vit en TypeScript et Postgres ne fait que tables + RLS.
- UI : nouvelle sous-section dans "Mes données" (`DataOwnershipSection.tsx`) — sélection de fichier → résumé du contenu (nombre de programmes/séances/pesées/etc. détectés) → confirmation (`ConfirmDialog`) → import. Le trigger et le bouton de confirmation avaient initialement le même texte ("Importer") — corrigé en "Confirmer l'import" pour le bouton de la modale, à la fois pour l'accessibilité (deux boutons visibles simultanément avec le même label) et pour que les tests e2e puissent les cibler sans ambiguïté.
- Tests e2e (`e2e/data-import.spec.ts`) : fichier JSON invalide → erreur claire, rien n'écrit ; export réel d'un programme puis réimport dans le même compte → doublon créé avec un nouvel id, nettoyé en fin de test via l'UI de suppression. Le test de suppression attend explicitement que la liste reflète chaque suppression avant de continuer (la query de liste n'étant pas "active" pendant qu'on est sur une page de détail, l'invalidation ne la refetch pas immédiatement — elle affiche ses dernières données connues le temps qu'un refetch en arrière-plan se termine) plutôt que de supposer la liste à jour dès que l'URL change.

## P6a — Planning bien-être : activités récurrentes + vues jour/semaine (2026-07-22)

Première moitié de la section "Planning bien-être quotidien" du brief (les notifications push PWA restent en P6b — nécessitent une vraie nouvelle brique d'infra, VAPID + service worker custom + scheduler serveur, volontairement séparées pour valider le modèle de données et l'UI d'abord).

- **Modèle de récurrence** : même pattern que `session_templates` (jours fixes 1-7) plutôt qu'un système général — `wellness_activities` (`name`, `days_of_week smallint[]`, `reminder_time` nullable, `active`) + `wellness_activity_logs` (une ligne = "fait" un jour donné, `unique(activity_id, completed_date)`, décocher = simple delete). `reminder_time` est déjà dans le schéma bien qu'inexploité avant P6b, pour éviter une deuxième migration.
- **`days_of_week`** contraint par `check (days_of_week <@ array[1..7]::smallint[])` plutôt que d'ajouter une table de jointure — cohérent avec la simplicité du reste du modèle jour-fixe.
- **UI** : nouvelle page `/bien-etre`, nav ajoutée dans `AppLayout` (icône `Sparkles`). Vue jour/semaine via le même segmented control (`Button` variant default/ghost dans un pill bordé) déjà utilisé pour `day_type`/`status` ailleurs dans l'app — pas de nouveau composant `Switch`/`Toggle`, le projet n'en a pas et n'en avait pas besoin. Formulaire de création/édition (`ActivityFields`) partagé entre la création inline (pattern `WeightSection`) et l'édition en `Dialog` — évite de dupliquer le multi-toggle de jours.
- **Vue semaine** : grille faite maison (pas de librairie), un cercle par jour par activité — vide/gris si non programmé, contour si programmé mais pas fait, rempli si fait. Dates de la semaine calculées en **heure locale** (comme "Aujourd'hui" du dashboard, pas comme le bucketing UTC d'Analytics — même distinction déjà documentée pour le dashboard).
- **`noUncheckedIndexedAccess`** (déjà actif dans `tsconfig.app.json`) a forcé deux ajustements propres plutôt que des `!` dispersés : `getCurrentWeekDates` retourne un tuple `[string,string,string,string,string,string,string]` (7 éléments garantis par construction) au lieu de `string[]`, et un helper `weekdayLabel(day)` centralise l'unique assertion non-null nécessaire (clé toujours 1-7 par construction, jamais un lookup arbitraire).
- **Vérifié en direct dans le navigateur — incident et correction** : en testant manuellement, l'onglet Chrome avait une session déjà persistée sur le **vrai compte** (`arnaud.zeh@gmail.com`) plutôt que le compte e2e-fixture — ma tentative de connexion avec les identifiants de test n'a rien fait puisque déjà authentifié. Une activité de test ("Wim Hof matinal") a été créée par erreur sur le vrai compte, repérée en comparant l'UI à une requête directe en base (`user_id` ne correspondait pas au compte e2e-fixture attendu), supprimée immédiatement après confirmation du user. Session déconnectée avant de poursuivre ; la suite de la vérification s'est faite exclusivement via les tests e2e Playwright (isolés par construction via `e2e/.auth/user.json`), pas de nouvelle session manuelle dans le navigateur.
- Tests e2e (`e2e/wellness.spec.ts`) : création avec le jour du jour réel sélectionné (calculé au moment du test, pas codé en dur — même principe que `dashboard.spec.ts`) → apparaît en vue jour, "Marquer comme fait" → persiste après un rechargement complet de page (pas juste un état local), visible aussi en vue semaine, nettoyage en fin de test. Deuxième test : le bouton "Ajouter" reste désactivé sans nom et sans jour sélectionné.
- **Trouvé et nettoyé au passage** : le compte e2e-fixture avait accumulé 18 programmes de test orphelins (dont 16 nommés identiquement "E2E Analytics …", débris de runs interrompus pendant les sessions de debug intensif de P5b/P6a aujourd'hui — le test correspondant a bien un cleanup, juste jamais atteint quand le run était interrompu avant la fin). Ce volume ralentissait suffisamment l'import (P5b) pour dépasser le timeout du test — pas une régression de code, juste de la donnée de test à nettoyer. Compte remis à zéro (0 programme, catalogue d'exercices partagé intact).

## P6b — Notifications push PWA pour le planning bien-être (2026-07-22)

Deuxième moitié de la section "Planning bien-être quotidien" du brief. `reminder_time` existait déjà dans le schéma depuis P6a (colonne inexploitée) ; P6b ajoute toute l'infrastructure pour réellement envoyer un rappel au moment prévu.

- **Service worker custom** : bascule de `generateSW` (stratégie par défaut de `vite-plugin-pwa`, utilisée jusque-là) à `injectManifest` — `generateSW` génère un SW opaque sans place pour du code custom, alors qu'un rappel push a besoin d'écouter `push` et `notificationclick` soi-même. `src/sw.ts` : précache via `workbox-precaching` (`precacheAndRoute(self.__WB_MANIFEST)`, seule brique conservée de `generateSW`), `push` affiche une notification à partir du payload JSON (titre/corps/URL, avec un fallback générique si le payload est absent ou malformé), `notificationclick` referme la notification et navigue/focus un client existant vers l'URL (`/bien-etre` par défaut) ou ouvre une nouvelle fenêtre sinon.
- **Paire de clés VAPID** générée une fois (`web-push generate-vapid-keys`) : la publique va dans `VITE_VAPID_PUBLIC_KEY` (`.env.local`, safe à exposer côté client — c'est le principe même de la paire publique/privée), la privée uniquement comme secret d'Edge Function (`supabase secrets set VAPID_PRIVATE_KEY=...`), jamais commitée nulle part.
- **`profiles.timezone`** (IANA, ex. `Europe/Paris`) : `reminder_time` est une heure murale saisie dans le navigateur, mais le scheduler tourne côté serveur en UTC — sans fuseau stocké, un rappel à 7h partirait à 7h UTC. Auto-détecté côté client (`Intl.DateTimeFormat().resolvedOptions().timeZone`) et synchronisé silencieusement à chaque chargement de l'app (`syncTimezone()` dans `profile-api.ts`, appelé depuis un `useEffect` de `AppLayout` — donc sur toute route authentifiée, pas seulement `/bien-etre`) ; best-effort, une erreur réseau est avalée (`.catch(() => {})`) plutôt que de casser l'affichage pour une donnée non bloquante. Comparaison avant écriture pour éviter un `update` inutile à chaque visite quand le fuseau n'a pas changé.
- **`push_subscriptions`** (nouvelle table, P6a→P6b) : un abonnement par `endpoint` (déjà unique par device/navigateur de par la Push API elle-même), RLS standard scopée `auth.uid() = user_id`. **Piège trouvé après coup, corrigé dans une migration séparée** : la migration initiale n'avait que select/insert/delete, pas de policy update — or le client fait un `upsert(..., { onConflict: 'endpoint' })` pour gérer le cas "même navigateur se réabonne" (ex. après avoir désactivé puis réactivé), et le chemin `ON CONFLICT DO UPDATE` nécessite une policy UPDATE même si l'intention initiale était "jamais de update direct". Migration `20260722160000` ajoutée plutôt que d'éditer la migration déjà poussée en base.
- **`wellness_reminder_sends`** : registre d'envoi interne au scheduler, RLS activée sans **aucune** policy pour `authenticated` (même raisonnement que l'absence de policy insert sur `session_templates`) — `unique(activity_id, sent_date)` sert de verrou d'idempotence : si deux exécutions du cron se chevauchent, la seconde reçoit une violation de contrainte sur l'insert et saute l'envoi plutôt que de notifier deux fois.
- **Edge Function `send-wellness-reminders`** : pour chaque activité active avec un `reminder_time`, convertit l'heure serveur (UTC) en heure murale du fuseau du propriétaire (`Intl.DateTimeFormat` avec `timeZone` + `hourCycle: 'h23'`), compare au `reminder_time`/jour de la semaine de l'activité, insère dans `wellness_reminder_sends` (verrou d'idempotence ci-dessus) puis envoie via `npm:web-push` à tous les abonnements du user. Un abonnement qui répond 404/410 (device désinstallé, permission révoquée, données navigateur effacées) est auto-supprimé de `push_subscriptions` plutôt que retenté indéfiniment.
- **Auth de la fonction — deux essais, le premier a échoué en test réel, pas juste en théorie** : cette fonction n'est appelée que par `pg_cron`/`pg_net`, jamais par un utilisateur final — pas de JWT de session à vérifier. Premier essai : comparer l'en-tête `Authorization` à `Bearer ${SUPABASE_SERVICE_ROLE_KEY}` (le secret auto-injecté par la plateforme dans chaque Edge Function). Testé en direct avec la vraie clé `service_role` récupérée via `supabase projects api-keys` : rejeté (401) — la valeur auto-injectée dans le runtime des fonctions ne correspond pas à celle renvoyée par cette commande CLI (probablement un format de clé différent/rotation). Abandonné au profit d'un secret dédié et généré par nous (`CRON_SECRET`, 32 octets aléatoires), stocké à la fois comme secret d'Edge Function et dans Supabase Vault (`vault.create_secret(..., 'cron-invoke-secret')`), envoyé via un en-tête custom `x-cron-secret` plutôt que `Authorization` — celui-ci est de toute façon intercepté par la vérification JWT de la plateforme *avant* que le code de la fonction ne s'exécute (confirmé en observant `UNAUTHORIZED_NO_AUTH_HEADER`, une erreur qui ne vient pas de notre code). Fonction déployée avec `--no-verify-jwt` (`[functions.send-wellness-reminders] verify_jwt = false` dans `config.toml`, pour que les futurs redéploiements gardent ce comportement) puisqu'il n'y a structurellement aucun JWT à vérifier ici.
- **`pg_cron` + `pg_net`** (migration `20260722150000`, jamais activés sur ce projet avant P6b) : job `send-wellness-reminders-every-minute` (`* * * * *`) qui appelle `public.invoke_send_wellness_reminders()`, une fonction `security definer` qui lit `cron-invoke-secret` depuis `vault.decrypted_secrets` puis fait `net.http_post` vers l'Edge Function avec l'en-tête `x-cron-secret` — le secret n'apparaît jamais en clair dans une migration committée, seulement au moment de l'exécution côté serveur.
- **Vérifié en direct, pas juste par lecture du code** : `curl` sans en-tête → 401 (plateforme) ; avec `x-cron-secret` invalide → 401 (notre check) ; avec la vraie valeur → `200 {"sent":0,"skipped":0}` (aucun abonnement/activité en base au moment du test, réponse attendue). Après le `db push` de la migration cron, confirmé `cron.job` contient bien le job actif puis attendu ~75s en tâche de fond (`Monitor`) pour voir une ligne apparaître dans `cron.job_run_details` avec `status = 'succeeded'` — le job tourne réellement en prod, pas seulement "créé sans jamais s'exécuter".
- **Client — abonnement push** (`src/lib/push-api.ts`, `src/hooks/useNotifications.ts`) : `getNotificationSupport()` distingue trois états — `supported` (Notification/serviceWorker/PushManager présents), `ios-not-installed` (Safari iOS n'expose la Web Push API qu'à une PWA ajoutée à l'écran d'accueil ; sans ça `Notification`/`PushManager` sont simplement absents du `window`, indiscernable de "pas supporté du tout" sans détecter spécifiquement ce cas via `navigator.userAgent` + `matchMedia('(display-mode: standalone)')`), et `unsupported` (carte masquée entièrement). `subscribeToPush` : demande la permission, `pushManager.subscribe()` avec la clé VAPID publique convertie en `Uint8Array`, puis `upsert` de l'endpoint/clés dans `push_subscriptions`. `unsubscribeFromPush` : supprime la ligne DB puis appelle `subscription.unsubscribe()` (dans cet ordre — si la ligne DB restait après un `unsubscribe()` réussi, le scheduler tenterait d'envoyer à un endpoint mort jusqu'au prochain cycle d'auto-nettoyage 404/410).
- **Détail TypeScript** : `Uint8Array.from(...)` type désormais en `Uint8Array<ArrayBufferLike>`, que `PushSubscriptionOptionsInit.applicationServerKey` (qui attend un `ArrayBufferView<ArrayBuffer>`) rejette — remplacé par `new Uint8Array(length)` rempli par boucle, dont le buffer sous-jacent est garanti `ArrayBuffer` (pas `SharedArrayBuffer`). Erreur qui n'apparaissait qu'en `tsc -b` (mode projet, utilisé par `npm run build`), pas en `tsc --noEmit` seul — piège à surveiller si un futur diagnostic type-check en local ne matche pas ce que `npm run build` rapporte.
- **UI** : nouvelle carte "Notifications" sur `/bien-etre` (au-dessus du formulaire de création), masquée si `unsupported`, message dédié "ajoutez à l'écran d'accueil" si `ios-not-installed`, sinon bouton "Activer/Désactiver les notifications" reflétant l'état d'abonnement réel du navigateur (pas juste un flag local — relit `pushManager.getSubscription()` au montage).
- **Non vérifié cette session, à faire par le user** : la réception réelle d'une notification (créer une activité avec un `reminder_time` proche, s'abonner depuis un vrai navigateur, attendre le rappel, cliquer dessus pour confirmer l'ouverture sur `/bien-etre`) — délibérément non testé via le navigateur automatisé cette session : la vérification manuelle P6a a montré qu'un onglet Chrome peut porter une session déjà authentifiée sur le **vrai compte** plutôt que le compte e2e-fixture, et s'abonner réellement pousserait une vraie ligne `push_subscriptions` + potentiellement un vrai push vers ce compte. Toute l'infrastructure serveur (auth de la fonction, cron, idempotence) a en revanche été vérifiée en direct comme détaillé ci-dessus.
- Tests : `e2e/wellness.spec.ts` étendu d'un test vérifiant que la carte "Notifications" s'affiche avec un bouton "Activer les notifications" actif (Chromium headless expose bien `Notification`/`serviceWorker`/`PushManager`) — le clic réel n'est pas testé (déclencherait un vrai abonnement contre le service de push de Google depuis l'environnement de test, potentiellement flaky et laissant une ligne persistante sur le compte fixture), cohérent avec le même choix déjà fait pour le chemin "clé IA valide" en P4a.

## P7 — Hypoxie intermittente (2026-07-22)

Décisions confirmées avec le user avant implémentation : pas de presets pré-remplis (liste vide au départ, cohérent avec le reste de l'app), un seul point d'entrée dans le workflow de séance (lien "Respiration" dans le header de `SessionLogPage`, utilisable à tout moment plutôt qu'un bouton dupliqué par exercice), et l'historique des sessions complétées **est** loggé (contrairement à un premier réflexe "pas d'analytics pour l'instant") — pose les bases d'un futur affichage sans devoir remonter le schéma plus tard.

- **Modèle de données** (migration `20260722170000`, même structure double-table que `wellness_activities`/`wellness_activity_logs` en P6a) : `breath_protocols` (`name`, `hold_seconds`, `recovery_seconds`, `cycles`, tous `smallint check (... > 0)`) + trigger `set_updated_at` réutilisé tel quel ; `breath_session_logs` (`protocol_id` → `on delete cascade`, `completed_cycles`, `started_at`, `completed_at`) en select/insert/delete seulement, pas d'update — un log ne se modifie pas après coup, même raisonnement que `wellness_activity_logs`.
- **Définition de "cycle complété"** : un cycle compte comme fait dès que sa phase d'**apnée** est terminée — la récupération n'est que la pause avant le cycle suivant, pas elle-même le "travail". Un arrêt manuel en cours de récupération du cycle N logge donc `completed_cycles = N` ; un arrêt en cours d'apnée du cycle N logge `N - 1`. Rien n'est loggé si l'utilisateur arrête avant même d'avoir terminé une seule apnée (`completed_cycles = 0` volontairement pas inséré — une tentative avortée immédiatement n'est pas une "session").
- **Minuteur plein écran** (`BreathRunner` dans `BreathPage.tsx`) : state machine pure (`RunnerState { phase, cycleIndex, secondsLeft }` + fonction `advance()` déterministe) plutôt qu'un enchaînement de `useState` épars — apnée → récup, répété `cycles` fois, puis `phase: 'done'`. Décompte par `setTimeout` récursif (même pattern que `RestTimer`), avec un `hasFiredRef` remis à `false` à chaque tick normal pour garantir qu'une transition de phase ne se déclenche qu'une seule fois par passage à zéro — nécessaire car mettre l'appel `vibrate`/`playBeep` directement dans le callback de `setState` aurait été invoqué deux fois par le double-appel StrictMode des updater functions en dev (piège identifié avant d'écrire le code, pas après un bug observé) ; les effets de bord restent donc dans le corps de l'effet, jamais dans un updater `setState`.
- **Refactor de passage** : `playBeep`/`formatTime` (et l'appel `navigator.vibrate`, désormais `vibrate()`) extraits de `RestTimer.tsx` vers `src/lib/timer-feedback.ts` — deuxième minuteur ayant besoin exactement de la même logique de retour haptique/sonore, une vraie déduplication plutôt qu'une abstraction prématurée. `RestTimer.tsx` importe désormais ces helpers sans changement de comportement (vérifié par la suite e2e existante, toujours verte).
- **"Passer"** (skip) appelle directement `advance()` sans passer par le décompte — pas de vibration/son sur un skip manuel (signal déjà explicite, contrairement à une transition automatique). **"Arrêter"** logge les cycles réellement complétés puis ferme l'overlay ; **"Pause"** suspend simplement le `useEffect` de décompte (`paused` bool), sans toucher au state de phase/cycle.
- **UI** : nouvelle page `/apnee` (icône `Wind` dans `AppLayout`, jusque-là inutilisée dans l'app), même structure 3-parties que `WellnessPage` (`CreateProtocolCard` / `ProtocolsListCard` / `EditProtocolDialog`, champ `ProtocolFields` partagé création/édition). Le minuteur est un overlay `fixed inset-0` custom (pas le composant `Dialog` shadcn, pensé pour une boîte centrée, pas un plein écran immersif façon "mode gym") monté conditionnellement depuis `BreathPage` plutôt qu'une route séparée — évite les edge cases de bouton retour du navigateur en plein protocole.
- **Point d'entrée séance** : bouton "Respiration" dans le header de `SessionLogPage`, à côté de "Terminer la séance"/"Supprimer" — simple lien vers `/apnee`, pas d'ouverture directe du minuteur (l'utilisateur choisit son protocole une fois sur la page), utilisable avant, entre deux exercices ou après la séance sans duplication par carte d'exercice.
- **Vérifié en direct** : build complet (`tsc -b && vite build`, y compris régénération du service worker), `tsc --noEmit`, ESLint, 67 tests unitaires. Tests e2e (`e2e/breath.spec.ts`) : un run réel à son terme (apnée 1s / récup 1s / 1 cycle, décompte réel observé jusqu'à "Protocole terminé 🎉", pas juste le bouton "Passer"), le bouton "Passer" testé séparément (avance immédiatement sans attendre), et le formulaire de création rejeté sans champs requis. Suite e2e complète (22 tests) rejouée in extenso après l'ajout, aucune régression. Fixture account confirmé à 0 ligne `breath_protocols`/`breath_session_logs` après nettoyage des tests (cascade delete du protocole entraîne bien la suppression de son historique).

## P8 — Module cycles menstruels (2026-07-22)

Décisions confirmées avec le user avant implémentation : saisie minimale (juste les dates de début de règles, phase calculée depuis l'historique — pas de date de fin ni de notes), activation via un toggle dans le Profil (`profiles.cycle_module_enabled`, désactivé par défaut, aucune icône de nav ni donnée tant que non activé), et pas d'intégration Dashboard pour ce premier passage (page dédiée `/cycle` uniquement).

- **Contrainte non négociable du brief respectée explicitement** : "aucune donnée sensible en localStorage non chiffré, notamment sur le module cycles" — `cycle_entries` ne passe **pas** par le cache offline Dexie (contrairement à `session_logs`/`session_log_sets` depuis P3c) ; lecture directe Supabase à chaque visite de `/cycle`, aucune persistance locale au-delà du cache en mémoire de TanStack Query (qui disparaît à la fermeture de l'onglet, jamais écrit sur disque).
- **Calcul de phase, volontairement pas de l'IA** : fonction pure `computeCyclePhase()` (`src/lib/cycle-phase.ts`), testée unitairement (11 tests, `cycle-phase.test.ts` — mêmes conventions que `one-rep-max.test.ts`). Longueur de cycle moyenne dérivée de l'écart entre dates de début consécutives (défaut 28j avec moins de 2 entrées) ; jour d'ovulation estimé par `longueur_cycle − 14` (la phase lutéale dure ~14j de façon relativement stable, contrairement à la phase folliculaire qui varie davantage — approximation standard, pas une position fixe dans le cycle). Le "jour du cycle" boucle (`modulo`) si aucune nouvelle date n'a été saisie au-delà de la longueur estimée, pour continuer à afficher une estimation plutôt que de s'arrêter net.
- **Conseils par phase = texte statique**, pas de calcul personnalisé ni d'appel LLM (`src/lib/cycle-advice.ts`, `CYCLE_PHASE_ADVICE`) : l'app ne trace aucune donnée nutritionnelle, donc rien à adapter numériquement — les recommandations "nutrition" du brief (besoins caloriques/glucidiques, vigilance déficit énergétique) restent volontairement qualitatives. Cohérent avec la couche IA différée à la fin du projet.
- **Disclaimer non-médical** : bandeau toujours visible en haut de `/cycle` (pas seulement au premier accès), mentionnant explicitement que ce ne sont pas des recommandations médicales et qu'elles ne tiennent pas compte de conditions particulières (SOPK, endométriose, contraception hormonale) — au-delà du minimum "cadré comme recommandations générales" du brief.
- **Modèle de données** (migration `20260722180000`) : `profiles.cycle_module_enabled boolean not null default false` (nouvelle colonne, pas une table à part — cohérent avec le reste du profil) ; `cycle_entries` (`start_date`, `unique(user_id, start_date)` pour empêcher un doublon silencieux à la même date) en CRUD complet (contrairement aux tables de logs pures du reste de l'app comme `wellness_activity_logs`/`breath_session_logs` — une date de règles mal saisie se corrige plus naturellement par édition que par delete+recreate, d'où une policy update ici).
- **UI** : toggle "Activer/Désactiver" instantané dans `ProfilePage` (`CycleModuleSection`, même pattern "bouton bascule" que le "Active/Inactive" de `WellnessPage`, pas de soumission de formulaire séparée) ; icône `Moon` dans `AppLayout` rendue conditionnellement (`profile?.cycle_module_enabled`, réutilise le `useProfile()` déjà en cache côté `HomePage`/`ProfilePage` — aucun fetch dupliqué) ; accès direct à `/cycle` sans activation → message d'invitation avec lien vers le Profil plutôt que la page complète.
- **Bug trouvé et corrigé pendant les tests e2e, pas dans l'app** : le premier jet du test utilisait `new Date().toISOString().slice(0, 10)` (date UTC) pour saisir "aujourd'hui" dans le champ `type="date"`, alors que `CyclePage` calcule "aujourd'hui" en date **locale** (`todayLocalDate()`, mêmes composants `getFullYear`/`getMonth`/`getDate` que `WellnessPage`/`toLocalDateString`) — l'environnement de test tournant en UTC-10 (Tahiti), les deux dates ne coïncidaient pas et le jour de cycle calculé ne correspondait pas à l'attendu du test. Corrigé dans le test (même fonction `todayLocalDate()` reproduite), le code applicatif était correct dès le départ.
- **Vérifié en direct** : `tsc --noEmit`, ESLint, build complet, 78 tests unitaires (dont les 11 nouveaux sur le calcul de phase). Tests e2e (`e2e/cycle.spec.ts`, `describe.serial` car les tests dépendent de l'état d'activation les uns des autres, avec un `afterAll` de nettoyage best-effort) : accès à `/cycle` désactivé → message d'invitation, pas l'icône de nav ; activation depuis le Profil → icône `Cycle` apparaît ; ajout d'une date du jour → "Phase actuelle : Menstruelle", jour 1/28, conseils entraînement+nutrition affichés, puis suppression ; désactivation → icône disparaît. Suite e2e complète rejouée (26 tests) : une seule flakiness observée sur `wellness.spec.ts` (carte notifications, timeout après la suite longue), non reproduite en isolation — sans lien avec ce module.

## Audit pré-merge Lighthouse/PWA (2026-07-22/23)

Dernier point du brief avant les items explicitement différés (couche IA, social). Objectif : passer l'app (build de production, pas le dev server) au crible Lighthouse + vérifier que les promesses PWA (installable, hors-ligne) tiennent vraiment, pas juste sur le papier.

- **La catégorie "PWA" de Lighthouse n'existe plus** : Google l'a retirée de l'outil (confirmé sur la version 13.4.1 installée ici — `--only-categories` n'accepte plus que `accessibility, best-practices, performance, seo, agentic-browsing`, et les audits historiques `installable-manifest`/`service-worker`/`splash-screen`/`maskable-icon` ont disparu de `--list-all-audits`, pas juste été déplacés). Plutôt que sauter cette partie de l'audit, remplacé par une vérification fonctionnelle directe (scripts Playwright jetables, supprimés après usage) : `navigator.serviceWorker.controller` bien défini après un premier reload (le SW contrôle réellement la page, pas juste "enregistré"), et surtout un vrai **reload à froid avec `context.setOffline(true)`** sur plusieurs routes — un test qui prouve le comportement plutôt que de vérifier des heuristiques statiques.
- **Bug réel trouvé par ce test offline, absent de tout ce qui existait avant** : le reload à froid échouait purement et simplement (`net::ERR_INTERNET_DISCONNECTED`) sur **toutes** les routes, y compris `/`. Cause : `src/sw.ts` (écrit à la main pour P6b, cf. section P6b) fait `precacheAndRoute(self.__WB_MANIFEST)` mais ne route que des URLs exactement précachées — `index.html` est bien caché, mais aucune règle ne mappe une navigation vers `/bien-etre` (ou même `/`) vers ce fallback. `generateSW` (la stratégie utilisée avant P6b) injecte cette redirection automatiquement ; le passage à `injectManifest` (nécessaire pour les listeners `push`/`notificationclick`) l'a fait disparaître silencieusement, et rien ne l'avait détecté depuis. **Corrigé** par un handler `fetch` écrit à la main (`event.request.mode === 'navigate'` → sert `matchPrecache('/index.html')`) plutôt que par `workbox-routing`/`NavigationRoute` (l'approche standard documentée) — cf. point suivant.
- **`workbox-routing` incompatible avec le SW de dev de `vite-plugin-pwa`, découvert en testant, pas en théorie** : la première version du fix utilisait `NavigationRoute`+`registerRoute` (`workbox-routing`), qui fonctionnait parfaitement une fois buildé en production (`vite build` + `vite preview`, reload offline vérifié sur `/`, `/login`, `/bien-etre`) mais cassait **l'enregistrement même du service worker** en mode `pnpm dev` (`devOptions.enabled: true`, activé depuis P6b pour tester les notifications push localement) — `ServiceWorker script evaluation failed`, sans détail exploitable renvoyé par `register()`. Isolé en retirant le SW à un `precacheAndRoute` minimal (l'enregistrement repassait), confirmant que le module ESM pré-bundlé de `workbox-routing` ne survit pas au harnais de dev SW de `vite-plugin-pwa` alors que `workbox-precaching` seul le supporte. Plutôt que de creuser cette incompatibilité plus loin, réécrit le fallback à la main avec `matchPrecache` (déjà exporté par `workbox-precaching`, déjà éprouvé) — quelques lignes, une dépendance en moins, et ça évite la classe entière du problème. `workbox-routing` retiré de `package.json`.
- **Cet enregistrement cassé avait un effet de bord concret** : il a fait régresser silencieusement `e2e/wellness.spec.ts` ("carte notifications... bouton actif") — `usePushSubscription` attend `navigator.serviceWorker.ready`, une promesse qui ne se résolvait plus jamais quand l'enregistrement échouait, laissant le bouton bloqué `disabled`. Repéré, diagnostiqué et corrigé avant de committer, pas après ; suite e2e complète rejouée pour confirmer.
- **`manifest.webmanifest` avait `lang: "en"` par défaut** (valeur par défaut de `vite-plugin-pwa` quand `lang` n'est pas explicitement fourni), alors que l'app entière est en français (`<html lang="fr">`). Ajouté `lang: 'fr'` à la config `manifest` de `vite.config.ts`. Pas de champ maskable icon — backlog, nécessite un vrai artwork avec zone de sécurité, pas juste une case à cocher.
- **Découpage du bundle par route (`React.lazy`)** : toutes les pages sauf `LoginPage`/`SignupPage`/`HomePage` (déjà chargées avant/juste après connexion, pas de bénéfice à les retarder) sont passées en lazy, avec un seul `<Suspense>` partagé autour de `<Outlet />` dans `AppLayout` plutôt qu'un par route (`AnalyticsPage` avait déjà ce pattern depuis Analytics — généralisé, plus de wrapper `Suspense` dupliqué dans `App.tsx`). Effet réel mesuré : le chunk principal passe de 979 Ko à 384 Ko minifiés ; une bonne partie du reste (react-dom, radix-ui, supabase-js, tanstack-query) se retrouve dans un chunk partagé nommé arbitrairement par Rollup (`card-*.js`, ~500 Ko) toujours chargé en `modulepreload` — attendu, ce sont des dépendances réellement partagées par `LoginPage` elle-même, pas une réoptimisation manquée. Le gain concret : le JS **spécifique** à `ProgramDetailPage`/`ProfilePage`/`SessionLogPage`/etc. ne fait plus partie du chemin critique de `/login`. Poussé plus loin (vendor-splitting manuel de React/Radix) jugé hors scope pour un projet solo sans pression de trafic — backlog si le besoin se fait vraiment sentir.
- **Trouvaille accessibilité réelle (WCAG), pas cosmétique** : `link-in-text-block` signalait le lien "Créer un compte" de `LoginPage` — contraste insuffisant (1.97:1, minimum 3:1) avec le texte environnant et aucun style (soulignement) pour le distinguer visuellement en dehors du survol. Corrigé sur `LoginPage` et son jumeau structurel `SignupPage` ("Se connecter") : soulignement permanent (`underline`, plus seulement `hover:underline`), couleur qui change au survol plutôt que l'inverse — même esprit que `DialogDescription` qui a déjà ce pattern. Score accessibilité Lighthouse : 93 → 100. D'autres liens `hover:underline` existent ailleurs (HomePage, ProgramDetailPage) mais ce sont des lignes isolées, pas du texte mélangé à un lien — Lighthouse ne les signale pas, laissés tels quels.
- **`meta description` et `robots.txt` ajoutés** — l'app est un outil personnel authentifié, jamais destiné à être indexé : `robots.txt` (`Disallow: /`) est le choix correct, pas un hack de score. Effet secondaire attendu et accepté : le score SEO Lighthouse **baisse** (82 → 63, l'audit `is-crawlable` échoue délibérément) puisque Lighthouse suppose par défaut qu'on veut être indexé — normal ici, pas un problème à corriger.
- **Métriques de performance déjà saines** : `Cumulative Layout Shift = 0`, `Total Blocking Time = 0 ms` sur toutes les pages auditées (`/login`, `/`, `/analytics`, `/bien-etre`) — pas de layout instable ni de janktes. FCP/LCP ~2.9s sous l'émulation mobile "slow 4G" de Lighthouse, attendu pour une SPA sans SSR, pas dégradé par les changements de cette phase.
- **Effet de bord découvert et nettoyé en cours de route, pas un bug de cette phase** : la suite e2e complète a régressé deux fois avec des échecs génériques sur "Créer le programme" à travers 8 specs différentes — tracé jusqu'à un cache d'optimisation Vite corrompu par des `rm -rf node_modules/.vite` répétés **pendant que le serveur de dev tournait encore** (mes propres scripts de diagnostic pendant cet audit), pas un vrai bug produit ; résolu par un redémarrage propre. Séparément, le compte e2e-fixture avait accumulé 121 programmes de test orphelins (mêmes scripts de diagnostic, plus des runs de test interrompus) qui faisaient dépasser le timeout du test d'import — même classe de problème que celui déjà documenté en P6a, nettoyé de la même façon (`delete` scopé par `user_id`, compte remis à zéro).
- **Vérifié en direct** : audits Lighthouse (performance/accessibility/best-practices/seo) sur `/login`, `/` (Dashboard), `/analytics`, `/bien-etre` via le build de production servi par `vite preview`, avec un vrai Chrome piloté par Playwright (authentifié via le compte e2e-fixture pour les pages protégées, un navigateur séparé pour `/login`). Vérification fonctionnelle du hors-ligne (précédemment cassé, maintenant réparé) confirmée sur `/`, `/login`, `/bien-etre`. Suite complète rejouée à plusieurs reprises pendant le debug (build + dev), verte à 26/26 au final, `tsc --noEmit`/ESLint/78 tests unitaires également au vert.
- **Non fait, backlog explicite** : vendor-splitting manuel du bundle partagé (`card-*.js`, ~500 Ko), icône maskable pour Android (nécessite un artwork dédié), et un audit Lighthouse Performance plus poussé en conditions réseau réelles (l'émulation "slow 4G" par défaut n'est qu'une approximation).

## P9a — Couche sociale, fondations : opt-in, 1RM auto-détecté, feed (2026-07-23)

Dernier point du brief avant la couche IA (toujours différée). Décisions confirmées avant implémentation : cercle fermé opt-in (pas de système d'ami/abonnement — tout utilisateur inscrit voit le feed de qui a activé le partage), photos + jalons élargis dès la cible finale mais **découpé en sous-phases** (P9a = fondations + 1RM seul + feed texte ; P9b = tonnage hebdo + streak ; P9c = photos) pour rester dans la taille de phase habituelle du projet.

- **Première brèche RLS du projet, cadrée avec soin** : `profiles.social_sharing_enabled` (désactivé par défaut) contrôle la visibilité. `milestones` (nouvelle table) : select `auth.uid() = user_id or exists(... social_sharing_enabled)`, **aucune policy insert/update** — un jalon n'est jamais écrit par le client, uniquement par un trigger `security definer` sur `session_log_sets`. Une policy **delete** a été ajoutée (voir plus bas, pas dans le jet initial) pour que chacun puisse retirer son propre jalon du feed.
- **`profiles` a des colonnes sensibles** (`date_of_birth`, `cycle_module_enabled`, `target_weight_kg`...) qu'une policy RLS "opted-in" directe sur la table exposerait en entier (RLS filtre des lignes, pas des colonnes). Solution : vue `public.public_profiles` (`id`, `display_name` seulement), créée **sans** `security_invoker` — elle tourne donc avec les droits du propriétaire (comportement par défaut, peut lire toute la table) mais ne projette que ce qui est sûr. La policy existante sur `profiles` (`auth.uid() = id`) reste intacte pour toute requête directe sur la table.
- **Bug RLS réel trouvé en vérifiant avec un vrai compte, pas en le lisant** : la policy `milestones` initiale faisait `exists (select 1 from public.profiles p where p.id = ... and p.social_sharing_enabled)` — mais `profiles` est elle-même restreinte à `auth.uid() = id`. Depuis la session d'un AUTRE utilisateur, cette sous-requête ne pouvait donc jamais voir la ligne qu'elle cherchait à vérifier, rendant la branche "opted-in" **toujours fausse**, peu importe le flag réel. Repéré uniquement parce que la vérification a été faite via un vrai compte jetable + de vraies requêtes REST authentifiées (pas du SQL superuser, qui contourne RLS et aurait masqué le bug) : `milestones` renvoyait `[]` pour un jalon pourtant bien opted-in. Corrigé (migration `20260723110000`) en faisant pointer la sous-requête vers `public_profiles` (qui contourne RLS par construction) plutôt que `profiles` directement. Reproductible : n'importe quelle policy RLS qui référence une AUTRE table protégée par RLS est elle-même sujette à cette récursion — à vérifier systématiquement pour toute future policy inter-tables.
- **Détection de PR, formule identique au client** : `estimate_one_rep_max(weight, reps)` en SQL (`immutable`) reproduit exactement `estimateOneRepMax` (`src/lib/one-rep-max.ts` — moyenne Epley/Brzycki, 1-12 reps, poids > 0). Trigger `after insert on session_log_sets` : calcule le 1RM du nouveau set, le compare au max historique de cet utilisateur sur cet exercice (hors la ligne courante), insère un jalon si nouveau record. `exercise_name` est un **instantané** copié depuis `exercises.name` au moment de la détection (pas juste `exercise_id`) — un exercice custom d'un utilisateur reste lisible dans le feed d'un autre utilisateur qui n'a pas accès à ce catalogue custom (RLS d'`exercises` scope les exercices non-système par propriétaire), en évitant complètement le problème plutôt qu'en le contournant.
- **Policy delete ajoutée après coup, pas dans le jet initial** (migration `20260723100000`) : découverte en écrivant le test e2e que les jalons créés pendant les tests s'accumulaient indéfiniment sur le compte fixture (rien ne les supprime — contrairement aux programmes, ils ne sont pas rattachés par cascade à `session_logs`, volontairement, pour rester une "histoire" indépendante). Plutôt que de bricoler un nettoyage hors-bande, ajouté une vraie fonctionnalité produit raisonnable : chacun peut supprimer son propre jalon du feed (bouton dédié sur `FeedPage`, `ConfirmDialog`, visible seulement sur ses propres entrées).
- **Effet de bord accepté, documenté** : les specs e2e pré-existantes qui loguent des séries sur "Squat" (`session-log.spec.ts`, `session-structure.spec.ts`, `data-import.spec.ts`) déclenchent maintenant elles aussi le trigger et laissent des jalons sur le compte fixture — croissance bornée (une ligne par valeur de 1RM réellement atteinte une première fois, jamais de doublon pour une valeur identique) et sans impact sur la justesse du feed, mais visible comme "bruit" si quelqu'un consulte ce compte. Pas retouché ces specs (hors scope de cette phase) ; nettoyage manuel ponctuel si besoin.
- **UI** : toggle "Activer/Désactiver" dans `ProfilePage` (`SocialSharingSection`, même pattern bouton-bascule que `CycleModuleSection`). Icône `Trophy` dans `AppLayout`, **toujours visible** (contrairement à l'icône `Cycle` qui est masquée tant que non activée) — voir son propre feed ou celui des autres ne nécessite pas d'avoir soi-même activé le partage, seule la nuance "partager MES progrès" est opt-in. Page `/feed` : liste chronologique, nom d'utilisateur, type de jalon + exercice + valeur + date.
- **Piège de test trouvé et corrigé, pas un bug applicatif** : le premier jet du test e2e cliquait sur `getByRole('button', {name: 'Activer'})` sans le scoper à sa carte — `ProfilePage` a maintenant deux sections avec des boutons "Activer"/"Désactiver" au texte identique (Module cycles + Partage de progrès), rendant le sélecteur ambigu. A cassé un test **déjà existant** (`cycle.spec.ts`) en plus du nouveau. Corrigé en scopant les deux tests à leur carte respective (`[data-slot="card"]` filtré par le titre), plutôt que de changer le texte des boutons (qui aurait juste déplacé le problème d'accessibilité — deux boutons "Activer" identiques sur une page n'est pas idéal, mais un `aria-label` distinct aurait aussi cassé les sélecteurs texte existants ; laissé en l'état, backlog si ça devient un vrai souci a11y).
- **Vérifié en direct, avec un vrai second compte, pas seulement en lisant le code** : compte jetable créé via l'API Admin (même technique que P5a), profil marqué `social_sharing_enabled = true` avec un jalon Deadlift inséré manuellement. Depuis une session **authentifiée réelle** du compte fixture (token REST, pas SQL superuser) : le jalon du compte jetable est visible tant qu'opted-in, disparaît dès que désactivé (`social_sharing_enabled = false`), `public_profiles` suit la même règle, la table `profiles` complète reste inaccessible en lecture directe pour un autre utilisateur, et un insert direct sur `milestones` depuis le client est bien rejeté (403, RLS). Compte jetable supprimé après coup.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 78 tests unitaires. Suite e2e complète (28 tests, incluant les 2 nouveaux tests `social.spec.ts` et les 2 corrigés dans `cycle.spec.ts`) rejouée à plusieurs reprises pendant le debug, verte au final. Fixture account nettoyé (jalons de test supprimés) après chaque run de vérification.

## P9b — Couche sociale : tonnage hebdo + streak de régularité (2026-07-23)

Deux jalons supplémentaires annoncés dans le scope initial de la couche sociale (P9a), livrés séparément comme prévu pour rester dans la taille de phase habituelle.

- **Bucketing de semaine identique au client** : `public.iso_week_start(timestamptz)` (SQL, `immutable`) reproduit exactement `getIsoWeekStart` (`src/lib/analytics.ts`) — `date_trunc('week', ...)` de Postgres suit déjà la définition ISO 8601 (lundi), il suffisait de normaliser la date en UTC avant troncature pour coïncider avec le calcul client (`.slice(0,10)` sur un timestamp déjà UTC). Un seul point de vérité pour "quelle semaine" plutôt que de dupliquer le calcul dans chaque trigger.
- **`week_start date`** ajouté à `milestones` (nullable — `one_rep_max` n'en a pas besoin), avec deux **index uniques partiels** (`where milestone_type = 'weekly_tonnage'` / `'regularity_streak'`) plutôt qu'une contrainte globale, pour ne jamais limiter `one_rep_max` à une ligne par semaine.
- **Tonnage hebdo — la valeur doit pouvoir grandir, pas se figer** : contrairement à `one_rep_max` (une ligne par record, jamais modifiée), le tonnage de la semaine en cours continue d'augmenter à mesure que d'autres séries sont loguées. Figer la valeur au moment où le seuil est franchi montrerait un total sous-évalué en fin de semaine. Le trigger utilise donc `insert ... on conflict (user_id, week_start) where milestone_type = 'weekly_tonnage' do update set value = excluded.value where milestones.value < excluded.value` — une seule ligne par semaine-record, toujours à jour avec le total le plus élevé réellement atteint. Chaque nouvelle semaine qui bat le record all-time précédent obtient sa **propre** ligne (l'historique s'accumule, comme pour `one_rep_max` — pas un seul "record actuel" qu'on écrase).
- **Streak de régularité — "gaps and islands" classique en SQL** : semaines actives (au moins une série loguée) triées par ordre décroissant, `semaine + (rang × 7 jours)` reste constant sur une série de semaines consécutives — grouper là-dessus isole chaque "île" d'activité sans boucle procédurale. Le streak courant est la longueur du groupe le plus récent ; comparé au meilleur des **autres** groupes (`filter (where recency_rank > 1)`) pour savoir si c'est un nouveau record. **Seuil à 2 semaines minimum** avant qu'un streak soit annoncé — sans ce garde-fou, la toute première semaine d'usage de n'importe quel utilisateur génère un "record : 1 semaine" immédiatement, du bruit pur.
- **Vérifié en direct avec des données antidatées, pas seulement en lisant le SQL** : programme/exercice/séries de test insérés directement via `supabase db query` avec des `started_at` répartis sur 4-6 semaines ISO réelles (2026-06-29 à 2026-08-03), scénario par scénario — semaines consécutives (streak progresse 1→2→3, sous le seuil de 2 non annoncé puis annoncé), tonnage qui grandit au sein d'une même semaine-record (vérifié que la ligne se **met à jour** plutôt que de dupliquer, testé explicitement avec 3 séries successives dans la même semaine), semaine plus légère qui ne bat pas le record (aucune ligne créée), **trou dans le streak** (saut d'une semaine → streak retombe à 1, non annoncé, l'ancien record de 3 reste intact), nouveau streak après le trou qui n'atteint pas l'ancien record (2 < 3, non annoncé). Les tests par simple usage réel de l'app (via le navigateur) ne pouvaient pas exercer ces scénarios multi-semaines sans attendre plusieurs semaines en temps réel — vérification directe en base la seule option praticable, avec le même niveau de rigueur que pour la formule 1RM en P9a.
- **Refactor de passage** : `MILESTONE_TYPE_LABELS`/`formatMilestoneValue` extraits de `social-api.ts` (qui importe `supabase`, donc pas testable unitairement sans mock) vers un nouveau `src/lib/social-display.ts` pur — même séparation déjà établie pour `cycle-phase.ts`/`cycle-api.ts` en P8. `formatMilestoneValue` gère l'affichage par type (kg avec décimales pour `one_rep_max`, kg arrondi pour `weekly_tonnage`, "X semaine(s) d'affilée" avec pluriel correct pour `regularity_streak`) — testé unitairement (`social-display.test.ts`, 4 tests).
- **Test e2e étendu, pas dupliqué** : le test P9a existant (999kg x 5) sert aussi de test tonnage — 4995kg de tonnage en une seule série dépasse largement tout ce que les autres specs de la suite peuvent produire cette semaine-là, donc fiable comme "nouveau record" sans configuration supplémentaire. Pas de test e2e pour le streak (nécessiterait des données antidatées, hors de portée d'un test piloté par navigateur qui logue toujours au temps réel) — couvert par la vérification directe en base ci-dessus.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 81 tests unitaires (3 nouveaux). Suite e2e complète (28 tests) rejouée après implémentation, verte. Fixture account nettoyé après chaque run.

## P9c — Couche sociale : photos de progression (2026-07-23)

Dernier morceau du scope initial de la couche sociale. Premier bucket Supabase Storage du projet.

- **Bucket privé, jamais d'URL publique** : `progress-photos` (`public = false`, 5 Mo max, JPEG/PNG/WebP). Convention de chemin `<user_id>/<uuid>.jpg` — le premier segment sert de propriétaire pour les policies RLS Storage (`storage.foldername(name)`). Même règle de visibilité que `milestones`/`public_profiles` (soi-même, ou un utilisateur avec `social_sharing_enabled`), appliquée à la fois sur `storage.objects` (accès au fichier) et sur `progress_photos` (la ligne en base) — les deux doivent être cohérentes puisque ce sont deux systèmes RLS indépendants.
- **Accès en lecture via URL signée**, pas `getPublicUrl()` (qui ne vérifie aucune permission — inutilisable sur un bucket privé) ni téléchargement de blob manuel. `createSignedUrl()` respecte RLS à la génération : demander une URL signée pour un chemin auquel on n'a pas droit échoue, avant même d'atteindre le fichier. Expiration à 1h, régénérée à chaque chargement du feed — largement suffisant pour une consultation, pas besoin de cache.
- **Compression côté client, aucune nouvelle dépendance** : `compressImage()` (`src/lib/image-compression.ts`) utilise `createImageBitmap` + `<canvas>` natifs du navigateur — redimensionne à 1600px de long côté max, réexporte en JPEG qualité 0.82. Une vraie photo de téléphone (3-8 Mo) devient une fraction de ça avant l'upload, pertinent pour un usage mobile-first sur un quota Storage limité (palier gratuit).
- **Bug trouvé en vérifiant avec un vrai compte jetable, pas en lisant le code — lacune réelle dans `account-delete` (P5a)** : `admin.auth.admin.deleteUser()` cascade bien la ligne `progress_photos` (vraie FK vers `auth.users`), mais **pas** le fichier Storage sous-jacent — confirmé par test direct (upload + suppression du compte + le fichier restait dans le bucket). Même famille de piège que `ai_provider_keys`/Vault trouvé en P5a (une ressource référencée depuis une table qui cascade, mais dont la cascade ne suit pas jusqu'à la ressource elle-même). Corrigé : `account-delete` liste et supprime maintenant les objets Storage du user avant `deleteUser()`. Redéployé et re-vérifié avec un nouveau compte jetable : `auth.users`, `progress_photos` et l'objet Storage sont tous à zéro après suppression.
- **Modèle de feed unifié** : `FeedEntry = MilestoneFeedEntry | PhotoFeedEntry` (`src/lib/social-display.ts`), fusionnées et triées par `occurredAt` via `mergeFeedEntries()` (pure, testée). `fetchFeed()` (`social-api.ts`) récupère milestones + `progress_photos` en parallèle, mutualise un seul lookup `public_profiles` pour les deux, génère les URLs signées en parallèle pour les photos.
- **Suppression d'une photo** : supprime d'abord la ligne `progress_photos` (soumis à RLS, peut échouer proprement), puis le fichier Storage en best-effort — dans cet ordre plutôt que l'inverse, pour ne jamais laisser une ligne pointant vers un fichier qui n'existe plus (ce qui casserait l'affichage), au pire un fichier orphelin nettoyable plus tard.
- **Vérifié en direct avec un vrai second compte** (même méthode qu'en P9a) : photo uploadée par un compte jetable non opted-in → invisible pour le compte fixture à la fois sur `progress_photos` (lecture table) et sur l'objet Storage lui-même (404, pas juste refusé côté table) ; devient visible dès `social_sharing_enabled = true`, sur les deux fronts ; insert direct d'une ligne `progress_photos` pour le user_id de quelqu'un d'autre → rejeté (RLS) ; delete direct de la ligne de quelqu'un d'autre → 0 ligne affectée.
- Test e2e (`e2e/social.spec.ts`) : upload d'une vraie image de test (`e2e/fixtures/test-photo.png`, PNG 4×4 généré une fois, committé) via `setInputFiles` sur l'input caché (même pattern que l'import JSON), légende, aperçu, publication, apparition dans le feed avec l'`<img>` rendu, suppression. `import.meta.dirname` pour le chemin du fixture (pas `__dirname`, ESM).
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 83 tests unitaires (2 nouveaux sur `mergeFeedEntries`). Suite e2e complète (29 tests) verte. Vérification visuelle à 375px (mobile-first) de la page `/feed` vide et du formulaire d'upload avec aperçu — layout propre, pas de débordement.
- **Non fait, backlog explicite** : pas de limite de nombre de photos par utilisateur (quota Storage à surveiller si l'usage grandit au-delà de quelques amis/collègues), pas d'édition de légende après publication (suppression + réupload seulement, cohérent avec le reste du feed).

## P4b — Couche IA : analyse de tendance (2026-07-23)

Première vraie feature de la couche IA différée depuis P4a — jusqu'ici, les clés BYOK n'avaient servi qu'à une validation (`GET /v1/models`), jamais un appel de complétion réel. Décisions confirmées avant implémentation : analyse de tendance globale (pas de recommandation de deload par exercice — ça reste pour une phase ultérieure), déclenchement à la demande uniquement (pas de job planifié qui dépenserait le quota API de l'utilisateur sans action explicite), carte sur le Dashboard.

- **Le résumé est calculé côté client, pas dans l'Edge Function** : `buildTrendSummary()` (`src/lib/analytics.ts`) réutilise telles quelles les fonctions pures déjà utilisées par la page Analytics (`computeWeeklyTonnage`, `countTrainingDaysThisWeek`, `computeOneRepMaxProgression`, `getLoggedExercises`) — l'IA raisonne sur exactement ce que l'utilisateur peut déjà voir à l'écran, pas une requête serveur parallèle qui risquerait de diverger. La fonction Edge Function (`ai-analyze-trends`) ne fait que relayer ce résumé déjà condensé au provider — cohérent avec le reste du projet (logique métier en TypeScript, Postgres/Edge Functions pour l'état et la sécurité uniquement).
- **Bug trouvé par le test unitaire, pas en relisant le code** : la première version de `buildTrendSummary` calculait la fenêtre "8 dernières semaines" en tranchant le tableau (sparse) retourné par `computeWeeklyTonnage` plutôt que par une vraie date de coupure calendaire — comme ce tableau ne contient que les semaines *avec* des séries loguées, un trou d'entraînement de plusieurs mois pouvait faire remonter la fenêtre bien plus loin que 8 semaines réelles (une semaine vide ne "compte" pas dans un slice(-8) sur un tableau sparse). Un test dédié à ce scénario (`excludes sets older than the 8-week tonnage window`) l'a immédiatement fait échouer. Corrigé en calculant une vraie date de coupure (`getIsoWeekStart(now) - 7×7 jours`) et en filtrant les séries par date réelle avant tout calcul, plutôt que de faire confiance à la forme du tableau retourné.
- **Deuxième bug, trouvé de la même façon** : la sélection des exercices les plus fréquents utilisait `frequencyByExercise` pour choisir *lesquels* garder, mais reconstruisait ensuite la liste finale via `getLoggedExercises()` (tri alphabétique) filtrée sur cette sélection — le tri alphabétique écrasait silencieusement l'ordre par fréquence annoncé. Le test `picks the most frequently logged exercises, most-frequent first` l'a révélé (attendu `['Squat', 'Bench Press']`, reçu l'ordre alphabétique). Corrigé en construisant la liste finale directement depuis `topExerciseIds` (déjà trié par fréquence), avec juste un lookup de nom depuis `getLoggedExercises`.
- **Modèles et provider — pas de skill dédié à OpenAI dans ce projet**, contrairement à Anthropic (`claude-api` skill déjà chargé cette session) : `gpt-5.6` confirmé comme modèle flagship actuel via une recherche web + une lecture directe de la doc OpenAI (`developers.openai.com/api/docs/guides/completions`) plutôt que deviné depuis les connaissances d'entraînement, qui peuvent être dépassées sur un catalogue de modèles qui change vite. Côté Anthropic, `claude-opus-4-8` (le défaut non-négociable du skill — "never downgrade for cost, that's the user's decision") avec `thinking: { type: 'adaptive' }`, cohérent avec la consigne du skill de l'activer "pour tout ce qui est un peu complexe" — une synthèse multi-signaux (tonnage, régularité, 1RM par exercice) qualifie.
- **Impossible de tester avec une vraie clé** (BYOK — seul l'utilisateur en a une), donc vérifié ce qui est vérifiable sans : auth manquante (401), provider invalide (400), résumé manquant (400), aucune clé enregistrée pour le provider demandé (404, chemin déjà éprouvé en P4a). Puis, pour aller plus loin qu'en P4a : une **fausse clé** insérée directement dans Vault (contournant volontairement `ai-key-save`, qui l'aurait rejetée) pour chacun des deux providers, afin de vérifier que l'appel HTTP réel atteint bien Anthropic/OpenAI avec une requête bien formée — les deux ont répondu avec de vraies erreurs d'authentification (401, message du provider), pas une erreur de requête malformée (400), ce qui confirme que la forme de la requête (modèle, en-têtes, corps) est correcte jusqu'au point d'authentification. Le chemin "clé valide → analyse réussie" reste à vérifier par le user avec sa propre clé, même limitation déjà documentée et acceptée pour P4a.
- **UI** : nouvelle carte "Analyse IA" sur le Dashboard (`HomePage.tsx`), après la carte Poids. Trois états : aucune clé configurée (invite vers le Profil), historique vide (bouton désactivé, message), prêt (bouton "Analyser mes progrès", sélecteur de provider affiché seulement si les deux sont configurés et valides). Résultat affiché tel quel sous forme de texte (`whitespace-pre-line`), pas de cache ni de requête auto-relancée — chaque clic est un nouvel appel payant pour l'utilisateur, donc jamais déclenché sans action explicite (cohérent avec la décision "à la demande" du cadrage).
- Test e2e (`e2e/dashboard.spec.ts`) : état "aucune clé configurée" (le seul rejouable sans clé réelle), lien vers le Profil vérifié. Pas de test pour le chemin "analyse réussie" — même raisonnement que le chemin "clé valide" jamais testé en e2e depuis P4a.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 87 tests unitaires (4 nouveaux sur `buildTrendSummary`, 2 bugs réels trouvés et corrigés par ces tests). Suite e2e complète (30 tests) verte. Vérification visuelle à 375px de la nouvelle carte sur le Dashboard.
- **Non fait, backlog explicite** : recommandation de deload par exercice et suggestions d'ajustement du planning bien-être (les deux autres volets du brief pour la couche IA, différés à une phase ultérieure), streaming de la réponse (skill par défaut, sciemment omis — `max_tokens` borné à 1536/1024 rend le risque de timeout faible, complexité non justifiée pour ce premier passage), assistant conversationnel (déjà noté en backlog comme la suite naturelle une fois cette fondation validée à l'usage).
