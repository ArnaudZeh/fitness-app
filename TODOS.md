# TODOs

Tracker unique des TODO en attente, conformément à la règle "aucun TODO non tracké en fin de phase".

## P0 — Infrastructure & squelette

- [ ] Remplacer les icônes/favicon placeholder (`public/pwa-192x192.png`, `public/pwa-512x512.png` — carrés unis `#0b0f14` ; `public/favicon.svg` — cercle teal minimal, remplacé début P1 car c'était encore le logo par défaut du template Vite) par de vraies icônes de marque lors de la phase design UI (`ui-ux-pro-max` + `frontend-design`). Repéré par `graphify` P1 (communauté isolée "Leftover Vite Favicon Branding").

## P1 — Auth & multi-tenant

- [ ] Avant le go-live (P13) : configurer un provider SMTP dédié (Resend/Postmark/SendGrid) pour les emails d'auth. Le service email intégré de Supabase est limité à quelques emails/heure sur ce projet (confirmé pendant les tests E2E — `over_email_send_rate_limit`), pas prévu pour de l'usage réel au-delà de tests ponctuels.
- [ ] Avant le go-live (P13) : revoir la config auth hébergée (`site_url`, `additional_redirect_urls`, `minimum_password_length`) — actuellement les valeurs par défaut Supabase, le `supabase/config.toml` local pointe encore vers `127.0.0.1` et ne doit pas être poussé tel quel (`supabase config push`) sans revue complète.
- [ ] Playwright : n'a que Chromium pour l'instant. Ajouter WebKit (proxy iOS Safari) en phase Polish (P11) quand le mobile-first sera vérifié visuellement.

## P2a — Programmes & blocs

- [ ] Le projet Supabase gratuit a un cold-start perceptible (une requête peut prendre plusieurs secondes après une période d'inactivité) — les tests E2E tournent en 1 seul worker (`playwright.config.ts`) pour éviter la contention, et l'assertion de suppression a un timeout étendu à 20s. À revisiter si on passe sur un tier payant ou une DB locale (Docker).
- [ ] Les types Supabase (`src/lib/database.types.ts`) sont générés une fois via `supabase gen types typescript` — à régénérer après chaque nouvelle migration (P2b et suivantes), sinon les nouvelles tables ne seront pas typées.

## P2b — Catalogue d'exercices & génération de séances

- [ ] `session_template_exercises.exercise_id` référence `exercises` par FK sans vérifier que l'exercice est accessible (catalogue système ou exercice perso de l'utilisateur) au moment de l'insert — un utilisateur pourrait créer une référence "orpheline" vers l'exercice privé d'un autre utilisateur (deviné par UUID). Aucune fuite de données possible (RLS bloque toujours la lecture), juste une référence morte théorique. Pas prioritaire, mais à durcir avec une contrainte/trigger si on ouvre l'app à plusieurs utilisateurs réels.

## Architecture pivot (2026-07-21, post P2a/P2b) — voir mémoire "P2 architecture pivot"

- Cette phase a supprimé `blocks` (périodisation), `sessions` + `generate_block_sessions()` (pré-génération de séances). `focus` est passé du Bloc au Programme. Le vrai logging de séance (remplaçant `sessions`) est repoussé à P3 — c'est sa place naturelle, pas la peine de le tracker séparément ici.
- [ ] `pnpm audit` : 1 vulnérabilité modérée acceptée en connaissance de cause — `shadcn` (devDependency, requis pour `src/index.css` → `@import 'shadcn/tailwind.css'`) dépend transitivement de `@modelcontextprotocol/sdk` → `@hono/node-server` (CVE path traversal Windows-only dans `serve-static`, sert uniquement le registry MCP local de la CLI shadcn, jamais invoqué par cette app, jamais bundlé côté client). Pas de version corrigée disponible en amont (4.13.1 = dernière version au 2026-07-21). À revérifier périodiquement (`pnpm audit` + `npm view shadcn versions`).

## Jours fixes Lundi-Dimanche (2026-07-21)

- Remplacé la création manuelle de "jours" par une structure fixe : chaque programme a toujours exactement 7 `session_templates` (1=Lundi...7=Dimanche), auto-créées par le trigger `on_program_created` (même pattern que `handle_new_user` en P0). L'utilisateur bascule juste chaque jour entre "Repos" et "Entraînement" au lieu de nommer/ordonner des jours.
- RLS durcie : policies INSERT/DELETE retirées sur `session_templates` (seuls SELECT/UPDATE restent) — un utilisateur ne peut plus casser l'invariant "toujours 7 jours" en ajoutant/supprimant une ligne. Vérifié en live.
- `duplicateProgram()` adapté en conséquence : les 7 jours de la copie existent déjà (trigger), donc on les met à jour (`day_type` + exercices copiés) au lieu de les insérer.
- RPE : définition ajoutée en texte d'aide sous le champ (pas de tooltip — mobile-first, le hover ne fonctionne pas au toucher).

## Phase Profil (2026-07-21/22)

- Le Profil (mensurations, objectifs) est conçu pour permettre, plus tard, des recommandations d'entraînement pilotées par IA en fonction des objectifs déclarés (le user veut que "tout soit lié" — profil → programmes → IA). L'IA elle-même reste backlog (P9), ce qui a été fait ici : étendre `profiles` (P0) plutôt que créer une nouvelle table, avec des colonnes explicitement typées (`goal`, `sex` en check constraints) faciles à référencer depuis un futur prompt IA.
- Table `weight_entries` créée (time-series, une entrée par jour via `unique(user_id, recorded_at)` + upsert) — pas de graphique pour l'instant, c'est le scope de P5 (analytics). RLS complète (select/insert/update/delete, `auth.uid() = user_id`) vérifiée en live avec deux comptes jetables (isolation croisée testée : lecture, update, insert tous bloqués sur le profil/les pesées d'autrui).
- `src/pages/ProfilePage.tsx` : formulaire profil (`ProfileForm`, state initialisé directement depuis les props — pas de `useEffect` de synchro, pour éviter le anti-pattern "setState dans un effect" détecté par `eslint-plugin-react-hooks`) + section pesée (log du jour + liste + suppression via `ConfirmDialog`). Testé en live à 390px (mobile) : sauvegarde profil persistée après reload, log de pesée, suppression de pesée avec confirmation — tout fonctionne.
- Aucune nouvelle dépendance ajoutée ; `pnpm audit` toujours à 1 vulnérabilité modérée acceptée (voir note P2a/P2b ci-dessus, inchangée).

## Sélecteur d'exercice — search bar + visuels (2026-07-22)

- Remplacé le `<Select>` groupé par muscle group dans `ExerciseSlotFormDialog` par `src/components/ExercisePicker.tsx` : recherche live (filtrage local, pas le filtre interne de cmdk — `shouldFilter={false}`) + groupes par muscle avec une icône `lucide-react` par groupe (Dumbbell/Shirt/PersonStanding/BicepsFlexed/Footprints/Target/Activity pour pectoraux/dos/épaules/bras/jambes/core/full_body). "Visuels" = ces icônes de groupe, pas de vraies photos d'exercices — ça reste backlog tant que le user n'a pas envoyé ses exemples 21st.dev.
- Nouvelle dépendance : composant shadcn `command` (ajoute `cmdk` — maintenu, ~40k dl/semaine) + `input-group` (dépendance du composant `command`). `shadcn add` a de nouveau créé le dossier `@/` parasite à la racine (bug CLI connu, cf. notes P0/P1) — seuls `command.tsx`/`input-group.tsx` étaient réellement nouveaux, déplacés manuellement ; `button.tsx`/`input.tsx`/`textarea.tsx`/`dialog.tsx` régénérés étaient identiques (diff = guillemets simples vs doubles uniquement), donc ignorés.
- Bug mobile réel découvert en testant ce picker à 390px : `DialogContent` (`src/components/ui/dialog.tsx`) n'avait pas de `max-height`/`overflow-y-auto` — un formulaire un peu long (comme celui-ci une fois la liste d'exercices ajoutée) poussait son propre titre et son bouton de soumission hors viewport, sans aucun moyen de scroller pour les atteindre. Corrigé au niveau du composant de base (`max-h-[calc(100vh-2rem)] overflow-y-auto`) donc ça bénéficie à toutes les dialogs de l'app, pas seulement celle-ci.
- `e2e/session-structure.spec.ts` mis à jour : `#exercise-select` (Radix Select, supprimé) → `getByPlaceholder('Rechercher un exercice…')` + `getByRole('option', ...)` (cmdk rend ses items avec `role="option"`, confirmé dans le bundle `cmdk`).
- Testé en live à 390px : recherche/filtrage, sélection (coche + résumé "Sélection : X"), pré-remplissage en mode édition, scroll interne de la liste + scroll du dialog, soumission — tout fonctionne. Suite Playwright complète (7 tests) + Vitest (13 tests) verts après la modification.

## P3a — Log de séance, boucle de base (2026-07-22)

- Le "Log de séance" complet du brief (timer de repos, mode silencieux gym, saisie vocale, calculateur de plaques, offline-first) est découpé en sous-phases (voir échange avec le user) : **P3a** = boucle de base en ligne (ce qui est fait ici) ; backlog : P3b (timer + mode gym), P3c (offline-first IndexedDB/Dexie + sync — le brief le dit "non négociable", volontairement fait après pour ne pas construire la sync sur un modèle de données pas encore prouvé), P3d (calculateur de plaques + saisie vocale).
- Nouvelles tables `session_logs` (une instance réelle d'un jour de `session_templates`, statut in_progress/completed) et `session_log_sets` (séries réellement faites, rattachées au `session_template_exercise_id` planifié pour comparer prévu/réalisé). RLS identique au pattern déjà utilisé (`auth.uid() = user_id`, CRUD complet — pas de restriction comme sur `session_templates`, rien à protéger structurellement ici).
- ⚠️ **RLS non re-vérifiée en live pour cette phase** — le rate limit d'emails Supabase (`over_email_send_rate_limit`, déjà documenté en P1) a bloqué la création de comptes jetables au moment de tester. Vérification faite par relecture de code à la place (policies strictement identiques à celles de `weight_entries`/`profiles`, déjà vérifiées en live). À refaire en live dès que le SMTP dédié (tracké en P1) est en place, ou plus tôt si un service_role key est fourni.
- UI : bouton "Démarrer la séance" sur `SessionTemplateCard` (visible seulement si le jour a au moins un exercice planifié) → crée le log et navigue vers `/sessions/:id` (`SessionLogPage.tsx`). Par exercice : séries déjà loggées (avec suppression) + formulaire d'ajout rapide (charge/reps/RPE, reps pré-rempli avec la cible). "Terminer la séance" fige la vue en lecture seule (plus de formulaire ni de suppression de série). `ProgramDetailPage` affiche un historique des séances passées (jour + date + statut + suppression).
- Testé en live (flux complet) : démarrage, 2 séries loggées avec numérotation automatique, fin de séance, historique affiché correctement, suppression cascade propre. Nouveau test Playwright `e2e/session-log.spec.ts` couvre ce parcours de bout en bout.
- Bug rencontré pendant la génération des types : `supabase gen types typescript --linked > ...` a capturé des lignes de statut de la CLI ("Initialising login role...", un tag `<claude-code-hint>`, une notice de mise à jour) au début/fin du fichier généré, cassant le parsing TS. Nettoyé manuellement ; à surveiller si ça se reproduit (rediriger uniquement stdout ne suffit pas ici, la CLI semble mélanger les flux).

## Retours post-gate P3a (2026-07-22)

- Catalogue : ajout de "Romanian Deadlift (RDL)" et "Single Leg Press" (groupe `jambes`, cohérent avec "Stiff-Leg Deadlift"/"Single-Leg Romanian Deadlift" déjà présents dans ce groupe). Le sigle "(RDL)" est resté dans le nom à la demande explicite du user (migration de rename séparée après un premier essai sans sigle).
- Superset : nouvelle colonne `session_template_exercises.superset_group` (texte libre, nullable — ex "A"). Champ optionnel dans `ExerciseSlotFormDialog`, affiché en badge "Superset A" sur `SessionTemplateCard` (vue planification) et `SessionLogPage` (vue log). Portée volontairement limitée à un **tag visuel** pour cette passe — ça ne change pas le flux de saisie (toujours une carte par exercice, une série à la fois). Un flux de saisie alterné (A1 puis A2 puis A1...) resterait à faire si le user le demande, plus naturel à construire avec le mode gym de P3b.
- Dupliquer une série : bouton "Copy" à côté de chaque série déjà loggée dans `SessionLogPage`, ré-utilise `createSessionLogSet` avec les mêmes `actual_reps`/`actual_weight_kg`/`actual_rpe` que la série copiée, au prochain `set_number`.
- Vérifié en live (390px et desktop) : superset badge affiché correctement, duplication de série fonctionnelle. Confirmé par le user (gate 1✅ 2✅).

## P3b — Timer de repos & mode gym (2026-07-22)

- Nouvelle colonne `session_template_exercises.target_rest_seconds` (smallint, nullable, > 0) — même pattern optionnel que `target_rpe`. Champ "Repos (secondes)" dans `ExerciseSlotFormDialog`.
- `src/components/RestTimer.tsx` : décompte visuel (`font-mono text-5xl`), vibration (`navigator.vibrate`, no-op silencieux si absent — pas de support iOS Safari, limitation de plateforme documentée dans le composant, pas un bug) + bip sonore (Web Audio API, `try/catch` défensif pour ne jamais casser le flux de log si l'audio échoue). Boutons "+15s"/"-15s"/"Passer", tous en `h-12` (48px) — les tailles `sm`/`lg` du design system ne montent qu'à 28-36px, en dessous du minimum 44px que `design-system/MASTER.md` impose explicitement "critique en mode gym" ; overrides via `h-12!` plutôt que d'élargir tous les boutons de l'app.
- Le timer démarre automatiquement après chaque série loggée (ajout normal ou duplication), avec une `key` React sur l'id de la série créée pour forcer un vrai reset à chaque nouvelle série plutôt que de réutiliser l'état du timer précédent.
- Mode gym : Charge/Reps/RPE (`SessionLogPage`) et le bouton "Série N" passés à une taille plus grande (`h-12`, `text-lg`) spécifiquement sur cette page — pas un changement de thème global, l'app est déjà sombre par défaut partout.
- Bug de lint évité : `hasRung` gardé en `useRef` plutôt que `useState` dans `RestTimer` — c'est un simple garde anti-double-son, pas un état qui doit re-render, donc `setState` dans l'effect (anti-pattern déjà rencontré en Phase Profil) ne s'applique pas ici avec un ref.
- Testé en live : décompte jusqu'à 0, pas d'erreur console (vibration/audio), ajustement +15/-15, duplication qui relance le timer proprement. Nouvelle assertion Playwright dans `e2e/session-log.spec.ts` (apparition du timer + bouton "Passer" après une série).
- Backlog restant du "Log de séance" complet du brief : **P3c** (offline-first IndexedDB/Dexie + sync), **P3d** (calculateur de plaques + saisie vocale).

## Repos par défaut selon le focus du programme (2026-07-22)

- Retour du user : le repos de base ne devrait pas être une valeur unique (90s) mais dépendre du focus du programme (force / hypertrophie / endurance). Remplacé la constante `DEFAULT_REST_SECONDS = 90` par `DEFAULT_REST_SECONDS_BY_FOCUS: Record<ProgramFocus, number>` dans `sessions-api.ts` :
  - **Force (180s / 3min)** : charges quasi-maximales, récupération ATP-PCr plus complète nécessaire. Repères NSCA (Essentials of Strength Training and Conditioning) et de Salles et al. (2009, Sports Medicine, "Rest Interval between Sets in Strength Training") : ~3-5 min pour le travail force/puissance.
  - **Hypertrophie (90s)** : Schoenfeld, Pope et al. (2016, J Strength Cond Res, "Longer Interset Rest Periods Enhance Muscle Strength and Hypertrophy in Resistance-Trained Men") montrent qu'un repos de 3 min donne des gains de force ET d'hypertrophie égaux ou supérieurs à 1 min, probablement via un meilleur maintien de la charge/du volume sur les séries suivantes — l'ancien réflexe "60s pour la congestion" n'est plus bien soutenu par la littérature récente. 90s reste un compromis raisonnable pour des séries de travail courantes (un repos plus long serait défendable mais moins nécessaire qu'en force pure).
  - **Endurance (45s)** : repères classiques NSCA pour l'endurance musculaire (repos courts, largement sous la minute) pour maintenir le stress métabolique.
  - Ces valeurs restent des **points de départ éditables** — `target_rest_seconds` sur chaque exercice prime toujours sur le défaut du focus.
- Le focus doit maintenant descendre jusqu'à trois endroits : `ProgramDetailPage` (a déjà `program.focus`) → `SessionTemplateCard` (nouvelle prop `focus`) → `ExerciseSlotFormDialog` (nouvelle prop `focus`, utilisée pour le placeholder et le texte d'aide) ; côté log, `SessionLogPage` le lisait via `useProgram(log.program_id)` — depuis le passage offline-first (P3c), il vient du plan mis en cache (`plan.focus`), avec le même repli sur `'hypertrophie'` si le plan n'est pas encore disponible.
- Bug corrigé au passage : `duplicateProgram()` (`programs-api.ts`) copiait les slots d'exercice sans `target_rest_seconds` ni `superset_group` — la liste de champs de l'insert n'avait pas été mise à jour lors de l'ajout de ces deux colonnes dans les phases précédentes. Un programme dupliqué perdait silencieusement ces deux valeurs. Corrigé ; pas de test de duplication existant pour attraper ce genre de régression (`e2e/programs.spec.ts` ne teste que la création) — à surveiller si on ajoute d'autres champs aux exercices planifiés.
- Vérifié en live sur le programme réel "FORCE 2026" (focus Force) : tous les exercices sans repos explicite affichent maintenant "repos 180s" au lieu de l'ancien "repos 90s" fixe.

## P3c — Offline-first avec Dexie (2026-07-22)

- Portée : le domaine "log de séance" (`session_logs`, `session_log_sets`, plus le plan du jour nécessaire pour l'afficher) devient **local-first** — Dexie (IndexedDB) est maintenant la source de vérité pour la lecture ET l'écriture de cet écran, Supabase n'étant plus qu'une cible de synchronisation en arrière-plan. C'est le seul domaine de l'app concerné : la planification (programmes/exercices/profil) reste 100% en ligne, le brief n'exige l'offline que pour "la séance" elle-même.
- Nouveaux fichiers :
  - `src/lib/offline-db.ts` — schéma Dexie : `sessionPlanCache` (plan du jour mis en cache : nom du programme, focus, jour, exercices avec toutes leurs cibles), `sessionLogs`/`sessionLogSets` (miroir local, avec un champ `dirty: 'create'|'update'|'delete'|null`).
  - `src/lib/offline-sync.ts` — moteur de sync : `syncPendingChanges()` pousse chaque ligne `dirty` vers Supabase (logs avant sets, car un set ne peut pas être inséré avant que son log parent existe côté serveur), déclenché après chaque écriture locale + sur l'évènement `window online`. `refreshSessionLog(s)Cache`/`refreshSessionLogSetsCache` font l'inverse (lecture à travers le cache) sans jamais écraser une ligne qui a des changements locaux en attente.
  - Résilience ajoutée après coup : si le push échoue avec une violation de clé étrangère Postgres (code `23503` — le programme/log parent a été supprimé avant d'avoir synchronisé), la ligne est abandonnée plutôt que retentée indéfiniment (sinon elle boucle pour toujours sans jamais pouvoir réussir).
- Identifiants générés côté client (`crypto.randomUUID()`) dès la création, pas `gen_random_uuid()` côté Postgres — indispensable pour écrire hors-ligne, et rend la sync idempotente via `upsert` (un retry après succès non confirmé ne crée pas de doublon).
- `requireUserIdOffline()` lit `useAuthStore.getState().session?.user.id` (store Zustand en mémoire, peuplé une fois au démarrage) plutôt que `supabase.auth.getUser()`, qui fait un aller-retour réseau et échouerait hors-ligne. Limite connue et acceptée : si l'app reste hors-ligne assez longtemps pour que le JWT expire complètement (access + refresh), une réauthentification pourrait être nécessaire au retour — cas extrême pour une séance de sport, pas traité dans cette passe.
- `pnpm add dexie dexie-react-hooks` (déjà prévus dans le brief initial section 3) + `pnpm add -D fake-indexeddb` (shim IndexedDB pour les tests Vitest/jsdom, câblé dans `src/test/setup.ts`).
- Bug TanStack Query découvert et corrigé : `networkMode` par défaut (`'online'`) met les mutations en pause tant que `navigator.onLine` est faux — ça interceptait mes appels AVANT qu'ils atteignent mon propre code offline-safe (le bouton "Série N" restait bloqué en "pending" indéfiniment hors-ligne). Corrigé en ajoutant `networkMode: 'always'` à toutes les mutations de `useSessionLogs.ts` (elles n'ont plus besoin du réseau, seulement de Dexie).
- Deux corrections de lint dans le même esprit que les précédentes (Phase Profil, RestTimer) : `setState` dans un effect évité dans `useSessionLog` en trackant les ids déjà rafraîchis (`Set<string>`) plutôt qu'un booléen réinitialisé ; `varsIgnorePattern: '^_'` ajouté à `eslint.config.js` pour permettre de "jeter" un champ via destructuring (`const {dirty: _dirty, ...rest} = row`) sans déclencher `no-unused-vars`.
- Tests : `src/lib/offline-sync.test.ts` (9 cas — push réussi, échec laissé dirty, hors-ligne = no-op, ordre logs-avant-sets, sync groupée, suppression après delete réussi, delete gardé dirty si échec, abandon sur violation FK pour logs et sets) avec `@/lib/supabase` mocké. `e2e/offline-session-log.spec.ts` : coupe le réseau (`page.context().setOffline(true)`), logge une série, vérifie qu'elle s'affiche sans erreur ; remet le réseau, attend la sync, **vide IndexedDB puis recharge la page** — la série doit réapparaître uniquement si elle a vraiment atteint Supabase (preuve qu'un cache local périmé ne peut pas donner un faux positif).
- Ajout de `"DOM"` à `lib` dans `tsconfig.e2e.json` — nécessaire pour typer `indexedDB` dans le callback `page.evaluate()` du nouveau test.
- Backlog restant du "Log de séance" complet du brief : **P3d** (calculateur de plaques + saisie vocale). Offline-first et timer/mode gym (P3b) sont maintenant faits.

## Retour post-gate P3c : retry périodique (2026-07-22)

- Retour du user : pas pu tester sur téléphone (app en localhost, pas accessible depuis le mobile pour l'instant) — test physique reporté à une passe de fin. Point produit important : l'app sera utilisée à ~99% du temps avec la 4G active, pas en vrai mode avion — le cas réaliste du quotidien est plutôt "signal faible/qui coupe par intermittence" que "zéro réseau prolongé".
- Ça a révélé un vrai trou dans la stratégie de retry : `navigator.onLine` reflète seulement si la radio est allumée, pas si les requêtes aboutissent réellement — en 4G faible il peut rester `true` alors que tout timeout. Mon code gérait déjà correctement l'échec ponctuel (toute erreur laisse la ligne `dirty` pour un retry ultérieur), mais les seuls déclencheurs de retry étaient "juste après une écriture" et l'évènement `online` — une coupure de quelques secondes sans nouvelle saisie après pouvait laisser une ligne non synchronisée en attente indéfiniment.
- Ajouté un retry périodique (`setInterval`, 20s) dans `offline-sync.ts`, au niveau du module (tourne tant que l'onglet est chargé, peu importe la page affichée) plutôt que scopé à `SessionLogPage` — plus robuste si l'utilisateur navigue ailleurs pendant qu'une sync est en attente. `syncPendingChanges()` est déjà un no-op bon marché s'il n'y a rien à synchroniser, donc pas de coût réel à cette fréquence.
- Pas de test automatisé dédié à ce timer (mécanique triviale, la logique de sync elle-même est déjà couverte par les 9 tests unitaires existants) — mais à garder à l'esprit si le comportement de retry doit un jour être ajusté (fréquence, backoff, etc.) une fois testé en conditions réelles de 4G.

## P3d — Calculateur de plaques & saisie vocale (2026-07-22)

Ferme le "Log de séance" complet du brief (P3a boucle de base, P3b timer/mode gym, P3c offline-first, P3d ici) — tout dans `SessionLogPage`, là où le brief place ces deux features.

- **Calculateur de plaques** : `src/lib/plate-calculator.ts` (`calculatePlateBreakdown`, pur, testé — 7 cas) — algorithme glouton (plus grosse plaque en premier, par côté) sur un jeu de plaques standard `[25, 20, 15, 10, 5, 2.5, 1.25]` kg, barre par défaut 20kg. Le glouton est optimal ici (pas juste une heuristique) parce qu'un jeu de plaques de salle standard a la même propriété qu'un système de pièces "canonique" (chaque dénomination divise proprement dans les suivantes) — pas besoin d'une résolution par programmation dynamique. Si la charge cible n'est pas atteignable exactement (granularité), retourne le plus proche possible en dessous plutôt que d'échouer, et `PlateCalculatorDialog` l'affiche clairement ("le plus proche : Xkg").
  - Jeu de plaques et poids de barre pas encore configurables par l'utilisateur (barre éditable dans le dialogue à chaque usage, mais pas persistée comme préférence, et le jeu de plaques est fixe) — backlog si le user veut une salle avec un inventaire différent.
  - `PlateCalculatorDialog.tsx` : bouton "Calculateur de plaques" dans le formulaire de saisie de série, pré-rempli avec la charge déjà tapée dans "Charge (kg)" à l'ouverture (pas de sync automatique dans l'autre sens — modifier la charge cible dans le dialogue n'affecte pas le champ du formulaire, ce sont deux valeurs indépendantes une fois le dialogue ouvert).
- **Saisie vocale** : Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`), pas dans les types DOM standards (spec pas finalisée) — interfaces minimales déclarées dans `useVoiceSetInput.ts` plutôt que `any`. Pas de support iOS Safari connu à ce jour (support Web Speech absent/inégal) — détection de fonctionnalité (`isSupported`) fait disparaître le bouton "Dicter la série" plutôt que d'afficher un contrôle qui ne marcherait jamais, même logique que pour l'API Vibration en P3b.
  - `src/lib/voice-set-parser.ts` (`parseVoiceSetInput`, pur, testé — 8 cas) : extrait charge/reps/RPE d'une transcription via mots-clés ("kilos", "reps"/"répétitions", "rpe") ; repli sur "premier nombre = charge, deuxième = reps" si aucun mot-clé n'est dit (ordre naturel à l'oral : "quatre-vingts, huit"). Parseur "best effort", pas du NLP complet — suppose que le moteur de reconnaissance transcrit déjà les nombres dits en chiffres (comportement réel de Chrome/Android), gère la virgule décimale française.
  - Le résultat vocal **remplit les champs sans soumettre automatiquement** — l'utilisateur voit et confirme avant de valider "Série N", pour éviter qu'une mauvaise reconnaissance loggue une série silencieusement fausse.
- Tests : 15 tests unitaires purs (7 calculateur + 8 parseur vocal) ; `e2e/plate-calculator.spec.ts` (calcul + changement du poids de barre recalculant en direct). Pas de test e2e pour la saisie vocale — Playwright n'a pas de moyen pratique de simuler l'API Web Speech, et le microphone déclencherait une demande de permission navigateur bloquante en test automatisé ; couverture via les tests unitaires du parseur + vérification manuelle en direct (boutons affichés, dialogue calculateur fonctionnel, valeurs correctes).

## Analytics — 1RM, progression, régularité (2026-07-22)

- Ferme la section "Analytics et progression" du brief. Nouvelle page `/analytics` (lien dans `AppLayout`, icône `ChartLine`), **en ligne uniquement** — comme Programmes/Profil, pas dans le périmètre offline-first de P3c (c'est une feature de reporting/réflexion, pas quelque chose utilisé en pleine série).
- `src/lib/one-rep-max.ts` : formules Epley et Brzycki, moyennées pour le nombre affiché. Fiables seulement jusqu'à ~12 répétitions (au-delà les deux formules divergent significativement) — `estimateOneRepMax` retourne `null` hors de cette plage plutôt qu'un nombre trompeur ; la charge max réelle reste affichée même quand le 1RM ne l'est pas.
- `src/lib/analytics-api.ts` (`fetchSetHistory`) : une seule requête Supabase avec double embed (`session_log_sets` → `session_template_exercises` → `exercises`, et → `session_logs` pour la date) ramène tout l'historique de séries de l'utilisateur, toutes séances/programmes confondus — nécessaire car la progression "par exercice" doit grouper par `exercise_id` (le catalogue partagé), pas par `session_template_exercise_id` (qui est spécifique à un jour d'un programme donné).
- `src/lib/analytics.ts` (pur, testé — 15 cas) : `getLoggedExercises` (liste pour le sélecteur, seulement les exercices avec de l'historique réel), `computeOneRepMaxProgression` (meilleur 1RM estimé par jour, par exercice), `computeWeeklyTonnage` (tonnage par semaine ISO, lundi comme premier jour — cohérent avec `WEEKDAY_LABELS`), `computeDailyVolume` (alimente la heatmap).
- `src/components/ContributionHeatmap.tsx` : grille façon GitHub, faite maison (CSS grid `grid-flow-col` + `grid-rows-7`, pas de librairie dédiée — juste une grille de cases colorées, pas besoin d'une dépendance de plus pour ça). Niveaux d'intensité calculés en tiers relatifs au jour le plus chargé de la fenêtre affichée. Logique de dates extraite dans `buildHeatmapDays` (paramètre `now` injectable) pour être testable sans mocker `Date` globalement — 5 tests couvrant l'alignement sur lundi et le cas où "aujourd'hui" est un dimanche.
- **Bug réel trouvé et corrigé en vérifiant en direct** : le graphe de tonnage hebdomadaire affichait "19 juil." pour une donnée dont la vraie date (lundi) était le 20. Cause : `toLocaleDateString('fr-FR', {...})` sans `timeZone` explicite utilise le fuseau local du navigateur pour formater une date-calendrier (pas un instant) — sur une machine en UTC-10, minuit UTC du 20 juillet devient 14h locale le 19. Corrigé avec `timeZone: 'UTC'` dans `formatShortDate` (`AnalyticsPage.tsx`). Le même bug préexistait dans `ProfilePage.tsx` (date des pesées, `recorded_at` — une colonne `date` pure, pas un timestamptz) : corrigé au passage. Distinction importante retenue : `session_logs.started_at` (un vrai instant, timestamptz) doit au contraire s'afficher en heure locale du visiteur — c'est correct tel quel dans `ProgramDetailPage`, pas touché.
- `recharts` ajouté (suggéré dans le brief section 3, actif/maintenu). Chargement paresseux (`React.lazy` + `Suspense`) pour la route `/analytics` — la lib pèse à elle seule ~110kB gzippés, ne vaut pas la peine d'alourdir le bundle initial de chaque autre page. Sans ce découpage, le chunk principal serait passé de ~273kB à ~382kB gzippés.
- Testé en live sur les vraies données du user (programme réel avec des séries "Hack Squat" loggées) : heatmap, tonnage hebdo et progression 1RM s'affichent correctement après la correction du fuseau horaire.
- Historique "par séance" déjà couvert par `ProgramDetailPage` (P3a) ; "par semaine" couvert par le graphe de tonnage hebdo. Pas de vue "historique global toutes séances confondues" séparée construite pour cette phase — backlog si le besoin se précise.

## Retour post-gate Analytics : contraste des graphiques (2026-07-22)

- Retour du user (au survol/tap sur "Tonnage hebdomadaire") : le rectangle de survol Recharts par défaut est un gris clair pensé pour un thème clair — jarrant sur le thème sombre de l'app, avec en plus un contour noir disgracieux sur la barre active.
- Corrigé sur les deux graphiques (`Tonnage hebdomadaire` et `Progression par exercice`) : `Tooltip.cursor` remplacé par une teinte primary/border très subtile au lieu du gris clair par défaut ; `labelStyle`/`itemStyle` du tooltip fixés explicitement en `--popover-foreground` (ne plus dépendre des couleurs par défaut de Recharts, pensées pour un fond clair) ; `activeBar`/`activeDot` stylés pour rester dans la palette du thème plutôt que d'hériter du contour noir par défaut.
- Vérifié en live (survol + zoom sur la tooltip) : plus de rectangle gris, contraste correct, cohérent avec la philosophie d'élévation du design system ("un ton de surface plus clair plutôt que des ombres lourdes").

## Dashboard — HomePage condense les données (2026-07-22)

- `HomePage.tsx` réécrit (était un placeholder quasi-vide depuis P0). Quatre cartes, dans cet ordre : **Aujourd'hui** (jour planifié du programme actif + démarrer/continuer/revoir la séance, ou statut repos, ou invite à activer un programme) → **Dernière séance** (recap : exercice/charge/reps + 1RM estimé du dernier jour loggé, réutilise `getMostRecentHighlight`) → **Cette semaine** (nombre de séances + heatmap compacte 8 semaines, réutilise `ContributionHeatmap`) → **Poids** (dernière pesée + tendance ↑/↓ + distance à l'objectif). Carte "Mes programmes" existante conservée en bas comme nav secondaire.
- **Bug réel trouvé en construisant "Aujourd'hui" — corrigé dans la même passe** : aucune UI ne permettait de passer un programme en statut `active` depuis sa création (toujours `draft` par défaut, jamais changé nulle part). Le dashboard n'aurait donc jamais pu afficher la séance du jour pour un utilisateur réel. Ajouté un contrôle segmenté (Brouillon/Actif/Archivé) sur `ProgramDetailPage`, même pattern que le toggle jour de repos/entraînement déjà utilisé sur `SessionTemplateCard`. Vérifié en live sur "FORCE 2026" (programme réel) : activé → "Aujourd'hui" affiche "FORCE 2026 — Mardi" avec le bouton Démarrer.
- Simplification assumée : plusieurs programmes peuvent techniquement être `active` en même temps (pas de contrainte d'unicité en base) — le dashboard prend le plus récemment créé parmi les actifs. Pas de désactivation automatique des autres programmes à l'activation d'un nouveau ; à revoir si ça crée de la confusion en usage réel.
- "Aujourd'hui" compare la date du jour en **heure locale** (pas UTC) — contrairement au bucketing des graphes Analytics (qui reste en UTC pour la cohérence entre lignes de données historiques), ici on veut vraiment "quel jour est-ce pour l'utilisateur maintenant", donc heure locale est le bon choix. Deux préoccupations différentes, résolues différemment, documenté en commentaire dans `HomePage.tsx`.
- `countTrainingDaysThisWeek`/`getMostRecentHighlight` ajoutés à `analytics.ts` (purs, testés — 5 cas), réutilisent `getIsoWeekStart` (maintenant exporté) pour rester cohérents avec le graphe de tonnage hebdo.
- Nouveau test e2e `e2e/dashboard.spec.ts` : calcule le jour de la semaine réel au moment du test (pas de jour codé en dur), crée un programme, ajoute un exercice sur le jour du jour, l'active via le nouveau contrôle, vérifie que le dashboard propose de démarrer, démarre, revient sur le dashboard et vérifie "Continuer la séance".
- En creusant un échec de ce test (une race condition sur le cache TanStack Query après avoir changé le statut), vérification directe en base via REST a révélé 3 programmes de test orphelins (comptes e2e-fixture uniquement, jamais la donnée réelle) laissés par d'anciennes itérations de debug qui avaient échoué avant d'atteindre leur étape de nettoyage — pas un bug de l'app (le `deleteProgram` fonctionne correctement, vérifié directement), juste de la debris de session nettoyée au passage.

## Backlog — idées futures (2026-07-21)

Notées par le user après le gate Dashboard, explicitement "à réfléchir par la suite" — pas scopées, pas planifiées.

- [ ] **Assistant IA sur le dashboard** : chat pour lancer un entraînement, créer un programme, etc. de façon conversationnelle. Probablement la matérialisation naturelle de la "couche IA" du brief (section 4) une fois construite — voir aussi la note P2 sur le lien profil → recommandations IA.
- [ ] **Curseurs de progression sur le dashboard** : indicateurs visuels pour l'atteinte d'objectif, l'obtention d'un nouveau PR, etc. — distinct des graphiques Analytics existants, plus dans l'esprit "coup d'œil" du dashboard.
- [ ] **Aspect social** : partage d'évolution entre utilisateurs, feed avec PR et photos d'évolution. Le user a été explicite : **à construire en tout dernier**, après tout le reste (implique déjà un modèle multi-tenant, mais des considérations produit/privacy supplémentaires vu que ça expose des données à d'autres utilisateurs — à cadrer sérieusement le moment venu, pas juste "ajouter du partage").
- [ ] Sélecteur d'exercice avec visuels réels (photos) — toujours en attente des exemples 21st.dev du user (rappel, déjà noté depuis P2).

## P4a — BYOK : gestion des clés API (2026-07-21)

Démarre la "couche IA" du brief (section 4). Contrainte produit explicite du user : chaque utilisateur fournit sa propre clé API (pas de clé partagée côté app) sur Anthropic et/ou OpenAI. Architecture décidée avec le user avant implémentation : clé chiffrée côté serveur (pas 100% client) + relai par Edge Function, les deux providers dès le départ, `Suggestion de deload/recommandation` comme première feature IA prévue pour P4b.

- **Stockage** : `supabase_vault` (pgsodium), déjà activé sur le projet hébergé (`create extension if not exists` a juste confirmé l'existant — pas eu besoin de l'activer manuellement). Nouvelle table `ai_provider_keys` (`user_id`, `provider` check `in ('anthropic','openai')`, `vault_secret_id` → `vault.secrets(id)` `on delete cascade`, `is_valid`, `last_validated_at`) — la ligne ne contient jamais la clé, seulement une référence opaque vers Vault. RLS : policy `select` uniquement pour `authenticated` (`auth.uid() = user_id`) ; volontairement **aucune** policy insert/update/delete — ces écritures passent exclusivement par les Edge Functions via `service_role`, qui gèrent le secret Vault et la ligne de façon atomique.
- **Wrappers Vault** (`public.ai_vault_create_secret/update_secret/read_secret/delete_secret`, `security definer`, `search_path = ''`) : nécessaires car le schéma `vault` n'est pas exposé par PostgREST (seul `public` l'est) — les Edge Functions appellent ces wrappers en RPC via `supabase-js` plutôt que d'ouvrir une connexion Postgres directe. `revoke all ... from public, anon, authenticated` + `grant execute ... to service_role` : vérifié après coup via `information_schema.routine_privileges` que seuls `postgres`/`service_role` ont l'EXECUTE — un `authenticated` ne peut ni lire ni écrire une clé même en appelant les wrappers directement. Les 4 wrappers testés individuellement en direct (`supabase db query --linked`, create → read → update → read → delete → confirmation que la ligne `vault.secrets` a bien disparu) avant d'écrire les Edge Functions par-dessus.
- **Trois Edge Functions** (premières du projet — `supabase/functions/` n'existait pas avant P4a) : `ai-key-save` (valide la clé auprès du provider avant de la stocker, upsert si une clé existait déjà pour ce provider), `ai-key-test` (revalide une clé déjà stockée sans que le user ait à la ressaisir — déchiffre côté serveur via le wrapper, jamais renvoyé au client), `ai-key-delete` (supprime le secret Vault ; la ligne `ai_provider_keys` disparaît par cascade FK, pas de delete explicite sur la table). Toutes vérifient le JWT de l'appelant via un client anon-key avant de créer un second client `service_role` pour les écritures — le `user_id` vient systématiquement du JWT vérifié, jamais du corps de la requête.
- **Validation de clé à coût nul** : les deux providers exposent un endpoint `GET /v1/models` qui ne vérifie que l'auth sans consommer de tokens de complétion — utilisé pour valider une clé à la saisie (P4a) et lors du "retester" sans dépenser un seul appel payant. `AbortController` avec timeout 10s sur ces appels (un provider qui ne répond pas ne doit pas laisser la fonction bloquée indéfiniment).
- **Vérifié en direct** (curl avec un vrai JWT du compte e2e-fixture) : clé invalide → 422 avec message clair par provider, rien n'est persisté (confirmé par lecture directe de la table après coup) ; provider inconnu → 400 ; pas d'Authorization header → 401 ; tentative d'insert direct sur `ai_provider_keys` avec le anon/authenticated JWT → 403 RLS (confirme qu'un client ne peut pas contourner les Edge Functions). Reproduit aussi dans le navigateur (Paramètres IA, section Profil) : clé Anthropic invalide affiche "Clé Anthropic invalide ou révoquée." en direct sous le champ, le badge reste "Non configurée".
- **Non vérifié cette session, à faire par le user** : le chemin "clé valide" de bout en bout (P4a suppose que `GET /v1/models` réussit avec une vraie clé Anthropic/OpenAI — mécaniquement identique au chemin déjà testé côté erreur, mais pas rejoué avec un vrai secret) ; isolation RLS testée uniquement via écriture directe bloquée (403), pas via un second compte réel lisant les clés du premier — la policy est le même pattern `auth.uid() = user_id` que `weight_entries`/`programs`/etc., déjà éprouvé ailleurs dans le projet, mais pas re-testé en croisé ici faute d'un second compte de test.
- **UI** : nouvelle section "Paramètres IA" dans `ProfilePage` (`AiSettingsSection.tsx`), une carte par provider avec statut (badge "Non configurée"/"Validée ✓"/"Invalide"), champ mot de passe masqué, bouton retester, suppression via `ConfirmDialog` existant. Les deux champs "Clé API" avaient initialement le même label accessible ("Clé API") — corrigé en "Clé API — {provider}" (ex. "Clé API — Anthropic (Claude)") avant même d'écrire le test e2e, un vrai souci d'accessibilité (deux champs de formulaire indiscernables au lecteur d'écran), pas juste une commodité de test.
- **Nouveauté d'outillage** : `eslint.config.js` ignore désormais `supabase/functions/**` — runtime Deno (globals `Deno.*`, imports `jsr:`), pas dans le tsconfig de l'app Vite/Node, donc hors du projet TypeScript qu'ESLint utilise pour le reste du repo.
- Test e2e (`e2e/ai-settings.spec.ts`) : couvre le chemin clé invalide (le seul rejouable sans vrai secret provider). Pas de test pour le chemin clé valide, pour la même raison que les tests API n'existent pas encore dans ce repo (aucun précédent `*-api.test.ts`) — jugé cohérent avec la convention existante plutôt que d'introduire un nouveau style de test pour ce module seul.

## Retour post-P4a : la couche IA passe en toute fin de projet (2026-07-21)

- Retour du user juste après le gate P4a (BYOK terminé et vérifié, hors test avec vraie clé) : la suite de la couche IA (P4b — recommandations/deload, puis l'assistant conversationnel) est **explicitement reportée à la toute fin du projet**, au même rang que l'aspect social déjà noté "à construire en tout dernier" plus haut dans ce fichier.
- P4a lui-même reste committé et en prod tel quel (migration + Edge Functions déjà live sur Supabase, code testé) — rien à défaire. C'est la suite (branchement d'une vraie feature IA sur le pipe BYOK) qui est reportée, pas l'infrastructure de gestion des clés.
- Prochaine reprise de ce fil : revoir `TODOS.md` section P4a pour le contexte technique, puis reprendre au choix du provider par défaut / première feature (voir discussion P4b dans l'historique de conversation) une fois le reste du brief traité.

## P5a — Data ownership : export JSON et suppression de compte (2026-07-22)

Ferme la première moitié de la section "data ownership" du brief (l'import, plus complexe — remapping d'IDs sur un graphe imbriqué programme→séances→séries — reste en P5b, non commencé).

- **Piège trouvé avant même d'écrire le code** (vérifié en lisant toutes les migrations) : `ai_provider_keys.vault_secret_id` référence `vault.secrets(id) on delete cascade`, mais dans le sens "suppression du secret Vault → suppression de la ligne", pas l'inverse. Une suppression de compte qui se contenterait de `auth.admin.deleteUser()` aurait laissé les secrets Vault (clés Anthropic/OpenAI) orphelins indéfiniment — la cascade FK ne les touche pas. La nouvelle Edge Function `account-delete` nettoie donc explicitement Vault en premier (boucle sur les `ai_provider_keys` du user, `ai_vault_delete_secret` par ligne), puis appelle `admin.deleteUser()` qui cascade tout le reste (`profiles`, `programs`, `session_templates`, `session_template_exercises`, `session_logs`, `session_log_sets`, `weight_entries` — confirmé par lecture de **toutes** les migrations avant d'écrire le code : chaque table a `on delete cascade` vers `auth.users`, directement ou transitivement).
- **Export 100% client** (`src/lib/data-export-api.ts`) : pas d'Edge Function nécessaire, RLS scope déjà toutes les lectures à l'utilisateur courant — même pattern que le reste de l'app. Un objet JSON plat, un tableau par table (`weight_entries`, `exercises` custom uniquement, `programs`, `session_templates`, `session_template_exercises`, `session_logs`, `session_log_sets`) plus `profile`, avec `schema_version`/`exported_at`. `user_id` retiré de chaque ligne (implicite — c'est déjà "les données de cet utilisateur"), `id` conservé pour permettre un remapping de relations lors d'un futur import (P5b). `ai_provider_keys` volontairement absent de l'export — rien d'exportable (juste une référence Vault opaque), et pas une "donnée utilisateur" au sens où on l'entend ici.
- Les types exportés sont dérivés de `Database['public']['Tables'][T]['Row']` via `Omit`/`Pick` plutôt que redéfinis à la main — reste synchronisé automatiquement si une colonne est ajoutée plus tard, cohérent avec l'intention "exporter vraiment tout".
- Téléchargement via `Blob` + `<a download>` synthétique (`fitness-export-YYYY-MM-DD.json`), aucun backend impliqué.
- **Edge Function `account-delete`** (nouvelle) : réutilise directement `_shared/http.ts` de P4a (`requireAuthedContext`) sans modification — le helper était déjà générique, pas spécifique aux clés IA malgré son emplacement.
- Après suppression côté serveur, le client vide aussi le cache Dexie local (`sessionPlanCache`, `sessionLogs`, `sessionLogSets` — offline-first depuis P3c, données qui existent en dehors de Supabase) puis `supabase.auth.signOut()` et redirection vers `/login`.
- Refactor de passage : extrait `invokeEdgeFunction` (avec son extraction de message d'erreur depuis la réponse de la Edge Function) de `ai-keys-api.ts` vers un nouveau `src/lib/edge-function.ts` partagé — la logique était identique à ce qu'il fallait pour `deleteAccount()`, dupliquer aurait été le seul autre choix.
- **Vérifié en direct, pas juste par lecture du code** : création d'un compte jetable via l'API Admin (`POST /auth/v1/admin/users`, évite le rate-limit d'emails déjà rencontré sur ce projet — voir note plus haut dans ce fichier sur `over_email_send_rate_limit`), ajout d'une vraie pesée + d'un vrai secret Vault avec sa ligne `ai_provider_keys` associée (créés directement en SQL, hors flux `ai-key-save`, pour isoler le test du piège Vault), appel de `account-delete` avec le JWT du compte jetable, puis confirmation en base que `auth.users`, `profiles`, `weight_entries`, `ai_provider_keys` **et** `vault.secrets` sont tous à zéro pour cet id — le piège identifié plus haut est bien corrigé, pas seulement en théorie.
- Test e2e (`e2e/data-export.spec.ts`) : couvre l'export (téléchargement intercepté via `page.waitForEvent('download')`, contenu JSON vérifié — présence de tous les tableaux attendus, absence de `ai_provider_keys`). Pas de test e2e pour la suppression de compte — trop destructif pour tourner dans la suite partagée (supprimerait potentiellement le compte fixture e2e si mal isolé) ; vérifié à la place manuellement avec un compte jetable dédié, comme décrit ci-dessus.

## P5b — Import JSON (2026-07-22)

Ferme la section "data ownership" du brief. Import ajouté dans `src/lib/data-export-api.ts` (`parseUserDataExport` + `importUserData`), même fichier que l'export, réutilise le type `UserDataExport`.

- **`session_templates` n'a pas de policy INSERT — piège trouvé en écrivant le premier jet, corrigé avant même de tester** : chaque programme reçoit ses 7 jours fixes (lundi-dimanche) via un trigger `on_program_created` à la création (`20260721220320_fixed_weekdays_session_templates.sql`), et la policy insert a été explicitement supprimée à l'époque pour préserver l'invariant "toujours exactement 7 jours". Importer un jour ne peut donc jamais être un insert — le code récupère les 7 jours déjà auto-créés sur le nouveau programme (matchés par `day_of_week`, la vraie clé naturelle de la table) et fait un `update` de leur `day_type` plutôt qu'un insert. Repéré en lisant la policy RLS live (`pg_policies`) avant d'écrire le code, pas après un échec — mais la suite a quand même révélé deux bugs réels, tous deux trouvés en testant le scénario "exporter puis réimporter dans le même compte" via e2e (`e2e/data-import.spec.ts`), un cas que rien d'autre dans le repo n'exerçait jusqu'ici :
  1. **Collision de tri sur `created_at`** : la première version préservait `created_at`/`updated_at` de l'export sur les lignes recréées (programmes, exercices, `session_template_exercises`, `session_logs`, `session_log_sets`) — cohérent en apparence avec la philosophie "restaurer les vraies données", mais un programme réimporté dans le même compte se retrouvait avec le **même `created_at`, à la microseconde près**, que l'original. Comme `fetchPrograms()` trie par `order by created_at desc` sans tie-breaker, Postgres ne garantit aucun ordre stable entre deux lignes à égalité — la liste changeait d'ordre de façon non déterministe entre deux refetch, un vrai bug produit (pas juste un souci de test) pour quiconque réimporte dans un compte non-vide. Corrigé : seules les dates réellement porteuses de sens métier restent préservées (`recorded_at` des pesées, `started_at`/`completed_at` des séances loguées — celles dont dépendent les graphes Analytics) ; `created_at`/`updated_at`, simples métadonnées de ligne sans lecteur applicatif, ne sont plus reportés — chaque ligne importée reçoit un timestamp frais.
  2. **`invalidateQueries()` sans filtre dans `useImportUserData`, combiné à un bug préexistant dans `usePrograms.ts`** : `useDeleteProgram`/`useCreateProgram`/`useDuplicateProgram` avaient `onSuccess: () => queryClient.invalidateQueries(...)` (corps d'expression, donc la promesse est **retournée**, pas juste déclenchée) — `mutateAsync()` attend cette promesse avant de se résoudre. Supprimer un programme depuis sa page de détail invalide `['programs']`, qui matche en préfixe la query `['programs', id]` **encore montée** (on est toujours sur cette page) ; son refetch échoue (la ligne vient de disparaître) et retry avec backoff avant que l'invalidation ne se résolve — le bouton de suppression restait désactivé plusieurs secondes, parfois au-delà du timeout de test. `useUpdateProgram` avait déjà le bon pattern (`void queryClient.invalidateQueries(...)`, non bloquant) ; aligné `useCreateProgram`/`useDeleteProgram`/`useDuplicateProgram` dessus. Bug préexistant, pas introduit par l'import — mais l'invalidation globale (`invalidateQueries()` sans clé) de la première version de `useImportUserData` ajoutait une charge de fond qui le rendait beaucoup plus visible ; corrigé aussi en scopant l'invalidation de l'import aux 5 clés réellement concernées (`profile`, `weight-entries`, `programs`, `exercises`, `set-history`) plutôt qu'une invalidation globale.
  - **Backlog** : le même pattern bloquant (`onSuccess` sans `void`) existe dans `useAiProviderKeys.ts`, `useExercises.ts`, `useSessionTemplates.ts`, `useWeightEntries.ts` — pas corrigé ici faute de preuve concrète d'impact (aucun test ne l'a fait apparaître), mais candidat à un passage de nettoyage dédié.
- **Sémantique d'import** : chaque ligne recréée avec un nouvel id (remapping en mémoire, `Map<ancien_id, nouvel_id>` par table) — réimporter ou importer dans un compte non-vide crée des doublons visibles plutôt qu'une fusion ou une corruption silencieuse. Seule exception : `weight_entries`, upsert sur `(user_id, recorded_at)` (vraie clé naturelle de la table). `exercise_id` sur `session_template_exercises` : remappé si l'exercice est un exercice custom présent dans l'export, laissé tel quel sinon (catalogue partagé, supposé stable sur le même projet).
- Pas de transaction Postgres — insertions séquentielles, erreurs accumulées ligne par ligne dans un tableau retourné à l'UI (`ImportResult { imported, errors }`), cohérent avec le reste du code où la logique métier vit en TypeScript et Postgres ne fait que tables + RLS.
- UI : nouvelle sous-section dans "Mes données" (`DataOwnershipSection.tsx`) — sélection de fichier → résumé du contenu (nombre de programmes/séances/pesées/etc. détectés) → confirmation (`ConfirmDialog`) → import. Le trigger et le bouton de confirmation avaient initialement le même texte ("Importer") — corrigé en "Confirmer l'import" pour le bouton de la modale, à la fois pour l'accessibilité (deux boutons visibles simultanément avec le même label) et pour que les tests e2e puissent les cibler sans ambiguïté.
- Tests e2e (`e2e/data-import.spec.ts`) : fichier JSON invalide → erreur claire, rien n'écrit ; export réel d'un programme puis réimport dans le même compte → doublon créé avec un nouvel id, nettoyé en fin de test via l'UI de suppression. Le test de suppression attend explicitement que la liste reflète chaque suppression avant de continuer (la query de liste n'étant pas "active" pendant qu'on est sur une page de détail, l'invalidation ne la refetch pas immédiatement — elle affiche ses dernières données connues le temps qu'un refetch en arrière-plan se termine) plutôt que de supposer la liste à jour dès que l'URL change.

## P6a — Planning bien-être : activités récurrentes + vues jour/semaine (2026-07-22)

Première moitié de la section "Planning bien-être quotidien" du brief (les notifications push PWA restent en P6b — nécessitent une vraie nouvelle brique d'infra, VAPID + service worker custom + scheduler serveur, volontairement séparées pour valider le modèle de données et l'UI d'abord).

- **Modèle de récurrence** : même pattern que `session_templates` (jours fixes 1-7) plutôt qu'un système général — `wellness_activities` (`name`, `days_of_week smallint[]`, `reminder_time` nullable, `active`) + `wellness_activity_logs` (une ligne = "fait" un jour donné, `unique(activity_id, completed_date)`, décocher = simple delete). `reminder_time` est déjà dans le schéma bien qu'inexploité avant P6b, pour éviter une deuxième migration.
- **`days_of_week`** contraint par `check (days_of_week <@ array[1..7]::smallint[])` plutôt que d'ajouter une table de jointure — cohérent avec la simplicité du reste du modèle jour-fixe.
- **UI** : nouvelle page `/bien-etre`, nav ajoutée dans `AppLayout` (icône `Sparkles`). Vue jour/semaine via le même segmented control (`Button` variant default/ghost dans un pill bordé) déjà utilisé pour `day_type`/`status` ailleurs dans l'app — pas de nouveau composant `Switch`/`Toggle`, le projet n'en a pas et n'en avait pas besoin. Formulaire de création/édition (`ActivityFields`) partagé entre la création inline (pattern `WeightSection`) et l'édition en `Dialog` — évite de dupliquer le multi-toggle de jours.
- **Vue semaine** : grille faite maison (pas de librairie), un cercle par jour par activité — vide/gris si non programmé, contour si programmé mais pas fait, rempli si fait. Dates de la semaine calculées en **heure locale** (comme "Aujourd'hui" du dashboard, pas comme le bucketing UTC d'Analytics — même distinction déjà documentée pour le dashboard).
- **`noUncheckedIndexedAccess`** (déjà actif dans `tsconfig.app.json`) a forcé deux ajustements propres plutôt que des `!` dispersés : `getCurrentWeekDates` retourne un tuple `[string,string,string,string,string,string,string]` (7 éléments garantis par construction) au lieu de `string[]`, et un helper `weekdayLabel(day)` centralise l'unique assertion non-null nécessaire (clé toujours 1-7 par construction, jamais un lookup arbitraire).
- **Vérifié en direct dans le navigateur — incident et correction** : en testant manuellement, l'onglet Chrome avait une session déjà persistée sur le **vrai compte** (`arnaud.zeh@gmail.com`) plutôt que le compte e2e-fixture — ma tentative de connexion avec les identifiants de test n'a rien fait puisque déjà authentifié. Une activité de test ("Wim Hof matinal") a été créée par erreur sur le vrai compte, repérée en comparant l'UI à une requête directe en base (`user_id` ne correspondait pas au compte e2e-fixture attendu), supprimée immédiatement après confirmation du user. Session déconnectée avant de poursuivre ; la suite de la vérification s'est faite exclusivement via les tests e2e Playwright (isolés par construction via `e2e/.auth/user.json`), pas de nouvelle session manuelle dans le navigateur.
- Tests e2e (`e2e/wellness.spec.ts`) : création avec le jour du jour réel sélectionné (calculé au moment du test, pas codé en dur — même principe que `dashboard.spec.ts`) → apparaît en vue jour, "Marquer comme fait" → persiste après un rechargement complet de page (pas juste un état local), visible aussi en vue semaine, nettoyage en fin de test. Deuxième test : le bouton "Ajouter" reste désactivé sans nom et sans jour sélectionné.
- **Trouvé et nettoyé au passage** : le compte e2e-fixture avait accumulé 18 programmes de test orphelins (dont 16 nommés identiquement "E2E Analytics …", débris de runs interrompus pendant les sessions de debug intensif de P5b/P6a aujourd'hui — le test correspondant a bien un cleanup, juste jamais atteint quand le run était interrompu avant la fin). Ce volume ralentissait suffisamment l'import (P5b) pour dépasser le timeout du test — pas une régression de code, juste de la donnée de test à nettoyer. Compte remis à zéro (0 programme, catalogue d'exercices partagé intact).

## P6b — Notifications push PWA pour le planning bien-être (2026-07-22)

Deuxième moitié de la section "Planning bien-être quotidien" du brief. `reminder_time` existait déjà dans le schéma depuis P6a (colonne inexploitée) ; P6b ajoute toute l'infrastructure pour réellement envoyer un rappel au moment prévu.

- **Service worker custom** : bascule de `generateSW` (stratégie par défaut de `vite-plugin-pwa`, utilisée jusque-là) à `injectManifest` — `generateSW` génère un SW opaque sans place pour du code custom, alors qu'un rappel push a besoin d'écouter `push` et `notificationclick` soi-même. `src/sw.ts` : précache via `workbox-precaching` (`precacheAndRoute(self.__WB_MANIFEST)`, seule brique conservée de `generateSW`), `push` affiche une notification à partir du payload JSON (titre/corps/URL, avec un fallback générique si le payload est absent ou malformé), `notificationclick` referme la notification et navigue/focus un client existant vers l'URL (`/bien-etre` par défaut) ou ouvre une nouvelle fenêtre sinon.
- **Paire de clés VAPID** générée une fois (`web-push generate-vapid-keys`) : la publique va dans `VITE_VAPID_PUBLIC_KEY` (`.env.local`, safe à exposer côté client — c'est le principe même de la paire publique/privée), la privée uniquement comme secret d'Edge Function (`supabase secrets set VAPID_PRIVATE_KEY=...`), jamais commitée nulle part.
- **`profiles.timezone`** (IANA, ex. `Europe/Paris`) : `reminder_time` est une heure murale saisie dans le navigateur, mais le scheduler tourne côté serveur en UTC — sans fuseau stocké, un rappel à 7h partirait à 7h UTC. Auto-détecté côté client (`Intl.DateTimeFormat().resolvedOptions().timeZone`) et synchronisé silencieusement à chaque chargement de l'app (`syncTimezone()` dans `profile-api.ts`, appelé depuis un `useEffect` de `AppLayout` — donc sur toute route authentifiée, pas seulement `/bien-etre`) ; best-effort, une erreur réseau est avalée (`.catch(() => {})`) plutôt que de casser l'affichage pour une donnée non bloquante. Comparaison avant écriture pour éviter un `update` inutile à chaque visite quand le fuseau n'a pas changé.
- **`push_subscriptions`** (nouvelle table, P6a→P6b) : un abonnement par `endpoint` (déjà unique par device/navigateur de par la Push API elle-même), RLS standard scopée `auth.uid() = user_id`. **Piège trouvé après coup, corrigé dans une migration séparée** : la migration initiale n'avait que select/insert/delete, pas de policy update — or le client fait un `upsert(..., { onConflict: 'endpoint' })` pour gérer le cas "même navigateur se réabonne" (ex. après avoir désactivé puis réactivé), et le chemin `ON CONFLICT DO UPDATE` nécessite une policy UPDATE même si l'intention initiale était "jamais de update direct". Migration `20260722160000` ajoutée plutôt que d'éditer la migration déjà poussée en base.
- **`wellness_reminder_sends`** : registre d'envoi interne au scheduler, RLS activée sans **aucune** policy pour `authenticated` (même raisonnement que l'absence de policy insert sur `session_templates`) — `unique(activity_id, sent_date)` sert de verrou d'idempotence : si deux exécutions du cron se chevauchent, la seconde reçoit une violation de contrainte sur l'insert et saute l'envoi plutôt que de notifier deux fois.
- **Edge Function `send-wellness-reminders`** : pour chaque activité active avec un `reminder_time`, convertit l'heure serveur (UTC) en heure murale du fuseau du propriétaire (`Intl.DateTimeFormat` avec `timeZone` + `hourCycle: 'h23'`), compare au `reminder_time`/jour de la semaine de l'activité, insère dans `wellness_reminder_sends` (verrou d'idempotence ci-dessus) puis envoie via `npm:web-push` à tous les abonnements du user. Un abonnement qui répond 404/410 (device désinstallé, permission révoquée, données navigateur effacées) est auto-supprimé de `push_subscriptions` plutôt que retenté indéfiniment.
- **Auth de la fonction — deux essais, le premier a échoué en test réel, pas juste en théorie** : cette fonction n'est appelée que par `pg_cron`/`pg_net`, jamais par un utilisateur final — pas de JWT de session à vérifier. Premier essai : comparer l'en-tête `Authorization` à `Bearer ${SUPABASE_SERVICE_ROLE_KEY}` (le secret auto-injecté par la plateforme dans chaque Edge Function). Testé en direct avec la vraie clé `service_role` récupérée via `supabase projects api-keys` : rejeté (401) — la valeur auto-injectée dans le runtime des fonctions ne correspond pas à celle renvoyée par cette commande CLI (probablement un format de clé différent/rotation). Abandonné au profit d'un secret dédié et généré par nous (`CRON_SECRET`, 32 octets aléatoires), stocké à la fois comme secret d'Edge Function et dans Supabase Vault (`vault.create_secret(..., 'cron-invoke-secret')`), envoyé via un en-tête custom `x-cron-secret` plutôt que `Authorization` — celui-ci est de toute façon intercepté par la vérification JWT de la plateforme *avant* que le code de la fonction ne s'exécute (confirmé en observant `UNAUTHORIZED_NO_AUTH_HEADER`, une erreur qui ne vient pas de notre code). Fonction déployée avec `--no-verify-jwt` (`[functions.send-wellness-reminders] verify_jwt = false` dans `config.toml`, pour que les futurs redéploiements gardent ce comportement) puisqu'il n'y a structurellement aucun JWT à vérifier ici.
- **`pg_cron` + `pg_net`** (migration `20260722150000`, jamais activés sur ce projet avant P6b) : job `send-wellness-reminders-every-minute` (`* * * * *`) qui appelle `public.invoke_send_wellness_reminders()`, une fonction `security definer` qui lit `cron-invoke-secret` depuis `vault.decrypted_secrets` puis fait `net.http_post` vers l'Edge Function avec l'en-tête `x-cron-secret` — le secret n'apparaît jamais en clair dans une migration committée, seulement au moment de l'exécution côté serveur.
- **Vérifié en direct, pas juste par lecture du code** : `curl` sans en-tête → 401 (plateforme) ; avec `x-cron-secret` invalide → 401 (notre check) ; avec la vraie valeur → `200 {"sent":0,"skipped":0}` (aucun abonnement/activité en base au moment du test, réponse attendue). Après le `db push` de la migration cron, confirmé `cron.job` contient bien le job actif puis attendu ~75s en tâche de fond (`Monitor`) pour voir une ligne apparaître dans `cron.job_run_details` avec `status = 'succeeded'` — le job tourne réellement en prod, pas seulement "créé sans jamais s'exécuter".
- **Client — abonnement push** (`src/lib/push-api.ts`, `src/hooks/useNotifications.ts`) : `getNotificationSupport()` distingue trois états — `supported` (Notification/serviceWorker/PushManager présents), `ios-not-installed` (Safari iOS n'expose la Web Push API qu'à une PWA ajoutée à l'écran d'accueil ; sans ça `Notification`/`PushManager` sont simplement absents du `window`, indiscernable de "pas supporté du tout" sans détecter spécifiquement ce cas via `navigator.userAgent` + `matchMedia('(display-mode: standalone)')`), et `unsupported` (carte masquée entièrement). `subscribeToPush` : demande la permission, `pushManager.subscribe()` avec la clé VAPID publique convertie en `Uint8Array`, puis `upsert` de l'endpoint/clés dans `push_subscriptions`. `unsubscribeFromPush` : supprime la ligne DB puis appelle `subscription.unsubscribe()` (dans cet ordre — si la ligne DB restait après un `unsubscribe()` réussi, le scheduler tenterait d'envoyer à un endpoint mort jusqu'au prochain cycle d'auto-nettoyage 404/410).
- **Détail TypeScript** : `Uint8Array.from(...)` type désormais en `Uint8Array<ArrayBufferLike>`, que `PushSubscriptionOptionsInit.applicationServerKey` (qui attend un `ArrayBufferView<ArrayBuffer>`) rejette — remplacé par `new Uint8Array(length)` rempli par boucle, dont le buffer sous-jacent est garanti `ArrayBuffer` (pas `SharedArrayBuffer`). Erreur qui n'apparaissait qu'en `tsc -b` (mode projet, utilisé par `npm run build`), pas en `tsc --noEmit` seul — piège à surveiller si un futur diagnostic type-check en local ne matche pas ce que `npm run build` rapporte.
- **UI** : nouvelle carte "Notifications" sur `/bien-etre` (au-dessus du formulaire de création), masquée si `unsupported`, message dédié "ajoutez à l'écran d'accueil" si `ios-not-installed`, sinon bouton "Activer/Désactiver les notifications" reflétant l'état d'abonnement réel du navigateur (pas juste un flag local — relit `pushManager.getSubscription()` au montage).
- **Non vérifié cette session, à faire par le user** : la réception réelle d'une notification (créer une activité avec un `reminder_time` proche, s'abonner depuis un vrai navigateur, attendre le rappel, cliquer dessus pour confirmer l'ouverture sur `/bien-etre`) — délibérément non testé via le navigateur automatisé cette session : la vérification manuelle P6a a montré qu'un onglet Chrome peut porter une session déjà authentifiée sur le **vrai compte** plutôt que le compte e2e-fixture, et s'abonner réellement pousserait une vraie ligne `push_subscriptions` + potentiellement un vrai push vers ce compte. Toute l'infrastructure serveur (auth de la fonction, cron, idempotence) a en revanche été vérifiée en direct comme détaillé ci-dessus.
- Tests : `e2e/wellness.spec.ts` étendu d'un test vérifiant que la carte "Notifications" s'affiche avec un bouton "Activer les notifications" actif (Chromium headless expose bien `Notification`/`serviceWorker`/`PushManager`) — le clic réel n'est pas testé (déclencherait un vrai abonnement contre le service de push de Google depuis l'environnement de test, potentiellement flaky et laissant une ligne persistante sur le compte fixture), cohérent avec le même choix déjà fait pour le chemin "clé IA valide" en P4a.

## P7 — Hypoxie intermittente (2026-07-22)

Décisions confirmées avec le user avant implémentation : pas de presets pré-remplis (liste vide au départ, cohérent avec le reste de l'app), un seul point d'entrée dans le workflow de séance (lien "Respiration" dans le header de `SessionLogPage`, utilisable à tout moment plutôt qu'un bouton dupliqué par exercice), et l'historique des sessions complétées **est** loggé (contrairement à un premier réflexe "pas d'analytics pour l'instant") — pose les bases d'un futur affichage sans devoir remonter le schéma plus tard.

- **Modèle de données** (migration `20260722170000`, même structure double-table que `wellness_activities`/`wellness_activity_logs` en P6a) : `breath_protocols` (`name`, `hold_seconds`, `recovery_seconds`, `cycles`, tous `smallint check (... > 0)`) + trigger `set_updated_at` réutilisé tel quel ; `breath_session_logs` (`protocol_id` → `on delete cascade`, `completed_cycles`, `started_at`, `completed_at`) en select/insert/delete seulement, pas d'update — un log ne se modifie pas après coup, même raisonnement que `wellness_activity_logs`.
- **Définition de "cycle complété"** : un cycle compte comme fait dès que sa phase d'**apnée** est terminée — la récupération n'est que la pause avant le cycle suivant, pas elle-même le "travail". Un arrêt manuel en cours de récupération du cycle N logge donc `completed_cycles = N` ; un arrêt en cours d'apnée du cycle N logge `N - 1`. Rien n'est loggé si l'utilisateur arrête avant même d'avoir terminé une seule apnée (`completed_cycles = 0` volontairement pas inséré — une tentative avortée immédiatement n'est pas une "session").
- **Minuteur plein écran** (`BreathRunner` dans `BreathPage.tsx`) : state machine pure (`RunnerState { phase, cycleIndex, secondsLeft }` + fonction `advance()` déterministe) plutôt qu'un enchaînement de `useState` épars — apnée → récup, répété `cycles` fois, puis `phase: 'done'`. Décompte par `setTimeout` récursif (même pattern que `RestTimer`), avec un `hasFiredRef` remis à `false` à chaque tick normal pour garantir qu'une transition de phase ne se déclenche qu'une seule fois par passage à zéro — nécessaire car mettre l'appel `vibrate`/`playBeep` directement dans le callback de `setState` aurait été invoqué deux fois par le double-appel StrictMode des updater functions en dev (piège identifié avant d'écrire le code, pas après un bug observé) ; les effets de bord restent donc dans le corps de l'effet, jamais dans un updater `setState`.
- **Refactor de passage** : `playBeep`/`formatTime` (et l'appel `navigator.vibrate`, désormais `vibrate()`) extraits de `RestTimer.tsx` vers `src/lib/timer-feedback.ts` — deuxième minuteur ayant besoin exactement de la même logique de retour haptique/sonore, une vraie déduplication plutôt qu'une abstraction prématurée. `RestTimer.tsx` importe désormais ces helpers sans changement de comportement (vérifié par la suite e2e existante, toujours verte).
- **"Passer"** (skip) appelle directement `advance()` sans passer par le décompte — pas de vibration/son sur un skip manuel (signal déjà explicite, contrairement à une transition automatique). **"Arrêter"** logge les cycles réellement complétés puis ferme l'overlay ; **"Pause"** suspend simplement le `useEffect` de décompte (`paused` bool), sans toucher au state de phase/cycle.
- **UI** : nouvelle page `/apnee` (icône `Wind` dans `AppLayout`, jusque-là inutilisée dans l'app), même structure 3-parties que `WellnessPage` (`CreateProtocolCard` / `ProtocolsListCard` / `EditProtocolDialog`, champ `ProtocolFields` partagé création/édition). Le minuteur est un overlay `fixed inset-0` custom (pas le composant `Dialog` shadcn, pensé pour une boîte centrée, pas un plein écran immersif façon "mode gym") monté conditionnellement depuis `BreathPage` plutôt qu'une route séparée — évite les edge cases de bouton retour du navigateur en plein protocole.
- **Point d'entrée séance** : bouton "Respiration" dans le header de `SessionLogPage`, à côté de "Terminer la séance"/"Supprimer" — simple lien vers `/apnee`, pas d'ouverture directe du minuteur (l'utilisateur choisit son protocole une fois sur la page), utilisable avant, entre deux exercices ou après la séance sans duplication par carte d'exercice.
- **Vérifié en direct** : build complet (`tsc -b && vite build`, y compris régénération du service worker), `tsc --noEmit`, ESLint, 67 tests unitaires. Tests e2e (`e2e/breath.spec.ts`) : un run réel à son terme (apnée 1s / récup 1s / 1 cycle, décompte réel observé jusqu'à "Protocole terminé 🎉", pas juste le bouton "Passer"), le bouton "Passer" testé séparément (avance immédiatement sans attendre), et le formulaire de création rejeté sans champs requis. Suite e2e complète (22 tests) rejouée in extenso après l'ajout, aucune régression. Fixture account confirmé à 0 ligne `breath_protocols`/`breath_session_logs` après nettoyage des tests (cascade delete du protocole entraîne bien la suppression de son historique).

## P8 — Module cycles menstruels (2026-07-22)

Décisions confirmées avec le user avant implémentation : saisie minimale (juste les dates de début de règles, phase calculée depuis l'historique — pas de date de fin ni de notes), activation via un toggle dans le Profil (`profiles.cycle_module_enabled`, désactivé par défaut, aucune icône de nav ni donnée tant que non activé), et pas d'intégration Dashboard pour ce premier passage (page dédiée `/cycle` uniquement).

- **Contrainte non négociable du brief respectée explicitement** : "aucune donnée sensible en localStorage non chiffré, notamment sur le module cycles" — `cycle_entries` ne passe **pas** par le cache offline Dexie (contrairement à `session_logs`/`session_log_sets` depuis P3c) ; lecture directe Supabase à chaque visite de `/cycle`, aucune persistance locale au-delà du cache en mémoire de TanStack Query (qui disparaît à la fermeture de l'onglet, jamais écrit sur disque).
- **Calcul de phase, volontairement pas de l'IA** : fonction pure `computeCyclePhase()` (`src/lib/cycle-phase.ts`), testée unitairement (11 tests, `cycle-phase.test.ts` — mêmes conventions que `one-rep-max.test.ts`). Longueur de cycle moyenne dérivée de l'écart entre dates de début consécutives (défaut 28j avec moins de 2 entrées) ; jour d'ovulation estimé par `longueur_cycle − 14` (la phase lutéale dure ~14j de façon relativement stable, contrairement à la phase folliculaire qui varie davantage — approximation standard, pas une position fixe dans le cycle). Le "jour du cycle" boucle (`modulo`) si aucune nouvelle date n'a été saisie au-delà de la longueur estimée, pour continuer à afficher une estimation plutôt que de s'arrêter net.
- **Conseils par phase = texte statique**, pas de calcul personnalisé ni d'appel LLM (`src/lib/cycle-advice.ts`, `CYCLE_PHASE_ADVICE`) : l'app ne trace aucune donnée nutritionnelle, donc rien à adapter numériquement — les recommandations "nutrition" du brief (besoins caloriques/glucidiques, vigilance déficit énergétique) restent volontairement qualitatives. Cohérent avec la couche IA différée à la fin du projet.
- **Disclaimer non-médical** : bandeau toujours visible en haut de `/cycle` (pas seulement au premier accès), mentionnant explicitement que ce ne sont pas des recommandations médicales et qu'elles ne tiennent pas compte de conditions particulières (SOPK, endométriose, contraception hormonale) — au-delà du minimum "cadré comme recommandations générales" du brief.
- **Modèle de données** (migration `20260722180000`) : `profiles.cycle_module_enabled boolean not null default false` (nouvelle colonne, pas une table à part — cohérent avec le reste du profil) ; `cycle_entries` (`start_date`, `unique(user_id, start_date)` pour empêcher un doublon silencieux à la même date) en CRUD complet (contrairement aux tables de logs pures du reste de l'app comme `wellness_activity_logs`/`breath_session_logs` — une date de règles mal saisie se corrige plus naturellement par édition que par delete+recreate, d'où une policy update ici).
- **UI** : toggle "Activer/Désactiver" instantané dans `ProfilePage` (`CycleModuleSection`, même pattern "bouton bascule" que le "Active/Inactive" de `WellnessPage`, pas de soumission de formulaire séparée) ; icône `Moon` dans `AppLayout` rendue conditionnellement (`profile?.cycle_module_enabled`, réutilise le `useProfile()` déjà en cache côté `HomePage`/`ProfilePage` — aucun fetch dupliqué) ; accès direct à `/cycle` sans activation → message d'invitation avec lien vers le Profil plutôt que la page complète.
- **Bug trouvé et corrigé pendant les tests e2e, pas dans l'app** : le premier jet du test utilisait `new Date().toISOString().slice(0, 10)` (date UTC) pour saisir "aujourd'hui" dans le champ `type="date"`, alors que `CyclePage` calcule "aujourd'hui" en date **locale** (`todayLocalDate()`, mêmes composants `getFullYear`/`getMonth`/`getDate` que `WellnessPage`/`toLocalDateString`) — l'environnement de test tournant en UTC-10 (Tahiti), les deux dates ne coïncidaient pas et le jour de cycle calculé ne correspondait pas à l'attendu du test. Corrigé dans le test (même fonction `todayLocalDate()` reproduite), le code applicatif était correct dès le départ.
- **Vérifié en direct** : `tsc --noEmit`, ESLint, build complet, 78 tests unitaires (dont les 11 nouveaux sur le calcul de phase). Tests e2e (`e2e/cycle.spec.ts`, `describe.serial` car les tests dépendent de l'état d'activation les uns des autres, avec un `afterAll` de nettoyage best-effort) : accès à `/cycle` désactivé → message d'invitation, pas l'icône de nav ; activation depuis le Profil → icône `Cycle` apparaît ; ajout d'une date du jour → "Phase actuelle : Menstruelle", jour 1/28, conseils entraînement+nutrition affichés, puis suppression ; désactivation → icône disparaît. Suite e2e complète rejouée (26 tests) : une seule flakiness observée sur `wellness.spec.ts` (carte notifications, timeout après la suite longue), non reproduite en isolation — sans lien avec ce module.

## Audit pré-merge Lighthouse/PWA (2026-07-22/23)

Dernier point du brief avant les items explicitement différés (couche IA, social). Objectif : passer l'app (build de production, pas le dev server) au crible Lighthouse + vérifier que les promesses PWA (installable, hors-ligne) tiennent vraiment, pas juste sur le papier.

- **La catégorie "PWA" de Lighthouse n'existe plus** : Google l'a retirée de l'outil (confirmé sur la version 13.4.1 installée ici — `--only-categories` n'accepte plus que `accessibility, best-practices, performance, seo, agentic-browsing`, et les audits historiques `installable-manifest`/`service-worker`/`splash-screen`/`maskable-icon` ont disparu de `--list-all-audits`, pas juste été déplacés). Plutôt que sauter cette partie de l'audit, remplacé par une vérification fonctionnelle directe (scripts Playwright jetables, supprimés après usage) : `navigator.serviceWorker.controller` bien défini après un premier reload (le SW contrôle réellement la page, pas juste "enregistré"), et surtout un vrai **reload à froid avec `context.setOffline(true)`** sur plusieurs routes — un test qui prouve le comportement plutôt que de vérifier des heuristiques statiques.
- **Bug réel trouvé par ce test offline, absent de tout ce qui existait avant** : le reload à froid échouait purement et simplement (`net::ERR_INTERNET_DISCONNECTED`) sur **toutes** les routes, y compris `/`. Cause : `src/sw.ts` (écrit à la main pour P6b, cf. section P6b) fait `precacheAndRoute(self.__WB_MANIFEST)` mais ne route que des URLs exactement précachées — `index.html` est bien caché, mais aucune règle ne mappe une navigation vers `/bien-etre` (ou même `/`) vers ce fallback. `generateSW` (la stratégie utilisée avant P6b) injecte cette redirection automatiquement ; le passage à `injectManifest` (nécessaire pour les listeners `push`/`notificationclick`) l'a fait disparaître silencieusement, et rien ne l'avait détecté depuis. **Corrigé** par un handler `fetch` écrit à la main (`event.request.mode === 'navigate'` → sert `matchPrecache('/index.html')`) plutôt que par `workbox-routing`/`NavigationRoute` (l'approche standard documentée) — cf. point suivant.
- **`workbox-routing` incompatible avec le SW de dev de `vite-plugin-pwa`, découvert en testant, pas en théorie** : la première version du fix utilisait `NavigationRoute`+`registerRoute` (`workbox-routing`), qui fonctionnait parfaitement une fois buildé en production (`vite build` + `vite preview`, reload offline vérifié sur `/`, `/login`, `/bien-etre`) mais cassait **l'enregistrement même du service worker** en mode `pnpm dev` (`devOptions.enabled: true`, activé depuis P6b pour tester les notifications push localement) — `ServiceWorker script evaluation failed`, sans détail exploitable renvoyé par `register()`. Isolé en retirant le SW à un `precacheAndRoute` minimal (l'enregistrement repassait), confirmant que le module ESM pré-bundlé de `workbox-routing` ne survit pas au harnais de dev SW de `vite-plugin-pwa` alors que `workbox-precaching` seul le supporte. Plutôt que de creuser cette incompatibilité plus loin, réécrit le fallback à la main avec `matchPrecache` (déjà exporté par `workbox-precaching`, déjà éprouvé) — quelques lignes, une dépendance en moins, et ça évite la classe entière du problème. `workbox-routing` retiré de `package.json`.
- **Cet enregistrement cassé avait un effet de bord concret** : il a fait régresser silencieusement `e2e/wellness.spec.ts` ("carte notifications... bouton actif") — `usePushSubscription` attend `navigator.serviceWorker.ready`, une promesse qui ne se résolvait plus jamais quand l'enregistrement échouait, laissant le bouton bloqué `disabled`. Repéré, diagnostiqué et corrigé avant de committer, pas après ; suite e2e complète rejouée pour confirmer.
- **`manifest.webmanifest` avait `lang: "en"` par défaut** (valeur par défaut de `vite-plugin-pwa` quand `lang` n'est pas explicitement fourni), alors que l'app entière est en français (`<html lang="fr">`). Ajouté `lang: 'fr'` à la config `manifest` de `vite.config.ts`. Pas de champ maskable icon — backlog, nécessite un vrai artwork avec zone de sécurité, pas juste une case à cocher.
- **Découpage du bundle par route (`React.lazy`)** : toutes les pages sauf `LoginPage`/`SignupPage`/`HomePage` (déjà chargées avant/juste après connexion, pas de bénéfice à les retarder) sont passées en lazy, avec un seul `<Suspense>` partagé autour de `<Outlet />` dans `AppLayout` plutôt qu'un par route (`AnalyticsPage` avait déjà ce pattern depuis Analytics — généralisé, plus de wrapper `Suspense` dupliqué dans `App.tsx`). Effet réel mesuré : le chunk principal passe de 979 Ko à 384 Ko minifiés ; une bonne partie du reste (react-dom, radix-ui, supabase-js, tanstack-query) se retrouve dans un chunk partagé nommé arbitrairement par Rollup (`card-*.js`, ~500 Ko) toujours chargé en `modulepreload` — attendu, ce sont des dépendances réellement partagées par `LoginPage` elle-même, pas une réoptimisation manquée. Le gain concret : le JS **spécifique** à `ProgramDetailPage`/`ProfilePage`/`SessionLogPage`/etc. ne fait plus partie du chemin critique de `/login`. Poussé plus loin (vendor-splitting manuel de React/Radix) jugé hors scope pour un projet solo sans pression de trafic — backlog si le besoin se fait vraiment sentir.
- **Trouvaille accessibilité réelle (WCAG), pas cosmétique** : `link-in-text-block` signalait le lien "Créer un compte" de `LoginPage` — contraste insuffisant (1.97:1, minimum 3:1) avec le texte environnant et aucun style (soulignement) pour le distinguer visuellement en dehors du survol. Corrigé sur `LoginPage` et son jumeau structurel `SignupPage` ("Se connecter") : soulignement permanent (`underline`, plus seulement `hover:underline`), couleur qui change au survol plutôt que l'inverse — même esprit que `DialogDescription` qui a déjà ce pattern. Score accessibilité Lighthouse : 93 → 100. D'autres liens `hover:underline` existent ailleurs (HomePage, ProgramDetailPage) mais ce sont des lignes isolées, pas du texte mélangé à un lien — Lighthouse ne les signale pas, laissés tels quels.
- **`meta description` et `robots.txt` ajoutés** — l'app est un outil personnel authentifié, jamais destiné à être indexé : `robots.txt` (`Disallow: /`) est le choix correct, pas un hack de score. Effet secondaire attendu et accepté : le score SEO Lighthouse **baisse** (82 → 63, l'audit `is-crawlable` échoue délibérément) puisque Lighthouse suppose par défaut qu'on veut être indexé — normal ici, pas un problème à corriger.
- **Métriques de performance déjà saines** : `Cumulative Layout Shift = 0`, `Total Blocking Time = 0 ms` sur toutes les pages auditées (`/login`, `/`, `/analytics`, `/bien-etre`) — pas de layout instable ni de janktes. FCP/LCP ~2.9s sous l'émulation mobile "slow 4G" de Lighthouse, attendu pour une SPA sans SSR, pas dégradé par les changements de cette phase.
- **Effet de bord découvert et nettoyé en cours de route, pas un bug de cette phase** : la suite e2e complète a régressé deux fois avec des échecs génériques sur "Créer le programme" à travers 8 specs différentes — tracé jusqu'à un cache d'optimisation Vite corrompu par des `rm -rf node_modules/.vite` répétés **pendant que le serveur de dev tournait encore** (mes propres scripts de diagnostic pendant cet audit), pas un vrai bug produit ; résolu par un redémarrage propre. Séparément, le compte e2e-fixture avait accumulé 121 programmes de test orphelins (mêmes scripts de diagnostic, plus des runs de test interrompus) qui faisaient dépasser le timeout du test d'import — même classe de problème que celui déjà documenté en P6a, nettoyé de la même façon (`delete` scopé par `user_id`, compte remis à zéro).
- **Vérifié en direct** : audits Lighthouse (performance/accessibility/best-practices/seo) sur `/login`, `/` (Dashboard), `/analytics`, `/bien-etre` via le build de production servi par `vite preview`, avec un vrai Chrome piloté par Playwright (authentifié via le compte e2e-fixture pour les pages protégées, un navigateur séparé pour `/login`). Vérification fonctionnelle du hors-ligne (précédemment cassé, maintenant réparé) confirmée sur `/`, `/login`, `/bien-etre`. Suite complète rejouée à plusieurs reprises pendant le debug (build + dev), verte à 26/26 au final, `tsc --noEmit`/ESLint/78 tests unitaires également au vert.
- **Non fait, backlog explicite** : vendor-splitting manuel du bundle partagé (`card-*.js`, ~500 Ko), icône maskable pour Android (nécessite un artwork dédié), et un audit Lighthouse Performance plus poussé en conditions réseau réelles (l'émulation "slow 4G" par défaut n'est qu'une approximation).

## P9a — Couche sociale, fondations : opt-in, 1RM auto-détecté, feed (2026-07-23)

Dernier point du brief avant la couche IA (toujours différée). Décisions confirmées avant implémentation : cercle fermé opt-in (pas de système d'ami/abonnement — tout utilisateur inscrit voit le feed de qui a activé le partage), photos + jalons élargis dès la cible finale mais **découpé en sous-phases** (P9a = fondations + 1RM seul + feed texte ; P9b = tonnage hebdo + streak ; P9c = photos) pour rester dans la taille de phase habituelle du projet.

- **Première brèche RLS du projet, cadrée avec soin** : `profiles.social_sharing_enabled` (désactivé par défaut) contrôle la visibilité. `milestones` (nouvelle table) : select `auth.uid() = user_id or exists(... social_sharing_enabled)`, **aucune policy insert/update** — un jalon n'est jamais écrit par le client, uniquement par un trigger `security definer` sur `session_log_sets`. Une policy **delete** a été ajoutée (voir plus bas, pas dans le jet initial) pour que chacun puisse retirer son propre jalon du feed.
- **`profiles` a des colonnes sensibles** (`date_of_birth`, `cycle_module_enabled`, `target_weight_kg`...) qu'une policy RLS "opted-in" directe sur la table exposerait en entier (RLS filtre des lignes, pas des colonnes). Solution : vue `public.public_profiles` (`id`, `display_name` seulement), créée **sans** `security_invoker` — elle tourne donc avec les droits du propriétaire (comportement par défaut, peut lire toute la table) mais ne projette que ce qui est sûr. La policy existante sur `profiles` (`auth.uid() = id`) reste intacte pour toute requête directe sur la table.
- **Bug RLS réel trouvé en vérifiant avec un vrai compte, pas en le lisant** : la policy `milestones` initiale faisait `exists (select 1 from public.profiles p where p.id = ... and p.social_sharing_enabled)` — mais `profiles` est elle-même restreinte à `auth.uid() = id`. Depuis la session d'un AUTRE utilisateur, cette sous-requête ne pouvait donc jamais voir la ligne qu'elle cherchait à vérifier, rendant la branche "opted-in" **toujours fausse**, peu importe le flag réel. Repéré uniquement parce que la vérification a été faite via un vrai compte jetable + de vraies requêtes REST authentifiées (pas du SQL superuser, qui contourne RLS et aurait masqué le bug) : `milestones` renvoyait `[]` pour un jalon pourtant bien opted-in. Corrigé (migration `20260723110000`) en faisant pointer la sous-requête vers `public_profiles` (qui contourne RLS par construction) plutôt que `profiles` directement. Reproductible : n'importe quelle policy RLS qui référence une AUTRE table protégée par RLS est elle-même sujette à cette récursion — à vérifier systématiquement pour toute future policy inter-tables.
- **Détection de PR, formule identique au client** : `estimate_one_rep_max(weight, reps)` en SQL (`immutable`) reproduit exactement `estimateOneRepMax` (`src/lib/one-rep-max.ts` — moyenne Epley/Brzycki, 1-12 reps, poids > 0). Trigger `after insert on session_log_sets` : calcule le 1RM du nouveau set, le compare au max historique de cet utilisateur sur cet exercice (hors la ligne courante), insère un jalon si nouveau record. `exercise_name` est un **instantané** copié depuis `exercises.name` au moment de la détection (pas juste `exercise_id`) — un exercice custom d'un utilisateur reste lisible dans le feed d'un autre utilisateur qui n'a pas accès à ce catalogue custom (RLS d'`exercises` scope les exercices non-système par propriétaire), en évitant complètement le problème plutôt qu'en le contournant.
- **Policy delete ajoutée après coup, pas dans le jet initial** (migration `20260723100000`) : découverte en écrivant le test e2e que les jalons créés pendant les tests s'accumulaient indéfiniment sur le compte fixture (rien ne les supprime — contrairement aux programmes, ils ne sont pas rattachés par cascade à `session_logs`, volontairement, pour rester une "histoire" indépendante). Plutôt que de bricoler un nettoyage hors-bande, ajouté une vraie fonctionnalité produit raisonnable : chacun peut supprimer son propre jalon du feed (bouton dédié sur `FeedPage`, `ConfirmDialog`, visible seulement sur ses propres entrées).
- **Effet de bord accepté, documenté** : les specs e2e pré-existantes qui loguent des séries sur "Squat" (`session-log.spec.ts`, `session-structure.spec.ts`, `data-import.spec.ts`) déclenchent maintenant elles aussi le trigger et laissent des jalons sur le compte fixture — croissance bornée (une ligne par valeur de 1RM réellement atteinte une première fois, jamais de doublon pour une valeur identique) et sans impact sur la justesse du feed, mais visible comme "bruit" si quelqu'un consulte ce compte. Pas retouché ces specs (hors scope de cette phase) ; nettoyage manuel ponctuel si besoin.
- **UI** : toggle "Activer/Désactiver" dans `ProfilePage` (`SocialSharingSection`, même pattern bouton-bascule que `CycleModuleSection`). Icône `Trophy` dans `AppLayout`, **toujours visible** (contrairement à l'icône `Cycle` qui est masquée tant que non activée) — voir son propre feed ou celui des autres ne nécessite pas d'avoir soi-même activé le partage, seule la nuance "partager MES progrès" est opt-in. Page `/feed` : liste chronologique, nom d'utilisateur, type de jalon + exercice + valeur + date.
- **Piège de test trouvé et corrigé, pas un bug applicatif** : le premier jet du test e2e cliquait sur `getByRole('button', {name: 'Activer'})` sans le scoper à sa carte — `ProfilePage` a maintenant deux sections avec des boutons "Activer"/"Désactiver" au texte identique (Module cycles + Partage de progrès), rendant le sélecteur ambigu. A cassé un test **déjà existant** (`cycle.spec.ts`) en plus du nouveau. Corrigé en scopant les deux tests à leur carte respective (`[data-slot="card"]` filtré par le titre), plutôt que de changer le texte des boutons (qui aurait juste déplacé le problème d'accessibilité — deux boutons "Activer" identiques sur une page n'est pas idéal, mais un `aria-label` distinct aurait aussi cassé les sélecteurs texte existants ; laissé en l'état, backlog si ça devient un vrai souci a11y).
- **Vérifié en direct, avec un vrai second compte, pas seulement en lisant le code** : compte jetable créé via l'API Admin (même technique que P5a), profil marqué `social_sharing_enabled = true` avec un jalon Deadlift inséré manuellement. Depuis une session **authentifiée réelle** du compte fixture (token REST, pas SQL superuser) : le jalon du compte jetable est visible tant qu'opted-in, disparaît dès que désactivé (`social_sharing_enabled = false`), `public_profiles` suit la même règle, la table `profiles` complète reste inaccessible en lecture directe pour un autre utilisateur, et un insert direct sur `milestones` depuis le client est bien rejeté (403, RLS). Compte jetable supprimé après coup.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 78 tests unitaires. Suite e2e complète (28 tests, incluant les 2 nouveaux tests `social.spec.ts` et les 2 corrigés dans `cycle.spec.ts`) rejouée à plusieurs reprises pendant le debug, verte au final. Fixture account nettoyé (jalons de test supprimés) après chaque run de vérification.

## P9b — Couche sociale : tonnage hebdo + streak de régularité (2026-07-23)

Deux jalons supplémentaires annoncés dans le scope initial de la couche sociale (P9a), livrés séparément comme prévu pour rester dans la taille de phase habituelle.

- **Bucketing de semaine identique au client** : `public.iso_week_start(timestamptz)` (SQL, `immutable`) reproduit exactement `getIsoWeekStart` (`src/lib/analytics.ts`) — `date_trunc('week', ...)` de Postgres suit déjà la définition ISO 8601 (lundi), il suffisait de normaliser la date en UTC avant troncature pour coïncider avec le calcul client (`.slice(0,10)` sur un timestamp déjà UTC). Un seul point de vérité pour "quelle semaine" plutôt que de dupliquer le calcul dans chaque trigger.
- **`week_start date`** ajouté à `milestones` (nullable — `one_rep_max` n'en a pas besoin), avec deux **index uniques partiels** (`where milestone_type = 'weekly_tonnage'` / `'regularity_streak'`) plutôt qu'une contrainte globale, pour ne jamais limiter `one_rep_max` à une ligne par semaine.
- **Tonnage hebdo — la valeur doit pouvoir grandir, pas se figer** : contrairement à `one_rep_max` (une ligne par record, jamais modifiée), le tonnage de la semaine en cours continue d'augmenter à mesure que d'autres séries sont loguées. Figer la valeur au moment où le seuil est franchi montrerait un total sous-évalué en fin de semaine. Le trigger utilise donc `insert ... on conflict (user_id, week_start) where milestone_type = 'weekly_tonnage' do update set value = excluded.value where milestones.value < excluded.value` — une seule ligne par semaine-record, toujours à jour avec le total le plus élevé réellement atteint. Chaque nouvelle semaine qui bat le record all-time précédent obtient sa **propre** ligne (l'historique s'accumule, comme pour `one_rep_max` — pas un seul "record actuel" qu'on écrase).
- **Streak de régularité — "gaps and islands" classique en SQL** : semaines actives (au moins une série loguée) triées par ordre décroissant, `semaine + (rang × 7 jours)` reste constant sur une série de semaines consécutives — grouper là-dessus isole chaque "île" d'activité sans boucle procédurale. Le streak courant est la longueur du groupe le plus récent ; comparé au meilleur des **autres** groupes (`filter (where recency_rank > 1)`) pour savoir si c'est un nouveau record. **Seuil à 2 semaines minimum** avant qu'un streak soit annoncé — sans ce garde-fou, la toute première semaine d'usage de n'importe quel utilisateur génère un "record : 1 semaine" immédiatement, du bruit pur.
- **Vérifié en direct avec des données antidatées, pas seulement en lisant le SQL** : programme/exercice/séries de test insérés directement via `supabase db query` avec des `started_at` répartis sur 4-6 semaines ISO réelles (2026-06-29 à 2026-08-03), scénario par scénario — semaines consécutives (streak progresse 1→2→3, sous le seuil de 2 non annoncé puis annoncé), tonnage qui grandit au sein d'une même semaine-record (vérifié que la ligne se **met à jour** plutôt que de dupliquer, testé explicitement avec 3 séries successives dans la même semaine), semaine plus légère qui ne bat pas le record (aucune ligne créée), **trou dans le streak** (saut d'une semaine → streak retombe à 1, non annoncé, l'ancien record de 3 reste intact), nouveau streak après le trou qui n'atteint pas l'ancien record (2 < 3, non annoncé). Les tests par simple usage réel de l'app (via le navigateur) ne pouvaient pas exercer ces scénarios multi-semaines sans attendre plusieurs semaines en temps réel — vérification directe en base la seule option praticable, avec le même niveau de rigueur que pour la formule 1RM en P9a.
- **Refactor de passage** : `MILESTONE_TYPE_LABELS`/`formatMilestoneValue` extraits de `social-api.ts` (qui importe `supabase`, donc pas testable unitairement sans mock) vers un nouveau `src/lib/social-display.ts` pur — même séparation déjà établie pour `cycle-phase.ts`/`cycle-api.ts` en P8. `formatMilestoneValue` gère l'affichage par type (kg avec décimales pour `one_rep_max`, kg arrondi pour `weekly_tonnage`, "X semaine(s) d'affilée" avec pluriel correct pour `regularity_streak`) — testé unitairement (`social-display.test.ts`, 4 tests).
- **Test e2e étendu, pas dupliqué** : le test P9a existant (999kg x 5) sert aussi de test tonnage — 4995kg de tonnage en une seule série dépasse largement tout ce que les autres specs de la suite peuvent produire cette semaine-là, donc fiable comme "nouveau record" sans configuration supplémentaire. Pas de test e2e pour le streak (nécessiterait des données antidatées, hors de portée d'un test piloté par navigateur qui logue toujours au temps réel) — couvert par la vérification directe en base ci-dessus.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 81 tests unitaires (3 nouveaux). Suite e2e complète (28 tests) rejouée après implémentation, verte. Fixture account nettoyé après chaque run.

## P9c — Couche sociale : photos de progression (2026-07-23)

Dernier morceau du scope initial de la couche sociale. Premier bucket Supabase Storage du projet.

- **Bucket privé, jamais d'URL publique** : `progress-photos` (`public = false`, 5 Mo max, JPEG/PNG/WebP). Convention de chemin `<user_id>/<uuid>.jpg` — le premier segment sert de propriétaire pour les policies RLS Storage (`storage.foldername(name)`). Même règle de visibilité que `milestones`/`public_profiles` (soi-même, ou un utilisateur avec `social_sharing_enabled`), appliquée à la fois sur `storage.objects` (accès au fichier) et sur `progress_photos` (la ligne en base) — les deux doivent être cohérentes puisque ce sont deux systèmes RLS indépendants.
- **Accès en lecture via URL signée**, pas `getPublicUrl()` (qui ne vérifie aucune permission — inutilisable sur un bucket privé) ni téléchargement de blob manuel. `createSignedUrl()` respecte RLS à la génération : demander une URL signée pour un chemin auquel on n'a pas droit échoue, avant même d'atteindre le fichier. Expiration à 1h, régénérée à chaque chargement du feed — largement suffisant pour une consultation, pas besoin de cache.
- **Compression côté client, aucune nouvelle dépendance** : `compressImage()` (`src/lib/image-compression.ts`) utilise `createImageBitmap` + `<canvas>` natifs du navigateur — redimensionne à 1600px de long côté max, réexporte en JPEG qualité 0.82. Une vraie photo de téléphone (3-8 Mo) devient une fraction de ça avant l'upload, pertinent pour un usage mobile-first sur un quota Storage limité (palier gratuit).
- **Bug trouvé en vérifiant avec un vrai compte jetable, pas en lisant le code — lacune réelle dans `account-delete` (P5a)** : `admin.auth.admin.deleteUser()` cascade bien la ligne `progress_photos` (vraie FK vers `auth.users`), mais **pas** le fichier Storage sous-jacent — confirmé par test direct (upload + suppression du compte + le fichier restait dans le bucket). Même famille de piège que `ai_provider_keys`/Vault trouvé en P5a (une ressource référencée depuis une table qui cascade, mais dont la cascade ne suit pas jusqu'à la ressource elle-même). Corrigé : `account-delete` liste et supprime maintenant les objets Storage du user avant `deleteUser()`. Redéployé et re-vérifié avec un nouveau compte jetable : `auth.users`, `progress_photos` et l'objet Storage sont tous à zéro après suppression.
- **Modèle de feed unifié** : `FeedEntry = MilestoneFeedEntry | PhotoFeedEntry` (`src/lib/social-display.ts`), fusionnées et triées par `occurredAt` via `mergeFeedEntries()` (pure, testée). `fetchFeed()` (`social-api.ts`) récupère milestones + `progress_photos` en parallèle, mutualise un seul lookup `public_profiles` pour les deux, génère les URLs signées en parallèle pour les photos.
- **Suppression d'une photo** : supprime d'abord la ligne `progress_photos` (soumis à RLS, peut échouer proprement), puis le fichier Storage en best-effort — dans cet ordre plutôt que l'inverse, pour ne jamais laisser une ligne pointant vers un fichier qui n'existe plus (ce qui casserait l'affichage), au pire un fichier orphelin nettoyable plus tard.
- **Vérifié en direct avec un vrai second compte** (même méthode qu'en P9a) : photo uploadée par un compte jetable non opted-in → invisible pour le compte fixture à la fois sur `progress_photos` (lecture table) et sur l'objet Storage lui-même (404, pas juste refusé côté table) ; devient visible dès `social_sharing_enabled = true`, sur les deux fronts ; insert direct d'une ligne `progress_photos` pour le user_id de quelqu'un d'autre → rejeté (RLS) ; delete direct de la ligne de quelqu'un d'autre → 0 ligne affectée.
- Test e2e (`e2e/social.spec.ts`) : upload d'une vraie image de test (`e2e/fixtures/test-photo.png`, PNG 4×4 généré une fois, committé) via `setInputFiles` sur l'input caché (même pattern que l'import JSON), légende, aperçu, publication, apparition dans le feed avec l'`<img>` rendu, suppression. `import.meta.dirname` pour le chemin du fixture (pas `__dirname`, ESM).
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 83 tests unitaires (2 nouveaux sur `mergeFeedEntries`). Suite e2e complète (29 tests) verte. Vérification visuelle à 375px (mobile-first) de la page `/feed` vide et du formulaire d'upload avec aperçu — layout propre, pas de débordement.
- **Non fait, backlog explicite** : pas de limite de nombre de photos par utilisateur (quota Storage à surveiller si l'usage grandit au-delà de quelques amis/collègues), pas d'édition de légende après publication (suppression + réupload seulement, cohérent avec le reste du feed).

## P4b — Couche IA : analyse de tendance (2026-07-23)

Première vraie feature de la couche IA différée depuis P4a — jusqu'ici, les clés BYOK n'avaient servi qu'à une validation (`GET /v1/models`), jamais un appel de complétion réel. Décisions confirmées avant implémentation : analyse de tendance globale (pas de recommandation de deload par exercice — ça reste pour une phase ultérieure), déclenchement à la demande uniquement (pas de job planifié qui dépenserait le quota API de l'utilisateur sans action explicite), carte sur le Dashboard.

**Mise à jour (même jour, après test en direct avec une vraie clé)** : modèle Anthropic changé de `claude-opus-4-8` vers `claude-sonnet-5` — décision explicite du user après avoir testé l'analyse fonctionnelle une première fois ("Opus 4.8 est trop abusé pour ça"). Pas de niveau "high" façon Gemini côté Anthropic — `thinking: { type: 'adaptive' }` (déjà en place) est l'équivalent le plus proche, Claude décide lui-même du budget de raisonnement. Re-vérifié via la même technique qu'en P4a/P4b (fausse clé insérée directement dans Vault, hors flux `ai-key-save`) : erreur d'authentification propre (401) reçue d'Anthropic pour `claude-sonnet-5`, confirmant une requête bien formée sans dépenser le crédit réel de l'utilisateur.

- **Le résumé est calculé côté client, pas dans l'Edge Function** : `buildTrendSummary()` (`src/lib/analytics.ts`) réutilise telles quelles les fonctions pures déjà utilisées par la page Analytics (`computeWeeklyTonnage`, `countTrainingDaysThisWeek`, `computeOneRepMaxProgression`, `getLoggedExercises`) — l'IA raisonne sur exactement ce que l'utilisateur peut déjà voir à l'écran, pas une requête serveur parallèle qui risquerait de diverger. La fonction Edge Function (`ai-analyze-trends`) ne fait que relayer ce résumé déjà condensé au provider — cohérent avec le reste du projet (logique métier en TypeScript, Postgres/Edge Functions pour l'état et la sécurité uniquement).
- **Bug trouvé par le test unitaire, pas en relisant le code** : la première version de `buildTrendSummary` calculait la fenêtre "8 dernières semaines" en tranchant le tableau (sparse) retourné par `computeWeeklyTonnage` plutôt que par une vraie date de coupure calendaire — comme ce tableau ne contient que les semaines *avec* des séries loguées, un trou d'entraînement de plusieurs mois pouvait faire remonter la fenêtre bien plus loin que 8 semaines réelles (une semaine vide ne "compte" pas dans un slice(-8) sur un tableau sparse). Un test dédié à ce scénario (`excludes sets older than the 8-week tonnage window`) l'a immédiatement fait échouer. Corrigé en calculant une vraie date de coupure (`getIsoWeekStart(now) - 7×7 jours`) et en filtrant les séries par date réelle avant tout calcul, plutôt que de faire confiance à la forme du tableau retourné.
- **Deuxième bug, trouvé de la même façon** : la sélection des exercices les plus fréquents utilisait `frequencyByExercise` pour choisir *lesquels* garder, mais reconstruisait ensuite la liste finale via `getLoggedExercises()` (tri alphabétique) filtrée sur cette sélection — le tri alphabétique écrasait silencieusement l'ordre par fréquence annoncé. Le test `picks the most frequently logged exercises, most-frequent first` l'a révélé (attendu `['Squat', 'Bench Press']`, reçu l'ordre alphabétique). Corrigé en construisant la liste finale directement depuis `topExerciseIds` (déjà trié par fréquence), avec juste un lookup de nom depuis `getLoggedExercises`.
- **Modèles et provider — pas de skill dédié à OpenAI dans ce projet**, contrairement à Anthropic (`claude-api` skill déjà chargé cette session) : `gpt-5.6` confirmé comme modèle flagship actuel via une recherche web + une lecture directe de la doc OpenAI (`developers.openai.com/api/docs/guides/completions`) plutôt que deviné depuis les connaissances d'entraînement, qui peuvent être dépassées sur un catalogue de modèles qui change vite. Côté Anthropic, `claude-opus-4-8` (le défaut non-négociable du skill — "never downgrade for cost, that's the user's decision") avec `thinking: { type: 'adaptive' }`, cohérent avec la consigne du skill de l'activer "pour tout ce qui est un peu complexe" — une synthèse multi-signaux (tonnage, régularité, 1RM par exercice) qualifie.
- **Impossible de tester avec une vraie clé** (BYOK — seul l'utilisateur en a une), donc vérifié ce qui est vérifiable sans : auth manquante (401), provider invalide (400), résumé manquant (400), aucune clé enregistrée pour le provider demandé (404, chemin déjà éprouvé en P4a). Puis, pour aller plus loin qu'en P4a : une **fausse clé** insérée directement dans Vault (contournant volontairement `ai-key-save`, qui l'aurait rejetée) pour chacun des deux providers, afin de vérifier que l'appel HTTP réel atteint bien Anthropic/OpenAI avec une requête bien formée — les deux ont répondu avec de vraies erreurs d'authentification (401, message du provider), pas une erreur de requête malformée (400), ce qui confirme que la forme de la requête (modèle, en-têtes, corps) est correcte jusqu'au point d'authentification. Le chemin "clé valide → analyse réussie" reste à vérifier par le user avec sa propre clé, même limitation déjà documentée et acceptée pour P4a.
- **UI** : nouvelle carte "Analyse IA" sur le Dashboard (`HomePage.tsx`), après la carte Poids. Trois états : aucune clé configurée (invite vers le Profil), historique vide (bouton désactivé, message), prêt (bouton "Analyser mes progrès", sélecteur de provider affiché seulement si les deux sont configurés et valides). Résultat affiché tel quel sous forme de texte (`whitespace-pre-line`), pas de cache ni de requête auto-relancée — chaque clic est un nouvel appel payant pour l'utilisateur, donc jamais déclenché sans action explicite (cohérent avec la décision "à la demande" du cadrage).
- Test e2e (`e2e/dashboard.spec.ts`) : état "aucune clé configurée" (le seul rejouable sans clé réelle), lien vers le Profil vérifié. Pas de test pour le chemin "analyse réussie" — même raisonnement que le chemin "clé valide" jamais testé en e2e depuis P4a.
- **Vérifié en direct côté app** : `tsc --noEmit`, ESLint, build complet, 87 tests unitaires (4 nouveaux sur `buildTrendSummary`, 2 bugs réels trouvés et corrigés par ces tests). Suite e2e complète (30 tests) verte. Vérification visuelle à 375px de la nouvelle carte sur le Dashboard.
- **Non fait, backlog explicite** : recommandation de deload par exercice et suggestions d'ajustement du planning bien-être (les deux autres volets du brief pour la couche IA, différés à une phase ultérieure), streaming de la réponse (skill par défaut, sciemment omis — `max_tokens` borné à 1536/1024 rend le risque de timeout faible, complexité non justifiée pour ce premier passage), assistant conversationnel (déjà noté en backlog comme la suite naturelle une fois cette fondation validée à l'usage).

## Couche IA — étape 1 : persona coach renforcé (2026-07-23)

Après le premier test réel de P4b, le user a posé une feuille de route en 5 étapes pour transformer la couche IA en "coach privé" (génération de programme, adaptation de séance, intégration cycle menstruel, puis assistant conversationnel) — cette entrée couvre uniquement la première étape, à faire une par une avec un go explicite entre chacune.

- **Persona extrait dans un fichier partagé dédié** (`supabase/functions/_shared/coach-persona.ts`), pas laissé inline dans `ai-analysis.ts` : les étapes 2-5 de la feuille de route (génération de programme, adaptation de séance, assistant conversationnel) auront chacune leur propre Edge Function mais doivent partager le même persona/ton/garde-fous — éviter la duplication maintenant plutôt que de la découvrir à l'étape 2.
- **`COACH_PERSONA`** (réutilisable) pose l'identité (coach de force expérimenté, pas un assistant généraliste) et des principes de raisonnement explicites plutôt que des platitudes : surcharge progressive, autorégulation, définition concrète d'un plateau (1RM stable/en baisse sur plusieurs points malgré un volume stable/en hausse), définition d'un signal de deload (régularité en baisse + stagnation), et la règle "toujours comparer au rythme récent de cet utilisateur, jamais à une norme universelle". Garde-fous stricts : aucun avis médical, aucun diagnostic, aucune prescription nutritionnelle chiffrée, dire explicitement quand les données sont insuffisantes plutôt qu'inventer — et tenir ces limites même si on les contourne directement (utile dès qu'un vrai assistant conversationnel existera à l'étape 5, pas seulement pour l'analyse actuelle).
- **`TREND_ANALYSIS_SYSTEM_PROMPT`** = `COACH_PERSONA` + la tâche spécifique à cette feature (format de réponse, ce qu'il faut signaler). Remplace l'ancien prompt inline de P4b (5 lignes génériques) sans changer le contrat de sortie (toujours 3-5 phrases, toujours terminé par une suggestion concrète) — uniquement le raisonnement qui produit cette sortie est plus ancré.
- **Vérifié en direct** : redéployé, revérifié avec la même technique qu'en P4a/P4b/le changement de modèle (fausse clé Vault) — erreur d'authentification propre reçue d'Anthropic pour le nouveau prompt (nettement plus long), confirmant une requête toujours bien formée. Aucun changement côté app (client/tests) — uniquement des fichiers Edge Function, hors du projet TypeScript de l'app.

## Couche IA — profil utilisateur injecté dans l'analyse (2026-07-23)

Demande explicite du user avant l'étape 2 : l'IA ne voyait jusqu'ici que l'historique d'entraînement, jamais le profil (taille, poids, objectif...). Corrigé pour l'analyse de tendance ; le même mécanisme sera réutilisé par les étapes 2+ (génération de programme, adaptation de séance), pas reconstruit à chaque fois.

- **`src/lib/user-context.ts`** (nouveau, pur, testé) : `buildUserProfileContext(profile, weightEntries)` combine sexe, taille, objectif, poids cible et poids actuel (dernière pesée) — et calcule l'âge à partir de `date_of_birth` plutôt que d'envoyer la date de naissance brute au provider (minimisation : seule la donnée dérivée utile est transmise). `computeAge()` gère correctement l'anniversaire pas encore passé cette année (pas une simple soustraction d'année), testé explicitement (4 tests).
- **Le persona partagé (`coach-persona.ts`) sait maintenant qu'il reçoit toujours un profil**, pas seulement `TREND_ANALYSIS_SYSTEM_PROMPT` — cohérent avec le fait que les étapes 2+ auront aussi besoin de cette info. Instruction explicite de calibrer l'analyse sur l'objectif (`goal`) plutôt qu'une norme générique, avec un exemple concret dans le prompt (une baisse de tonnage n'est pas un problème si l'objectif est `perte_de_poids`).
- **Edge Function** : `profileContext` désormais requis dans le corps de la requête (400 si absent) — mais un profil "vide" (tous les champs à `null`, utilisateur qui n'a rien renseigné) reste valide, la validation ne porte que sur la présence de la clé, pas sur son contenu.
- **Vérifié en direct** : `tsc --noEmit`, ESLint, build complet, 91 tests unitaires (4 nouveaux sur `computeAge`/`buildUserProfileContext`). Edge Function redéployée et revérifiée avec la fausse clé habituelle (400 si `profileContext` manquant, 401 propre d'Anthropic si présent avec une fausse clé — requête bien formée confirmée).
- **Suite e2e complète non rejouée en entier cette fois** : deux tentatives ont pris 1,4h puis 1,8h avec des échecs dispersés sur des specs sans rapport avec ce changement (breath, wellness, programs, session-log...) — diagnostiqué comme de la pression mémoire système (35 Go utilisés, quasiment plus de mémoire libre, `top` confirme une compression mémoire active), probablement liée à un grand nombre d'onglets Chrome du user accumulés sur plusieurs jours, pas un vrai bug. `e2e/dashboard.spec.ts` (le seul test touchant directement ce changement) rejoué isolément et vert en 17s une fois la charge retombée — cohérent avec la vérification statique complète déjà au vert. À rejouer en entier la prochaine fois que la machine a de la mémoire disponible.

## Couche IA — étape 2 : génération de programme par l'IA (2026-07-23)

Deuxième étape de la feuille de route "coach privé" du user. Cadrage confirmé avant implémentation (AskUserQuestion) : paramètres précisés par l'utilisateur avant génération (jours/semaine, équipement, contraintes — pas de déduction 100% automatique), et l'IA ne peut proposer que des exercices déjà pratiqués par l'utilisateur (pas tout le catalogue système). Toujours "propose puis révise" : l'IA ne crée jamais de programme silencieusement, l'utilisateur voit un aperçu en lecture seule et confirme avant toute écriture réelle.

- **`supabase/functions/_shared/ai-program-generation.ts`** (nouveau) : logique partagée d'appel provider pour la génération structurée, sur le modèle de `ai-analysis.ts`. Un même schéma JSON (`PROGRAM_PROPOSAL_SCHEMA`, avec `additionalProperties: false` et toutes les propriétés listées dans `required` — nécessaire pour le mode strict d'OpenAI, inoffensif pour Anthropic) sert aux deux chemins :
  - **Anthropic** : tool-use forcé (`tool_choice: {type:'tool', name:'propose_program'}`). **`thinking` volontairement omis** pour cet appel précis — la réflexion étendue d'Anthropic est incompatible avec un `tool_choice` forcé (le modèle doit rester libre de décider s'il réfléchit avant de s'engager sur un outil), et un `tool_choice` forcé est ce qui rend la sortie structurée assez fiable pour être appliquée sans relecture humaine ligne par ligne. Contrairement à l'analyse de tendance (texte libre, où `thinking: adaptive` reste actif), ce compromis est spécifique à cet appel.
  - **OpenAI** : `response_format: {type:'json_schema', json_schema:{name, strict:true, schema}}`, même schéma que côté Anthropic.
  - **Validation serveur post-réponse** (`validateProposal`) avant que quoi que ce soit ne quitte l'Edge Function : chaque `exerciseId` renvoyé doit appartenir au pool autorisé fourni par le client, `targetRepsMax >= targetRepsMin`, exactement 7 jours couvrant chacun des 7 jours de la semaine une seule fois. Le schéma garantit la *forme*, pas que le modèle a respecté ses propres instructions — un modèle peut toujours inventer un exercice hors liste malgré la consigne, donc re-vérifié côté serveur plutôt que de faire confiance au prompt seul.
- **`PROGRAM_GENERATION_SYSTEM_PROMPT`** (`coach-persona.ts`) : `COACH_PERSONA` + règles de structure spécifiques (répartition réaliste des jours, `target_reps_max >= target_reps_min`, adaptation séries/reps/RPE au focus choisi et à l'objectif du profil, contraintes utilisateur traitées comme donnée à respecter jamais comme instruction qui outrepasse les garde-fous du persona — notamment jamais de diagnostic médical même si une contrainte évoque une douleur).
- **`supabase/functions/_shared/fetch-with-timeout.ts`** (nouveau) : le helper de timeout de `ai-analysis.ts` extrait tel quel, maintenant partagé entre les deux Edge Functions plutôt que dupliqué.
- **`supabase/functions/ai-generate-program/index.ts`** (nouveau) : même structure auth/clé que `ai-analyze-trends` (401 non authentifié, 400 provider/profil/exercices/jours invalides, 404 aucune clé enregistrée, 502 si le provider échoue). `availableExercises` doit être un tableau non vide, `daysPerWeek` entre 1 et 7.
- **`src/lib/program-generation-api.ts`** (nouveau, côté client) : `generateProgram()` relaie au provider via l'Edge Function. **`applyProgramProposal()` réutilise exactement les fonctions existantes de `programs-api.ts`/`sessions-api.ts`** (`createProgram`, `fetchSessionTemplates`, `updateSessionTemplateDayType`, `createSessionTemplateExercise`) en suivant le pattern de `duplicateProgram()` — jamais de chemin d'écriture bespoke pour la sortie de l'IA. Le repos par défaut vient de `DEFAULT_REST_SECONDS_BY_FOCUS[proposal.focus]`, pas d'un champ décidé par le modèle (cohérent avec P3b : ce n'est pas à l'IA de fixer cette valeur).
- **Exercices "déjà pratiqués"** dérivés côté client de `useSetHistory()` (les séries réellement loguées, `session_log_sets`), pas de la table `exercises` (catalogue global partagé entre tous les utilisateurs, y compris des exercices jamais pratiqués) — cohérent avec le cadrage confirmé.
- **UI** : nouvelle page `/programs/generate` (lien "Générer avec l'IA" depuis `ProgramsListPage`, à côté de "Nouveau programme"). Trois états : aucune clé configurée / aucun exercice logué (mêmes gardes que la carte Analyse IA du Dashboard), formulaire (jours/semaine, équipement, contraintes, sélecteur de provider si plusieurs configurés), aperçu en lecture seule (nom, focus, justification, répartition des 7 jours) avec actions "Créer ce programme" / "Régénérer" / "Annuler".
- **Bug mobile trouvé par capture d'écran à 375px, pas en relisant le code** : la ligne de boutons "Nouveau programme" + le nouveau "Générer avec l'IA" débordait du viewport sur `ProgramsListPage` (texte tronqué), et la ligne "Créer ce programme" / "Régénérer" / "Annuler" de l'aperçu faisait pareil. Corrigé par un header qui empile titre/actions en dessous de `sm:` (`flex-col sm:flex-row`) plutôt qu'en largeur fixe, et `flex-wrap` sur la ligne d'actions de l'aperçu.
- **Vérifié en direct sans dépenser de crédit réel** : requête bien formée confirmée pour les deux providers via la technique habituelle (fausse clé insérée directement dans Vault pour le compte fixture e2e — jamais le compte réel de l'utilisateur — puis nettoyée) : 401 propre reçu d'Anthropic et d'OpenAI, pas de 400. L'aperçu en lecture seule (UI la plus nouvelle, jamais testée par la technique fausse-clé puisqu'elle ne dépend pas du provider) vérifié par une réponse réseau mockée (`page.route()` sur l'appel Edge Function) plutôt qu'un vrai appel — capture d'écran à 375px après correction des deux débordements ci-dessus.
- `tsc -b`, ESLint et 91 tests unitaires verts. Nouveau test e2e (`programs.spec.ts`) : état "aucune clé configurée" de `/programs/generate`, seul chemin rejouable sans clé réelle — même limitation déjà acceptée pour P4b.
- **Non fait, backlog explicite** : le chemin "génération réussie avec une vraie clé" reste à valider par le user lui-même (comme pour P4b), étapes 3-5 de la feuille de route (adaptation de séance, intégration cycle menstruel, assistant conversationnel).

## Couche IA — étape 3 : adaptation de séance par l'IA (2026-07-23)

Troisième étape de la feuille de route "coach privé". Cadrage confirmé avant implémentation (AskUserQuestion, deux tours) : l'IA peut adapter n'importe quelle séance du programme actif (pas seulement celle du jour), peut ajuster ET remplacer des exercices (pas juste les séries/reps/RPE), et l'adaptation confirmée écrit réellement dans `session_template_exercises` pour cette occurrence (même mécanisme que la génération de programme). UI en modale sur la carte du jour plutôt qu'une page dédiée (périmètre = un seul jour, contrairement à l'étape 2). Un champ de contexte libre optionnel ("Comment tu te sens aujourd'hui ?") complète les données historiques, pour que l'IA puisse réagir à un état ponctuel (fatigue, douleur, contrainte de temps) et pas seulement à la tendance passée.

- **`supabase/functions/_shared/ai-session-adaptation.ts`** (nouveau) : même architecture que `ai-program-generation.ts` (tool-use forcé sans `thinking` côté Anthropic, `response_format` JSON schema strict côté OpenAI, validation serveur post-réponse), mais le schéma de sortie n'a plus de notion de jours/semaine — juste `{ rationale, exercises: [...] }` (`minItems: 1`, jamais une séance vidée). **`EXERCISE_SCHEMA` exporté depuis `ai-program-generation.ts` et réutilisé tel quel** plutôt que redéfini — les deux fonctionnalités partagent exactement la même forme d'exercice (exerciseId/exerciseName/targetSets/targetRepsMin/targetRepsMax/targetRpe).
- **`SESSION_ADAPTATION_SYSTEM_PROMPT`** (`coach-persona.ts`) : `COACH_PERSONA` + règles spécifiques à l'adaptation — l'IA reçoit la composition actuelle de la séance et peut la garder telle quelle, l'ajuster, ou remplacer un exercice (pas d'obligation de tout réécrire), doit s'appuyer sur les signaux de régularité/tonnage pour de l'autorégulation classique, et le contexte du jour (s'il est fourni) prime sur la tendance générale pour cette séance précise — toujours sous les mêmes garde-fous (pas de diagnostic même si une douleur est mentionnée).
- **`supabase/functions/ai-adapt-session/index.ts`** (nouveau) : même structure auth/clé/erreurs que les deux Edge Functions précédentes. Valide en plus que `currentExercises` est un tableau non vide (une séance à adapter a forcément déjà au moins un exercice).
- **`src/lib/session-adaptation-api.ts`** (nouveau) : `generateSessionAdaptation()` relaie à l'Edge Function. **`applySessionAdaptation()` supprime tous les créneaux existants du jour puis recrée ceux proposés**, via les fonctions existantes (`deleteSessionTemplateExercise`/`createSessionTemplateExercise`) — pas de diff exercice-par-exercice, la proposition remplace la séance en bloc (cohérent avec le choix "modifie le template pour cette occurrence"). Repos par défaut toujours depuis `DEFAULT_REST_SECONDS_BY_FOCUS`, jamais décidé par le modèle.
- **`src/hooks/useSessionTemplates.ts`** : `exercisesKey` exportée (était locale au fichier) pour que le nouveau hook `useApplySessionAdaptation` puisse invalider exactement la même query key que les hooks existants, sans la redéfinir en double.
- **UI (`SessionAdaptationDialog.tsx`)** : bouton "Adapter avec l'IA" sur `SessionTemplateCard`, visible uniquement sur un jour d'entraînement avec au moins un exercice (à côté de "Démarrer la séance"). Modale avec les mêmes états que la génération de programme (aucune clé / aucun exercice logué / formulaire / aperçu), scope réduit à un seul jour. Exercices "déjà pratiqués" dérivés de `useSetHistory()`, même logique que l'étape 2, non extraite en fonction partagée pour l'instant (deux occurrences, pas encore trois — cohérent avec la discipline du projet de ne pas abstraire avant d'en avoir besoin).
- **Bug mobile pré-existant repéré, pas corrigé** : le header de `ProgramDetailPage` (bouton "Supprimer") déborde légèrement à 375px — visible pendant la vérification visuelle de cette étape mais aucun fichier de ce header n'a été touché ici ni à l'étape 2, donc pré-existant et hors périmètre. Noté en backlog plutôt que corrigé au passage (pas de refactor opportuniste pendant une phase).
- **Vérifié en direct** : `tsc -b`, ESLint, 91 tests unitaires (aucun nouveau — pas de nouvelle logique pure, tout est UI/appel provider comme l'étape 2). Requête bien formée confirmée pour les deux providers via la fausse clé Vault sur le compte fixture (401 propre, pas de 400). Aperçu en lecture seule vérifié par réponse réseau mockée + capture d'écran à 375px (formulaire et aperçu propres, pas de débordement). Nouveau test e2e (`session-structure.spec.ts`) : état "aucune clé configurée" du dialog. Suite e2e complète rejouée en entier après cette étape.
- **Non fait, backlog explicite** : étapes 4-5 de la feuille de route (intégration cycle menstruel, assistant conversationnel), diff visuel entre l'ancienne et la nouvelle composition d'une séance dans l'aperçu (actuellement juste la nouvelle liste, pas de comparaison ligne à ligne), le chemin "adaptation réussie avec une vraie clé" reste à valider par le user lui-même.

## Couche IA — étape 4 : contexte cycle menstruel plié dans les étapes 1-3 (2026-07-23)

Quatrième étape de la feuille de route. Cadrage confirmé (AskUserQuestion) : le contexte de phase reste **informatif, l'IA décide** de son utilisation plutôt qu'une règle systématique par phase (ex. pas de "toujours réduire la charge en phase menstruelle" imposé) — cohérent avec le garde-fou déjà en place ("comparer au rythme propre de l'utilisateur, jamais une norme universelle") et avec l'absence de consensus scientifique fort sur l'effet du cycle à l'échelle individuelle. Le contexte envoyé est minimal : nom de la phase + jour du cycle, pas le texte des conseils déjà écrits pour `/cycle` (`CYCLE_PHASE_ADVICE`) — évite de dupliquer/figer un texte qui pourrait diverger du persona avec le temps.

- **Pas de nouvelle Edge Function ni de changement de schéma** : les 3 fonctions existantes (`ai-analyze-trends`, `ai-generate-program`, `ai-adapt-session`) relaient déjà `profileContext` tel quel sans valider son contenu interne (juste sa présence) — le nouveau champ `cyclePhase` traverse automatiquement. Seuls `coach-persona.ts` (partagé, donc les 3 fonctions redéployées) et le client ont changé.
- **`src/lib/user-context.ts`** : `UserProfileContext.cyclePhase: { phase: CyclePhase; cycleDay: number } | null`, calculé par `buildUserProfileContext()` qui prend désormais aussi `cycleEntries` en paramètre. Réutilise `computeCyclePhase()` de P8 tel quel (aucune nouvelle logique de calcul) — seulement calculé si `profile.cycle_module_enabled` est vrai, jamais si le module est désactivé même avec des entrées passées (opt-in strict, cohérent avec le reste de l'app). `toLocalDateString()` dupliqué localement plutôt qu'extrait en utilitaire partagé, comme le fait déjà `todayLocalDate()` dans plusieurs pages (`CyclePage`/`HomePage`/`WellnessPage`/`FeedPage`) — pas de précédent d'abstraction partagée pour cette fonction triviale dans ce projet.
- **`useCycleEntries()`** accepte désormais `{ enabled? }` : les 3 sites d'appel (HomePage, ProgramGeneratePage, SessionAdaptationDialog) ne fetch les `cycle_entries` que si `profile.cycle_module_enabled` est vrai — même prudence sur les données sensibles que partout ailleurs où le module apparaît, pas seulement une porte d'affichage sur `/cycle`.
- **`COACH_PERSONA`** : nouveau paragraphe expliquant le champ `cyclePhase` optionnel — donnée de contexte à utiliser seulement si elle éclaire un signal déjà présent ailleurs dans les données (ex. une baisse de régularité qui coïncide avec cette phase), jamais une justification autonome ni une règle automatique par phase.
- **Vérifié en direct** : `tsc -b`, ESLint, 94 tests unitaires (3 nouveaux : phase incluse quand le module est actif avec des données, absente quand le module est désactivé même avec des données, absente quand actif sans données). Les 3 Edge Functions redéployées et une requête bien formée reconfirmée (401 propre) avec un `profileContext` contenant `cyclePhase` — persona plus long, toujours bien formé. Pas de nouvelle UI cette fois (contexte enrichi en coulisses, aucun nouveau composant visible), donc pas de capture d'écran ; suite e2e complète rejouée pour écarter toute régression de timing/re-render liée au hook `useCycleEntries` supplémentaire sur ces 3 écrans.
- **Non fait, backlog explicite** : étape 5 (assistant conversationnel, dernière de la feuille de route), pas de vérification en direct avec un compte ayant réellement le module cycle activé + une vraie clé (BYOK, comme pour toutes les étapes couche IA précédentes).

## Couche IA — étape 5 : assistant conversationnel (2026-07-23)

Cinquième et dernière étape de la feuille de route "coach privé" — un vrai chat plutôt qu'une analyse ou une génération ponctuelle. Contrairement aux étapes 2-4 (qui étendaient les Edge Functions existantes), celle-ci en introduit une nouvelle (`ai-coach-chat`) qui orchestre les 3 outils déjà construits via tool-calling standard (`tool_choice: "auto"`, contrairement à `ai-generate-program`/`ai-adapt-session` qui forcent leur outil) — l'assistant doit rester libre de simplement répondre en texte la plupart du temps.

- **Modèle plat, un seul fil par utilisateur** (`assistant_messages`, migration `20260723140000_assistant_messages.sql`) — pas de table `conversations` séparée, rien dans le produit n'exige encore du multi-thread. `tool_proposal` (jsonb) et `applied_at` permettent de ré-afficher une proposition sous forme de carte après reload sans double-application, même mécanisme que les cartes de proposition des étapes 2-3.
- **Trois outils exposés au modèle** : `analyser_tendance` (n'écrit rien, relit les données déjà fournies), `generer_programme` et `adapter_seance` (proposent seulement — l'écriture réelle reste un geste explicite de l'utilisateur sur la carte, jamais l'IA). `COACH_CHAT_SYSTEM_PROMPT` (`coach-persona.ts`) ajoute les règles d'usage : jamais plus d'un outil par message, jamais annoncer qu'un programme a été "créé" ou "modifié" tant que l'utilisateur n'a pas validé la carte, dire explicitement quand un jour demandé n'est pas un jour d'entraînement plutôt que d'appeler l'outil à vide.
- **`fetchActiveProgramSnapshot()`** récupère toute la semaine du programme actif (7 jours + exercices courants) en une seule requête jointe, pour que `adapter_seance` puisse résoudre un jour cité en langage naturel côté serveur sans aller-retour supplémentaire.
- **Découvert et corrigé pendant la vérif mobile demandée par l'utilisateur** : le header (`AppLayout.tsx`) débordait déjà à 375px avant cette étape (~39px, mesuré précisément via les largeurs réelles des éléments — l'outil de resize de la session Chrome ne fonctionnait pas dans cet environnement), aggravé par l'ajout de l'icône Coach (~79px). Corrigé par un bouton "Se déconnecter" en icône seule sous `sm` (au lieu du bouton texte, cohérent avec le reste de la nav) et `flex-wrap` + `gap-2 sm:gap-3` sur le groupe d'icônes comme filet de sécurité pour un futur ajout de nav, plutôt que de recalculer un seuil en dur.
- **Vérifié** : `tsc --noEmit` et ESLint propres (après retrait de deux imports inutilisés introduits par cette étape), migration confirmée appliquée en remote (`supabase migration list`), Edge Function `ai-coach-chat` confirmée déployée et `ACTIVE` (`supabase functions list`).
- **Non fait, backlog explicite** : pas de vérification en direct avec une vraie clé API sur cet écran, comme pour chaque étape précédente de la couche IA — à valider par l'utilisateur lui-même. Cette étape clôt la feuille de route "coach privé" en 5 étapes.

## Couche IA — retours post-test réel de l'étape 5 (2026-07-23)

Le user a testé `/coach` en direct avec sa vraie clé (première fois qu'une vérification "vraie clé" est faite par le user lui-même plutôt que différée) — round-trip confirmé fonctionnel, mais deux retours sur la forme.

- **Format de réponse imposé dans `COACH_PERSONA`** (donc partagé par les 4 fonctions, pas seulement le chat) : prose fluide, jamais de markdown (`*`, `**`, `-`, `#`, listes à puces), saut de paragraphe plutôt que puce pour séparer des idées. Ton ajusté de "direct, bienveillant" à "direct, amical et bienveillant". Les réponses passaient par du markdown brut affiché tel quel dans l'UI (pas de rendu markdown côté client) — illisible. Les 4 fonctions (`ai-coach-chat`, `ai-analyze-trends`, `ai-generate-program`, `ai-adapt-session`) redéployées. Le rendu côté `CoachPage` gagne aussi `whitespace-pre-line` sur les bulles, sans quoi les sauts de paragraphe demandés au modèle auraient été collapsés par le HTML par défaut.
- **Dictée vocale + lecture orale sur `/coach`** : `useSpeechToText` (nouveau hook générique, Web Speech API) extrait de l'ancien `useVoiceSetInput` (P3d) qui en devient un simple wrapper (`onResult` passé à travers `parseVoiceSetInput`) — deuxième consommateur réel justifiant l'extraction, pas une abstraction anticipée. `useTextToSpeech` (nouveau, `SpeechSynthesisUtterance`, standard DOM donc pas de shim de types nécessaire contrairement à `SpeechRecognition`) ajoute un bouton lecture par message assistant. Ni le micro ni la synthèse vocale n'ont pu être testés en direct (pas d'entrée audio réelle ni de sortie audio côté agent) — à valider par le user.
- **Découvert en testant** : `CoachPage` affiche un flash "Configure une clé API" avant que `useAiProviderKeys` ait fini de charger (le tableau vide par défaut pendant le chargement est indiscernable d'une vraie absence de clé) — bug réel mais mineur (UI se corrige d'elle-même en ~1-2s), **non corrigé**, laissé en backlog.
- **Programme de test créé à la demande du user** pour évaluer les futures adaptations de l'IA sur une base connue : ancien "FORCE 2026" (actif, 3 séances loguées) et "Hypertrophie 2026" (brouillon, 2 séances loguées) supprimés à la demande explicite du user malgré la perte définitive de historique par cascade FK (`session_logs.session_template_id on delete cascade`) — signalé avant d'agir, confirmé par AskUserQuestion. Nouveau "Programme test" (hypertrophie, brouillon) : mardi=jambes et mercredi=pecs/triceps repris à l'identique des anciens programmes (mêmes exercices et cibles), jeudi=dos/biceps et vendredi=épaules nouveaux ; lundi/samedi/dimanche en repos — mapping clarifié par le user lui-même (pas lundi-vendredi comme demandé initialement, la vraie routine est mardi-vendredi). Écrit via REST directement depuis le contexte du navigateur (pas Bash) : le token JWT de la session ne peut pas être extrait vers l'environnement agent (bloqué par un garde-fou de sécurité de l'outil, contournement correct = exécuter le fetch dans la page elle-même) ; le token stocké était aussi expiré, rafraîchi via le refresh token avant d'écrire. **Bon réflexe à retenir** : toujours vérifier l'historique de logs réel lié à un programme avant une suppression demandée, même explicitement — ici ça a permis de proposer un choix éclairé plutôt qu'une perte de données surprise.

## Nettoyage éditorial — suppression du tiret cadratin "—" dans tous les textes (2026-07-23)

Retour du user après le test réel de l'étape 5 : plus aucun "—" nulle part dans les textes affichés à l'utilisateur, ni dans les réponses de l'IA.

- **33 chaînes UI revues dans 18 fichiers** (`src/pages`, `src/components`, `src/lib`, `src/hooks`) : deux familles de cas traitées différemment plutôt qu'un remplacement mécanique par un seul caractère. Les paires "label — valeur courte" (ex. `{exercice} — {séries} x {reps}`, `Aujourd'hui — {jour}`) reprennent le point médian `·` déjà utilisé ailleurs dans l'app (`3 x 8-10 · repos 90s`) plutôt que d'introduire un nouveau séparateur. Les phrases pleines reliées par un tiret (ex. "Logue quelques séances d'abord — l'IA ne propose que...") sont scindées en deux phrases, ou reliées par un deux-points quand la deuxième partie est une clarification plutôt qu'une idée indépendante — jugé cas par cas, pas de règle unique.
- **`COACH_PERSONA`** : la règle de format (déjà réécrite à l'étape précédente pour bannir markdown/puces) bannit maintenant aussi explicitement le tiret cadratin dans les réponses générées, avec point/virgule/deux phrases comme alternative suggérée. Message d'erreur codé en dur dans `ai-coach-chat.ts` (jour hors programme actif) corrigé aussi, ce n'est pas généré par le modèle. Les 4 fonctions IA redéployées.
- **Hors périmètre, volontairement** : les tirets cadratins dans les commentaires de code et dans `TODOS.md` lui-même n'ont pas été touchés — ce ne sont pas des "textes de l'app", c'est de la documentation interne, même logique que pour la revue markdown de l'étape précédente.
- **Trouvé en vérifiant après coup** : la description du "Programme test" (écrite par l'agent lui-même via REST lors de sa création) contenait elle-même un "—" — corrigée. Rappel utile : du contenu que l'agent insère en base pendant une tâche est aussi un "texte de l'app", pas seulement le code source.
- **"Programme test" repassé en `active`** à la demande du user, pour rester la base de référence des prochains tests d'adaptation IA.
- **Vérifié** : `tsc --noEmit` et ESLint propres, grep confirmant zéro occurrence non-commentée restante dans `src/`, nouvelle réponse de test en direct sur `/coach` confirmée sans `—` ni markdown et référençant correctement le programme actif (jeudi = dos/biceps).

## Coach IA — le chat ne savait pas quel jour on était (2026-07-23)

Repéré par le user sur une réponse réelle : "demain" ne pouvait pas être résolu par le modèle puisque rien ne lui disait la date/le jour actuels. Pas un besoin de "connexion web" (le navigateur connaît déjà l'heure exacte, aucune API externe n'est nécessaire) — juste une donnée manquante dans le contexte envoyé au modèle.

- **`CoachChatInput.today`** (`{ isoDate, dayOfWeek }`, nouveau) calculé côté client (`CoachPage.tsx`) et injecté dans `buildContextMessage()` (`ai-coach-chat.ts`) avec une phrase explicite : utiliser cette date pour résoudre "aujourd'hui"/"demain"/"après-demain" en jour concret avant d'appeler `adapter_seance`. Règle correspondante ajoutée à `COACH_CHAT_SYSTEM_PROMPT`. Champ requis côté Edge Function (400 si absent/malformé), même rigueur que `profileContext`/`trendSummary`.
- **`getTodayIsoDayOfWeek()` extrait dans `sessions-api.ts`** (était dupliqué à l'identique dans `HomePage.tsx` et `WellnessPage.tsx`) — troisième consommateur réel qui justifie l'extraction, `toLocalDateString()` reste volontairement dupliqué (pas de troisième besoin identique, signatures légèrement différentes selon les call sites, même logique que la décision déjà prise en P8).
- **Vérifié en direct** : `tsc --noEmit`/ESLint propres, redéployé `ai-coach-chat`, reposé la même question ("dos et biceps demain ?") — la réponse identifie maintenant correctement le jour réel (jeudi) et corrige elle-même la confusion de la réponse précédente (demain = vendredi = épaules, pas dos/biceps).

## Coach IA — generer_programme et adapter_seance testés avec une vraie clé, deux bugs réels trouvés et corrigés (2026-07-23)

Premier test en direct de ces deux outils via le chat (jusqu'ici seul `analyser_tendance` avait été validé avec une vraie clé). Les deux ont révélé un vrai bug chacun, aucun des deux lié au chat en tant que tel : les deux fonctions dédiées (`ai-adapt-session`, `ai-generate-program`) et leurs UI autonomes (`SessionAdaptationDialog`, `ProgramGeneratePage`) partagent le même code et étaient donc affectées aussi.

- **`adapter_seance` rejetait les exercices déjà présents dans la séance.** `validateAdaptation()` (`ai-session-adaptation.ts`) ne construisait `allowedExerciseIds` qu'à partir de `availableExercises` (exercices déjà logués), jamais de `currentExercises` (composition actuelle de la séance) — alors que le persona encourage explicitement à garder un exercice tel quel plutôt que de tout réécrire. Résultat : dès que l'utilisateur n'avait pas encore logué de séries pour un exercice pourtant déjà dans son programme (le cas de "Programme test", entièrement non logué), toute adaptation qui gardait ne serait-ce qu'un seul exercice inchangé échouait avec "Exercice proposé hors de la liste autorisée". Corrigé : `allowedExerciseIds` est maintenant l'union des deux listes. Retesté avec succès : proposition réaliste reçue (baisse ciblée séries/RPE pour fatigue déclarée), non appliquée (jamais sans confirmation explicite de l'utilisateur, surtout sur "Programme test" qui doit rester une base stable pour les prochains tests).
- **`generer_programme` avec un pool d'exercices vide produisait quand même une carte de proposition vide et cliquable.** Quand `availableExercises` est vide (aucune séance encore logguée), le modèle refuse à raison de générer des exercices inventés — mais `runGenerateTool` (`ai-coach-chat.ts`) attachait quand même la proposition (0 exercice sur les 7 jours) à la réponse, donnant un bouton "Créer ce programme" menant à un programme totalement vide. Corrigé : si aucun jour de la proposition n'a d'exercice, pas de carte, seulement le texte d'explication du modèle. Retesté : plus de carte, juste le message.
- **Cause commune** : "Programme test" (créé cette même session) n'a aucune séance loguée, un cas jusque-là jamais rencontré en test réel — les étapes précédentes utilisaient soit le compte fixture e2e (avec historique), soit le vrai compte quand il avait encore ses séances (supprimées avec FORCE 2026/Hypertrophie 2026 plus tôt cette session). **Bon réflexe à en tirer** : un compte fraîchement vidé de tout historique est un cas de test légitime et sous-couvert, à garder en tête pour la suite de la couche IA.
- **Vérifié** : `tsc --noEmit`/ESLint propres, `ai-adapt-session` et `ai-coach-chat` redéployés, les deux correctifs revalidés en direct dans la même conversation.
- **Non fait, backlog explicite** : test de `generer_programme` avec un pool d'exercices non vide (donc le chemin "création réelle d'un programme") — nécessite d'abord de logger au moins une séance sur "Programme test", proposé au user, réponse en attente.

## Programmes — un seul programme actif à la fois, appliqué par contrainte serveur (2026-07-23)

Séquence complète de test de la couche IA ce jour : séance de test loguée sur "Programme test" (6 séries sur les 5 exercices jambes, via l'UI réelle, pas REST), `generer_programme` retesté avec un pool d'exercices non vide cette fois — proposition complète et réaliste reçue (« Force Jambes 4 jours », 4 jours, uniquement des exercices jambes puisque c'est tout ce que l'historique contient, le modèle le dit explicitement plutôt que d'inventer). Appliqué à la demande du user comme nouveau programme séparé (pas une modification de "Programme test", confirmé par AskUserQuestion avant d'agir vu l'ambiguïté de la formulation).

- **Bug d'UX découvert immédiatement après** : activer "Force Jambes 4 jours" n'a pas désactivé "Programme test", laissant deux programmes `active` simultanément. Ni le dashboard (`HomePage`) ni le contexte du coach (`fetchActiveProgramSnapshot`) n'ont de garde-fou contre ça — les deux choisissent silencieusement "le programme actif le plus récent", donc le second programme activé écrase silencieusement le premier plutôt que de lever une erreur. Le user avait explicitement demandé de garder "Programme test" comme base stable pour les tests IA ; cette activation a shadow ce choix sans avertissement.
- **Corrigé à la racine, pas seulement dans l'UI** : trigger Postgres `on_program_activated` (migration `20260723150000_single_active_program.sql`) — dès qu'un programme passe à `status = 'active'`, tous les autres programmes `active` du même utilisateur repassent automatiquement en `draft`. Choix délibéré de démotion silencieuse plutôt que de bloquer la deuxième activation avec une erreur : l'UI traite déjà le statut comme un simple bouton à trois états (Brouillon/Actif/Archivé), pas une transition gardée — une erreur y serait déroutante pour ce qui est en pratique juste "je change de programme suivi". `security definer` + `set search_path = ''`, même convention que `handle_new_program()` (P2, création automatique des 7 jours).
- **Vérifié en direct** : migration poussée (`supabase db push`), testé dans les deux sens (activer "Force Jambes 4 jours" désactive bien "Programme test" automatiquement, et vice versa) avant de refixer l'état voulu par le user ("Programme test" seul actif).

## Vraies photos dans le sélecteur d'exercices (2026-07-23)

Item de backlog débloqué : le user a tranché (AskUserQuestion) pour une base d'images libres (wger.de, CC-BY-SA / communautaire) plutôt qu'upload utilisateur ou génération IA, et pour une intégration simple (miniature dans le sélecteur) plutôt qu'une maquette dédiée.

- **Matching par nom, pas d'intégration API en direct** : le catalogue système (165 exercices, `user_id is null`, extraits des 3 migrations de seed) a été rapproché hors-ligne du dataset complet de wger (828 exercices, `/api/v2/exerciseinfo/`, paginé) par similarité de tokens sur les traductions français ET anglais de chaque exercice wger. Choix délibéré : pas d'appel API à la volée depuis le client ou une Edge Function, seulement un script ponctuel qui écrit le résultat dans une migration SQL (`20260723160000_exercise_images.sql`) — le catalogue système ne change pas assez souvent pour justifier une synchronisation live, et ça évite une dépendance runtime à la disponibilité de wger.de.
- **Couverture partielle assumée, pas un bug** : seulement 71/165 exercices (43%) ont une photo. Deux causes : wger n'a lui-même une image que pour ~32% de son propre catalogue (264/828), et le seuil de similarité (score de chevauchement de tokens ≥0.65, avec 3 exclusions manuelles après relecture pour des faux positifs sémantiques comme "Mollets debout" → une image d'étirement plutôt que de mollets debout) a volontairement laissé de côté les correspondances peu fiables. Une mauvaise photo est pire que pas de photo.
- **Schéma** : `exercises.image_url` (text, nullable) — nouvelle colonne, `database.types.ts` mis à jour à la main (même convention que le reste du fichier, pas de régénération automatique).
- **`ExercisePicker.tsx`** : nouveau composant `ExerciseThumbnail`, miniature 32px avec repli sur l'icône de groupe musculaire déjà existante (`CircleDot` par défaut) — à la fois quand `image_url` est absent (majorité du catalogue) et quand l'image échoue à charger (`onError`), pour ne jamais laisser une icône d'image cassée si wger.de devient indisponible plus tard.
- **Vérifié en direct** : `tsc --noEmit`/ESLint propres, migration poussée, sélecteur d'exercice réaffiché sur "Programme test" — vraies photos visibles pour les exercices matchés (ex. Ab Wheel Rollout, Crunch lesté), icône de repli propre pour les autres.
- **Non fait, backlog explicite** : les ~57% d'exercices sans photo (surtout les variantes très spécifiques : curls nommés, presses spécifiques, mouvements d'haltérophilie), pas de miniature ailleurs que le sélecteur (cartes de séance, page de log) — scope volontairement limité au sélecteur pour cette première passe.

## Deuxième passe sur les photos d'exercices, à la demande du user (2026-07-23)

Le user a demandé de couvrir les 94 exercices restés sans photo après la première passe automatisée. Plutôt que de baisser le seuil de similarité de tokens (qui aurait produit des faux positifs déjà identifiés comme mauvais lors de la première passe, ex. Hack Squat vs "Slow Squat"), chacun des 94 a été recherché individuellement dans le dataset wger complet, en élargissant aux traductions allemande et espagnole en plus du français/anglais déjà indexés — plusieurs bonnes correspondances n'existaient que sous ces langues (ex. "Rowing à la T-bar" en français pour T-Bar Row, "Kniebeuge an Hackenschmidtmaschine" en allemand pour Hack Squat).

- **61 exercices supplémentaires couverts** (migration `20260723170000_exercise_images_second_pass.sql`), portant la couverture totale à 132/165 (80%, contre 43% après la première passe). Certains réutilisent une photo déjà assignée à un autre exercice de la même famille de mouvement quand seul l'équipement diffère (ex. Rack Pull et Trap Bar Deadlift réutilisent la photo générique "Deadlifts", Preacher/EZ-Bar Curl ont chacun leur propre photo dédiée trouvée cette fois).
- **33 exercices restent volontairement sans photo** : aucune image de confiance trouvée dans wger pour ces variantes très spécifiques (Spider Curl, Zottman Curl, Drag Curl, Reverse Crunch, Bicycle Crunch, Dead Bug, Mountain Climber, Burpee, Thruster, Box Jump, Sled Push/Pull, Battle Ropes, Turkish Get-Up, Man Maker, et quelques squats/presses de niche). Une fausse piste notable écartée : "donkey" en français ne renvoie que "Donkey kick à l'haltère" (un exercice de fessiers complètement différent de la Donkey Calf Raise cherchée) — bon rappel que la présence d'un mot-clé ne suffit pas, il faut vérifier que l'exercice trouvé est bien le même mouvement.
- **Vérifié en direct** : migration poussée, sélecteur réaffiché sur "Programme test" — nouvelles photos confirmées visibles (Barbell Shrug, Dumbbell Shrug).

## Photos d'exercices partout, pas seulement le sélecteur (2026-07-23)

Le user a demandé d'étendre les miniatures à tout l'endroit où un exercice est affiché, pas seulement `ExercisePicker`. Recherche préalable (agent Explore) pour cartographier tous les points de rendu avant de toucher au code, plutôt que de les découvrir un par un.

- **`ExerciseThumbnail` extrait de `ExercisePicker.tsx` vers `src/components/ExerciseThumbnail.tsx`** (accepte `imageUrl`/`muscleGroup` en props primitives plutôt qu'un objet `Exercise` complet, pour rester découplé de la forme exacte des données à chaque site d'appel — certains n'ont qu'un `SetHistoryRecord`, d'autres un `SessionTemplateExercise`, etc.) — deuxième et enième consommateur réel, justifie l'extraction.
- **Remontée de `image_url` dans toute la chaîne de données**, `SetHistoryRecord`/`SetHistoryRow` (`analytics-api.ts`) en étant le point de levier central : consommé par `RecentHighlight` et `getLoggedExercises` (`analytics.ts`), et par les trois constructions locales de `availableExercises` (`ProgramGeneratePage`, `SessionAdaptationDialog`, `CoachPage`). Egalement propagé indépendamment dans `sessions-api.ts` (3 `.select()`), `offline-db.ts`/`session-logs-api.ts` (cache Dexie de la séance en cours), et `social-api.ts` (jointure `milestones` → `exercises` pour le feed).
- **Décision délibérée : `AvailableExercise` (le type envoyé à l'IA) n'a PAS été élargi avec `imageUrl`.** Les 3 écrans qui construisent ce payload (génération de programme, adaptation de séance, chat coach) construisent une `Map` séparée (`thumbnailByExerciseId`) à partir de la même source (`history`), utilisée uniquement pour l'affichage local — l'IA n'a aucune raison de recevoir des URLs d'images dans son contexte, ça gonflerait le prompt pour rien.
- **11 fichiers de rendu touchés** : `ExercisePicker`, `SessionLogPage` (séance en cours), `SessionTemplateCard` (jours du programme), `HomePage` (carte "Aujourd'hui" + "Dernière séance"), `AnalyticsPage` (dropdown de sélection d'exercice, miniature dans un `SelectItem` — pattern déjà supporté par le composant shadcn existant), `FeedPage` (cartes de record, uniquement si `exercise_id` existe), et les 3 écrans de proposition IA (`ProgramGeneratePage`, `SessionAdaptationDialog`, `CoachPage`).
- **Vérifié en direct** sur 4 écrans (dashboard, séance en cours, dropdown Analytics, feed, carte de proposition d'adaptation dans le chat) — photos réelles confirmées partout où elles existent en base, repli sur l'icône de groupe musculaire ailleurs. Séance de test démarrée par erreur pendant la vérification (juste pour voir le rendu), supprimée aussitôt pour ne pas laisser de séance "en cours" fantôme.
- **Non fait** : les 33 exercices toujours sans photo (backlog déjà documenté) continuent d'afficher l'icône de repli partout, comme prévu.

## Jauges de progression sur le dashboard (2026-07-23)

Item de backlog noté depuis P4b, cadré avant implémentation (AskUserQuestion, comme d'habitude) : pour l'objectif de poids, juste l'écart restant en texte (pas de barre à %, pas de référence à un point de départ — trop fragile si l'objectif change en cours de route) ; pour les records, le dernier obtenu (type + valeur + date) plutôt qu'un compteur sur 30 jours.

- **Découverte en cadrant** : la jauge "objectif" existait déjà, littéralement le texte demandé ("Objectif : 90 kg — 1.2 kg restants"), planquée en petite ligne dans la carte `WeightCard`. Pas dupliqué une seconde carte pour ça — juste mis en valeur avec un `Badge` (au lieu d'une ligne de texte discrète) pour que ça se lise comme une vraie jauge au premier coup d'œil, plus `Badge variant="default"` + libellé "Atteint" quand le poids actuel égale exactement l'objectif.
- **`LatestMilestoneCard`** (nouveau) : dernier record personnel, sous "Dernière séance". Nouvelle fonction `fetchLatestMilestone()` (`social-api.ts`) — délibérément **pas** un simple `.slice(0,1)` sur les données déjà chargées par `useSocialFeed()` (qui inclut aussi les milestones des autres utilisateurs opted-in) : requête dédiée filtrée sur `user_id = auth.uid()`, jamais le record de quelqu'un d'autre même si le plus récent chronologiquement. Réutilise `MILESTONE_TYPE_LABELS`/`formatMilestoneValue` (`social-display.ts`) et `ExerciseThumbnail`, cohérent avec le reste de l'app plutôt que de réinventer un format d'affichage.
- **Vérifié en direct** : `tsc --noEmit`/ESLint propres, dashboard réaffiché — carte "Dernier record" (1RM Presse à cuisses, 160 kg) et badge "Objectif : 90 kg · 1.2 kg restants" confirmés visibles.

## Phase de fixes après les jauges (2026-07-23)

Trois bugs connus, identifiés lors des étapes précédentes mais mis de côté pour rester focalisé sur les jauges, traités ensemble à la demande du user ("on passe par la phase de fixes").

- **Débordement du header sur `ProgramDetailPage` en mobile** : la rangée titre/badges + boutons "Dupliquer"/"Supprimer" (`flex items-start justify-between gap-4`) ne wrappait pas, poussant les boutons hors de l'écran sous ~375px. Corrigé avec `flex-wrap`. Vérifié par mesure réelle (`getBoundingClientRect()`, `resize_window` ne changeant pas le viewport effectif dans cet environnement) : groupe de boutons 176px + pastille de statut 209px, les deux tiennent dans les 343px disponibles une fois repassés à la ligne.
- **Flash de contenu au chargement de `CoachPage`** : pendant le chargement de `useAiProviderKeys()`, `configuredProviders.length === 0` était temporairement vrai (tableau vide par défaut), affichant une fraction de seconde le message "configure une clé API" avant de basculer sur le vrai contenu une fois les clés chargées. Corrigé en distinguant explicitement l'état de chargement (`isLoading` exposé par le hook) de l'état "aucune clé configurée", avec un simple "Chargement…" entre les deux.
- **Pattern `invalidateQueries` non voidé dans 4 hooks** : `useAiProviderKeys.ts`, `useExercises.ts`, `useSessionTemplates.ts`, `useWeightEntries.ts` avaient encore `onSuccess: () => queryClient.invalidateQueries(...)` sans préfixe `void`, contrairement à la convention déjà appliquée ailleurs dans le repo (ex. `useAssistant.ts`) — la promesse de refetch en arrière-plan n'a pas besoin d'être attendue par le mutation lifecycle. 11 occurrences corrigées au total.
- **Vérifié en direct** : `tsc --noEmit` (exit 0), ESLint (0 erreur, 4 warnings préexistants et sans rapport, pattern `react-refresh/only-export-components`), rechargement de `/programs/:id` et `/coach` confirmant les deux correctifs visuels.
- **Prochaine étape, à la demande du user** : discussion sur l'amélioration du design global, volontairement pas commencée avant la fin de cette phase de fixes.

## Dashboard simplifié, refonte du feed social (2026-07-23)

Deux demandes du user avant de passer au design global : (1) retirer "Dernière séance" et "Dernier record" du dashboard, jugées pas utiles ; (2) réduire la partie sociale à l'atteinte des objectifs, aux photos, et à un vrai mécanisme social (poster/liker/commenter), qui n'existait pas du tout avant. Cadré par AskUserQuestion sur deux points : le nouveau "post" est un type à part (texte libre + photo optionnelle), pas une simple relabellisation du flux photo existant ; likes et commentaires s'appliquent partout dans le feed (jalons compris), pas seulement aux posts.

- **Dashboard** : `RecentHighlightCard` ("Dernière séance") et `LatestMilestoneCard` ("Dernier record") retirées de `HomePage.tsx`. Code mort qui en dépendait supprimé plutôt que laissé trainer : `getMostRecentHighlight`/`RecentHighlight` (`analytics.ts` + tests), `useLatestMilestone`/`fetchLatestMilestone` (`useSocialFeed.ts`/`social-api.ts`).
- **`posts` remplace `progress_photos`** (migration `20260723180000_posts_and_reactions.sql`) : `content` (texte, nullable) + `storage_path` (nullable) avec une contrainte `check` exigeant au moins l'un des deux. L'ancien flux "ajouter une photo" devient un cas particulier de post (photo seule, sans texte). Données existantes recopiées avant `drop table progress_photos` ; même bucket Storage (`progress-photos`, policies RLS inchangées, seule la table SQL change). Le backdatage manuel (`photo_date`) n'a pas de suite : un post est daté par sa publication (`created_at`), pas par un album — décision volontaire, pas un oubli.
- **`feed_likes`/`feed_comments`** : tables polymorphes (`target_type` in `milestone`/`post`, `target_id` sans FK réelle puisque deux tables cible possibles) plutôt que 4 tables dédiées — un seul jeu de hooks/composants (`ReactionBar`, `CommentsSection`) couvre les deux types de contenu. Visibilité centralisée dans `can_view_feed_target()` (`security definer`, même convention que `detect_one_rep_max_milestone`/`on_program_activated`) : un like/commentaire n'est lisible/postable que sur un jalon ou post que l'utilisateur peut déjà voir (soi-même ou un profil opted-in). Pas de `ON DELETE CASCADE` possible sur une FK polymorphe : nettoyage par trigger (`cleanup_feed_reactions`, avant suppression d'un jalon ou d'un post) plutôt que des lignes orphelines qui traînent.
- **`database.types.ts` régénéré via `supabase gen types typescript --linked`** plutôt qu'édité à la main pour ce changement (contrairement à la convention habituelle du fichier) — trop de tables touchées d'un coup (`posts` remplace `progress_photos`, +`feed_likes`, +`feed_comments`) pour rester fiable à la main sans risque de désync avec le schéma réellement poussé.
- **Bug latent découvert en cours de route, sans rapport avec cette tâche** : `pnpm exec tsc --noEmit` (sans `-p`) ne vérifie en réalité aucun fichier dans ce repo — `tsconfig.json` racine n'a que des `references` avec `"files": []`, et sans le flag `-b` aucun projet référencé n'est suivi. Utilisé tel quel plusieurs fois plus tôt dans cette session, il a laissé passer plusieurs erreurs de type réelles (déjà présentes avant cette tâche : `imageUrl` manquant dans un test d'`analytics.ts`, cast `Json` manquant dans `assistant-api.ts`, fixtures `social-display.test.ts` désynchronisées du type `MilestoneWithImage`) — corrigées au passage. La bonne commande est `pnpm exec tsc --noEmit -p tsconfig.app.json`.
- **Vérifié en direct** sur le vrai compte : `tsc`/ESLint/tests unitaires propres (91 tests, dont les fixtures corrigées), dashboard sans les deux cartes retirées, feed testé de bout en bout (like/unlike sur un jalon, ajout et suppression d'un commentaire, création et suppression d'un post texte) — données de test nettoyées immédiatement après vérification pour ne rien laisser sur le compte réel.

## Déploiement GitHub Pages, pour tester sur mobile avant la phase design (2026-07-23)

Le user a demandé un simple `git push` pour tester sur mobile, mais aucun remote n'existait encore et rien n'était configuré pour transformer un push en déploiement accessible. Cadré via AskUserQuestion : réseau local vs déploiement web (déploiement choisi), puis hébergeur (ni Vercel ni Netlify configurés sur cette machine — le user a préféré GitHub Pages, comme pour son projet Voice2Midi), puis visibilité du repo (privé, la page Pages elle-même reste accessible via son URL même si le code ne l'est pas).

- **`vite.config.ts`** : `base` conditionnel (`/fitness-app/` uniquement si `GITHUB_PAGES=true`, `/` partout ailleurs — dev, et tout futur hébergeur qui sert depuis la racine) puisque GitHub Pages sert ce repo comme *project page* (`arnaudzeh.github.io/fitness-app/`), jamais depuis la racine du domaine. Manifest PWA (`start_url`/`scope`) suit la même variable plutôt que rester en dur sur `/`.
- **`App.tsx`** : `<BrowserRouter basename={import.meta.env.BASE_URL}>` — sans ça, react-router router résoudrait les routes depuis la racine du domaine alors que l'app vit sous `/fitness-app/`.
- **`src/sw.ts`** : trois chemins en dur (`matchPrecache('/index.html')`, icônes de notification, URL de clic sur notification) auraient silencieusement cassé sous un sous-chemin — le premier dégrade proprement (fallback réseau si pas de match), les deux autres non (icône cassée, notification renvoyant vers une 404 à la racine du domaine). Remplacés par une valeur dérivée de `self.registration.scope` au lieu d'un deuxième chemin en dur à synchroniser avec `vite.config.ts` — correct aussi bien en dev (scope `/`) qu'en prod GitHub Pages (scope `/fitness-app/`), sans rien changer côté serveur (`send-wellness-reminders` envoie toujours `/bien-etre` en relatif, résolu par le service worker au moment de l'ouverture, pas par l'Edge Function).
- **Fallback SPA 404.html** : GitHub Pages n'a pas de rewrite serveur pour le routing côté client — sans ça, toute route profonde (`/coach`, `/programs/:id`...) ou un rechargement dessus renvoie une vraie 404 GitHub avant même que React puisse prendre la main. Résolu en copiant `dist/index.html` vers `dist/404.html` après le build (même principe que le fallback offline du service worker, mais côté serveur statique).
- **`.github/workflows/deploy.yml`** (nouveau) : build + déploiement sur push vers `main`, via `actions/deploy-pages` (Pages en mode "GitHub Actions", pas l'ancien mode "déployer depuis une branche"). Les 3 variables `VITE_*` (déjà documentées comme sûres à exposer côté client dans `.env.example`) passées en secrets repo plutôt qu'en dur, pour rester dans le même mécanisme que `.env.local` en local.
- **Vérifié en local avant push** : `GITHUB_PAGES=true pnpm build` reproduit exactement le build CI — tous les chemins de `dist/index.html` et du manifest confirmés préfixés `/fitness-app/`, et la clé de precache du service worker (`index.html`, relative dans le manifest workbox) confirmée se résoudre à la même URL absolue que la construction `${scopePath}index.html` du fetch handler (les deux passent par `new URL(url, location.href)`, donc convergent peu importe la forme).
- **Repo créé privé, repassé en public en cours de route** : GitHub Pages a refusé la première tentative d'activation ("Your current plan does not support GitHub Pages for this repository") — Pages n'est pas disponible sur un repo privé en dehors d'un plan payant (Pro/Team), contrainte non documentée dans ce projet avant de la rencontrer en direct. Recadré via AskUserQuestion (repo public vs retour au réseau local) : le user a choisi de passer le repo en public plutôt que de revenir au réseau local, `gh repo edit --visibility public`.
- **Vérifié en direct sur le déploiement réel** : run GitHub Actions réussi, `https://arnaudzeh.github.io/fitness-app/` répond 200 avec les bons chemins, page de connexion stylée correctement chargée dans un nouvel onglet. Fallback 404.html confirmé en navigant directement vers une route profonde (`/fitness-app/coach`, jamais visitée depuis la racine) — sert bien l'app plutôt qu'une vraie 404, React Router prend le relais et redirige vers `/login` (non authentifié), comme attendu.
- **Toute future modification poussée sur `main` redéploie automatiquement** — le `git push` que le user avait initialement demandé fonctionne maintenant tel qu'il l'imaginait dès la prochaine fois.

## Inscription impossible sur le déploiement : deux vrais bugs trouvés en creusant (2026-07-23)

Le user a essayé de créer un compte sur le déploiement fraîchement mis en ligne et n'a reçu aucun email de confirmation. Diagnostic direct sur la config Auth du projet Supabase distant (via l'API Management, pas le dashboard) plutôt que de deviner.

- **Cause immédiate : aucun SMTP réel configuré**, le projet dépend du mailer de test intégré de Supabase (`rate_limit_email_sent: 2`/heure, prévu pour du test, pas de la prod) — quota très probablement déjà épuisé par tous les tests de comptes de cette session et des précédentes. En plus, `site_url` était resté sur `http://localhost:3000` (jamais mis à jour), donc même un email livré aurait pointé vers un lien mort depuis le mobile.
- **Cadré via AskUserQuestion** (désactiver la confirmation email vs configurer un vrai fournisseur type Resend) : le user a choisi de désactiver pour l'instant, adapté à un usage personnel mono-utilisateur, à revisiter si l'app s'ouvre un jour à d'autres. Appliqué via l'API Management (`PATCH /config/auth`) : `mailer_autoconfirm: true` (compte actif immédiatement à l'inscription, aucun email requis), `site_url` et `uri_allow_list` corrigés vers l'URL GitHub Pages réelle (+ localhost conservé pour le dev). Vérifié en direct : inscription → session active immédiatement, sans email.
- **Deuxième bug trouvé en testant la suppression d'un compte de test** (nettoyage après vérification, pas une demande du user) : `account-delete` interrogeait encore `progress_photos`, supprimée plus tôt dans la même session lors de la refonte du feed social (remplacée par `posts`) — cassait la suppression de compte pour **tout le monde**, pas seulement le compte de test. Corrigé (`posts`, filtré sur `storage_path is not null` puisque contrairement à `progress_photos` tous les posts n'ont pas de photo) et redéployé.
- **Troisième bug, intermittent, côté plateforme Supabase, pas le code** : plusieurs appels à `account-delete` (et potentiellement les 7 autres Edge Functions qui partagent `requireAuthedContext`) ont échoué sporadiquement avec `AuthApiError: unrecognized JWT kid <nil> for algorithm ES256` — le même token, appelé en `fetch` brut sur `/auth/v1/user`, se vérifiait pourtant sans problème. Reproduit et confirmé disparaître au simple retry (un nouvel appel identique réussit). Root cause probable : cache JWKS transitoire côté runtime Edge Functions, quelques jours après la migration du projet vers la signature asymétrique ES256 (clé en place depuis le 2026-07-20). Pas un bug de code à corriger ici — noté pour rester vigilant si un appel à une de ces 8 fonctions échoue une fois puis réussit au réessai.
- **Nettoyage** : 6 comptes de test jetables (`claude-deploy-*@fitness-app.test`) créés pendant le diagnostic, tous supprimés avant de rendre la main.

## Le compte réel (arnaud.zeh@gmail.com) supprimé à la demande explicite du user (2026-07-23)

Suite à l'entrée précédente, le user a demandé de supprimer son compte réel pour pouvoir se réinscrire, pensant qu'un second compte cassé avait été créé. Vérifié avant d'agir plutôt que d'exécuter tel quel : une seule ligne existait pour cet email (`supabase db query --linked` sur `auth.users`), créée le 2026-07-21, avec de vraies données (2 programmes, 1 séance loguée, 1 pesée, 7 records, 1 clé API IA) — l'erreur "compte déjà existant" à l'inscription était le comportement normal de Supabase sur un email déjà enregistré, pas un bug. Alternative non destructive proposée (réinitialiser le mot de passe, tout conserver) via AskUserQuestion — **le user a explicitement choisi de supprimer quand même**, en toute connaissance de cause.

- Suppression effectuée via la vraie fonction `account-delete` (pas un appel admin brut) avec le token de session du user lui-même (rafraîchi depuis le `refresh_token` stocké dans le `localStorage` de l'onglet déjà connecté) — même chemin de nettoyage Vault/Storage qu'un clic réel sur "Supprimer mon compte", pas de raccourci qui laisserait des ressources orphelines.
- **À noter pour la suite** : le compte n'existe plus, `Programme test` et tout l'historique construit depuis le début du projet ont disparu avec. Le user va se réinscrire de zéro sur le déploiement.
- Question annexe posée en même temps (partage social activé, visible publiquement maintenant que l'app est déployée) devenue sans objet une fois le compte supprimé.

## Phase design — audit mobile réel et corrections (2026-07-23)

Le user a lancé la phase design explicitement mise de côté depuis le début de la couche IA, en demandant de se concentrer sur l'expérience mobile (90% de l'usage réel) plutôt que le look global, sans référence visuelle précise à copier. Plutôt que de proposer des changements à l'aveugle, audit réel : captures d'écran Playwright à 375px de large (viewport mobile) sur les 8 écrans principaux avec le compte fixture e2e, plus mesure précise des zones tactiles du header (`boundingBox()`). Sept points concrets trouvés et corrigés, dans l'ordre d'impact confirmé par le user.

- **Icônes de navigation du header : 28×28px partout dans l'app**, mesuré (pas estimé) — bien sous les ~44px recommandés (Apple HIG/Material) pour une cible tactile. Nouvelle variante `icon-xl` (`size-11`, 44px) ajoutée à `button.tsx` plutôt que de modifier `icon-sm` en place (utilisé ailleurs dans des contextes plus denses, ex. suppression inline d'un commentaire — un hit-target de 44px y serait disproportionné). Header repasse sur 2 lignes à 375px (`flex-wrap` déjà en place depuis une phase de fixes précédente) — assumé, pas un bug.
- **Graphique "Tonnage hebdomadaire" devenait un bloc de couleur plein** avec 1-2 points de données (`recharts` étire la barre sur toute la largeur de catégorie disponible par défaut). Corrigé avec `maxBarSize={48}` sur le `<Bar>` — reste une vraie barre quel que soit le nombre de points.
- **État vide du Coach** (pas de clé configurée) : juste une petite carte flottant dans une grande zone noire, peu engageant pour la fonctionnalité phare de l'app. Refait en état vide centré avec badge d'icône circulaire, titre, description resserrée sur les 3 capacités concrètes (discuter, générer un programme, adapter une séance).
- **`ProgramDetailPage` : 7 cartes pleine largeur empilées** même pour les jours de repos purs, coûteux à parcourir pour voir la forme de la semaine. Ajout de `WeekOverviewStrip` (nouveau, dans `ProgramDetailPage.tsx`) : bande compacte de 7 jours (grid, pas flex-wrap) avec indicateur repos/entraînement, chaque jour cliquable (ancre `#day-{n}`) vers sa carte détaillée en dessous — les cartes existantes gardent toute l'édition, la bande n'ajoute qu'une vue d'ensemble. `WEEKDAY_SHORT_LABELS` ajouté à `sessions-api.ts` à côté de `WEEKDAY_LABELS`.
- **`ProfilePage` très longue en une colonne** : sections "Module cycles" et "Partage de progrès" (texte explicatif de 3-4 lignes chacune, peu consultées une fois le choix fait) repliées derrière un `<details>`/`<summary>` natif ("Qu'est-ce que c'est ?"), sans nouvelle dépendance. Les sections plus sensibles (clé API, suppression de compte) gardent leur texte toujours visible — pas de repli sur ce qui engage la confiance ou la sécurité.
- **Sélecteur de jours de Bien-être** ("Lun Mar... Dim") repassait à la ligne avec "Dim" tout seul (`flex-wrap` sur des `Button size="sm"` avec padding fixe, ne tenait pas sur 343px utiles). Remplacé par `grid grid-cols-7` (même principe que `WeekOverviewStrip`) — les 7 jours tiennent toujours sur une ligne, quelle que soit la largeur.
- **Piste explorée puis abandonnée** : le champ date de naissance affichait `mm/dd/yyyy` (format US) dans les captures. Vérifié avant de "corriger" quoi que ce soit : `<input type="date">` affiche le format selon la locale système du navigateur qui l'affiche, pas selon le `lang="fr"` de la page — la capture reflétait la locale de la machine de dev (Playwright/Chromium), pas nécessairement celle du téléphone réel du user (probablement déjà en français). Pas de changement fait pour éviter de "corriger" un problème qui n'existe peut-être pas en usage réel ; à revérifier si le user confirme le voir sur son propre téléphone.
- **Deux tests e2e cassés trouvés en vérifiant** (`ai-settings.spec.ts`, `analytics.spec.ts`) : assertions sur du texte contenant encore un tiret cadratin ("—"), jamais mis à jour après le nettoyage typographique d'une session précédente (commit `1b1e08c`) qui avait changé le code réel vers "·" sans toucher aux tests correspondants. Corrigés (pas de rapport avec le design, trouvé en vérifiant que les changements du jour ne cassaient rien).
- **Vérifié en direct** : `tsc`/ESLint/tests unitaires propres, captures avant/après comparées pour chacun des 7 points, suite e2e ciblée (wellness/programs/analytics/ai-settings) repassée entièrement verte après les deux corrections de tests.

## Nav principale remplacée par une pastille flottante façon "tubelight" (2026-07-23)

Le user a fourni un composant de référence (prompt d'intégration 21st.dev/shadcn type, "tubelight-navbar" : pastille arrondie avec halo lumineux sous l'onglet actif) et a demandé de refaire la nav principale dans cet esprit. Le composant collé était pensé pour Next.js (`"use client"`, `next/link`, état actif suivi localement au clic) — adapté plutôt que copié tel quel.

- **`src/components/ui/tubelight-navbar.tsx`** (nouveau) : `next/link` → `Link` de `react-router` ; l'onglet actif n'est plus un `useState` mis à jour au clic (aurait affiché le mauvais onglet après un rechargement ou un lien profond) mais dérivé de `useLocation().pathname` à chaque rendu. Animation du halo via `framer-motion` (`layoutId`, nouvelle dépendance — absente du projet jusque-là). `NavItem` gagne un `ariaLabel` optionnel (distinct du libellé visuel `name`) pour garder un nom accessible complet ("Hypoxie intermittente") même quand le texte affiché dans la pastille est raccourci ("Hypoxie") par manque de place.
- **Cadrage avant implémentation (AskUserQuestion)** : le composant de référence n'a de place ni pour le logo "Fitness" ni pour "Se déconnecter" (ce n'est qu'une pastille flottante). Le user a choisi de déplacer la déconnexion vers la page Profil (bouton en haut, à côté du titre) plutôt que de la garder dans le chrome persistant — la pastille de nav devient le seul élément de navigation visible partout, le logo reste séparé.
- **`AppLayout.tsx`** : header passé en grid 3 colonnes (`1fr auto 1fr`) pour garantir que la pastille reste vraiment centrée sur desktop quel que soit la largeur du logo/email de chaque côté, plutôt qu'un centrage approximatif via `justify-between`. Deux instances de `NavBar` rendues (`hidden sm:block` dans le header pour desktop, `sm:hidden` en position `fixed bottom-0` pour mobile) plutôt qu'une seule instance avec des classes responsive internes complexes à faire cohabiter avec `position: fixed` vs `static` — même pattern de double-rendu par breakpoint déjà utilisé dans ce fichier pour le bouton de déconnexion avant refonte. `main` gagne un `pb-28` sur mobile pour que le contenu ne passe pas sous la pastille flottante.
- **Bug trouvé en vérifiant** : raccourcir "Hypoxie intermittente" en "Hypoxie" cassait le nom accessible attendu par `cycle.spec.ts` — corrigé via le nouveau champ `ariaLabel`, pas en gardant le libellé long dans la pastille.
- **Suite e2e complète relancée** (pas seulement le sous-ensemble ciblé, vu que ce changement touche `AppLayout` donc toutes les pages) : a mis au jour 3 groupes de problèmes préexistants, aucun lié à la nav elle-même :
  1. **Trois specs de plus avec le même tiret cadratin obsolète** (`session-log.spec.ts`, `offline-session-log.spec.ts`, `social.spec.ts`) — même cause que les deux déjà corrigés dans l'entrée précédente, corrigés pareil.
  2. **Un test testait un flux entièrement remplacé** (`social.spec.ts`, upload de photo avec champ "Légende" séparé) — obsolète depuis la refonte du feed social plus tôt cette session (post texte + photo optionnelle a remplacé le flux photo-seule). Réécrit pour coller au formulaire actuel (`Textarea` unique, pas de champ légende séparé).
  3. **Deux échecs non corrigés, hors sujet** : un test d'import de données qui timeout (le bouton de confirmation générique `ConfirmDialog` affiche toujours "Suppression…" pendant le chargement, quel que soit le `confirmLabel` réel — trouvé en lisant le DOM au moment de l'échec, pas juste supposé) et un test de milestone 1RM qui ne détecte plus de nouveau record. Cause probable commune aux deux : le compte fixture e2e a accumulé énormément d'historique au fil de très nombreuses sessions (déjà signalé une fois avant, ~120 programmes nettoyés à l'époque, visiblement reparti depuis) — le poids testé n'est peut-être plus un vrai record, et l'export/import d'un compte aussi chargé peut simplement dépasser le timeout du test. Pas creusé davantage : hors sujet par rapport à la demande du jour, nécessiterait soit un nettoyage du compte fixture soit une refonte des tests pour ne plus dépendre de valeurs seuils fixes.
- **Vérifié en direct** : captures mobile (375px) et desktop (1280px) avant/après, halo lumineux confirmé se déplacer correctement entre onglets en navigant réellement (pas juste au clic), `tsc`/ESLint/tests unitaires propres, suite e2e complète repassée avec seulement les 2 échecs pré-existants et documentés ci-dessus restants.

## Nettoyage du compte fixture e2e (2026-07-23)

Suite directe de l'entrée précédente : le user a demandé de nettoyer le compte fixture plutôt que de laisser les 2 échecs pré-existants trainer. Inventaire complet avant de toucher à quoi que ce soit (`supabase db query --linked`, comptage par table) : **324 programmes** accumulés (contre les ~120 déjà nettoyés une fois dans une session bien antérieure — reparti depuis), 16 séances loguées, 13 séries, 1 jalon, 1 activité bien-être + son log. Tout le reste (poids, clés IA, posts, cycle, exercices custom) déjà à zéro.

- **Suppression ciblée plutôt qu'un simple `admin.deleteUser` + recréation du compte** : `delete from programs`/`milestones`/`wellness_activities` pour ce `user_id` uniquement — la cascade FK (`programs → session_templates → session_template_exercises`, `programs → session_logs → session_log_sets`, `wellness_activities → wellness_activity_logs`, toutes déjà `on delete cascade`, vérifié dans les migrations avant de compter dessus) nettoie le reste automatiquement en une seule suppression par table racine. Le compte `auth.users`/`profiles` lui-même n'est pas touché — reste utilisable pour l'auth des futurs runs e2e sans reconfigurer `PLAYWRIGHT_TEST_EMAIL`/`PASSWORD`.
- **Confirmé que c'était bien la cause des 2 échecs précédents** : les deux tests (`data-import.spec.ts` timeout, `social.spec.ts` milestone 1RM) passent maintenant sans aucune autre modification. Suite e2e complète (32 tests) relancée entièrement verte.
- **Confirmé aussi que l'accumulation vient de runs interrompus, pas d'un vrai défaut des tests** : après la suite complète (32/32 verts), le compte est retombé à 0 programme — chaque test nettoie bien derrière lui quand il réussit jusqu'au bout. Les 324 programmes venaient de runs ayant échoué en cours de route (avant d'atteindre leur propre étape de nettoyage), accumulés sur une longue période. **Pas un problème à corriger dans le code des tests** — juste un ménage occasionnel à refaire si ça regonfle.

## Phase design globale — nav repensée, fusion Bien-être/Hypoxie, Analytics sur le dashboard, densité (2026-07-24)

Le user est passé à la phase design différée depuis le début de la couche IA, avec une liste précise de changements structurels plutôt qu'un simple ajustement visuel.

- **Pastille de nav agrandie, texte toujours visible** (`tubelight-navbar.tsx`) : sur mobile, passage d'une petite pastille centrée à une barre pleine largeur avec icône empilée au-dessus du libellé (le classique bottom-tab-bar) — nécessaire dès que les 6 items affichent leur texte en permanence, un simple élargissement de l'ancienne pastille icône-seule n'aurait pas tenu sur 375px. Desktop garde la pastille horizontale icône+texte d'origine. "Accueil" ajouté en premier (le plus à gauche), "Programmes" ajouté (nouveau, absent de toute nav jusque-là — seulement une carte sur le dashboard). Analytics retiré de la pastille à la demande explicite du user (accessible uniquement depuis le dashboard désormais, pour ne pas surcharger le menu).
- **Hypoxie intermittente fusionnée dans Bien-être** (vraie fusion, pas juste deux onglets) : `BreathPage.tsx` supprimée, sa logique (protocoles, runner plein écran) rapatriée dans `WellnessPage.tsx`. Carte "Ajouter un exercice" unique avec un sélecteur de type (Activité récurrente / Protocole hypoxie) qui bascule les champs du formulaire ; carte "Mes exercices" unique listant activités et protocoles ensemble, chacun avec ses actions propres (Marquer comme fait / Actif-Inactif pour une activité, Lancer pour un protocole). La vue Jour/Semaine planifiée reste spécifique aux activités récurrentes — un protocole n'a pas de jour "prévu", ça n'aurait pas eu de sens de l'y forcer. Route `/apnee` supprimée ; le lien "Respiration" de `SessionLogPage` pointe désormais vers `/bien-etre`. `e2e/breath.spec.ts` fusionné dans `wellness.spec.ts` (adapté : sélectionner l'onglet "Protocole hypoxie" avant de remplir le formulaire, plus besoin de naviguer vers une route à part).
- **Cycle retiré de la pastille aussi** : plus de lien de nav conditionnel, un bouton "Voir mon cycle" apparaît directement dans la section "Module cycles" du Profil une fois le module activé. `e2e/cycle.spec.ts` mis à jour (vérifie le lien dans la carte du Profil, plus le lien de nav qui n'existe plus).
- **Aperçu Analytics condensé sur le dashboard** : la carte "Cette semaine" devient "Analytics" — nombre de séances **et** tonnage de la semaine en cours (`computeWeeklyTonnage` + `getIsoWeekStart`, jusque-là seulement calculé sur la page Analytics complète) affichés côte à côte au-dessus du heatmap existant, avec le lien "Voir les stats complètes" inchangé. La carte "Mes programmes" (lien vers `/programs`) retirée du dashboard — redondante maintenant que Programmes a sa propre entrée dans la pastille.
- **Passe de densité globale**, hors feed social (exclusion explicite du user, cette page reste une liste à scroller) : composant `Card` partagé resserré une bonne fois pour toutes (`--card-spacing` 16px→12px, titre 16px→14px) — répercuté automatiquement sur toutes les pages plutôt que page par page, en réutilisant le variant `size="sm"` déjà prévu dans le composant mais jamais utilisé jusque-là. `<h1>` de page 24px→20px partout, espacement vertical entre cartes 24px→16px (12px sur le dashboard spécifiquement, l'écran le plus consulté). Gain concret vérifié par capture : le dashboard mobile affiche désormais 4 cartes quasi complètes sans scroll contre 2-3 avant.
- **Bugs pré-existants recroisés en vérifiant, pas causés par cette phase** : `analytics.spec.ts` cassé par le retrait volontaire d'Analytics de la pastille (corrigé : navigue depuis le dashboard comme un vrai utilisateur le ferait désormais). Le test de milestone 1RM/tonnage a de nouveau échoué à cause du tonnage cumulé par les nombreux runs de test de cette même session — même cause que l'entrée précédente, pas un vrai bug ; compte fixture renettoyé après la suite complète.
- **Vérifié en direct** : captures mobile (375px) et desktop (1280px) de la nav, de la page Bien-être fusionnée et du dashboard densifié ; `tsc`/ESLint/tests unitaires propres ; suite e2e complète repassée entièrement verte (32 tests, dont les specs déplacées/adaptées).

## Quatre petites corrections directes sur la phase design (2026-07-24)

Suite immédiate de l'entrée précédente : le user a demandé quatre ajustements ciblés plutôt qu'une nouvelle phase.

- **"g" de "Programmes" coupé dans la pastille mobile** (`tubelight-navbar.tsx`) : `leading-none` combiné à un texte très petit (`text-[0.65rem]`) coupait les descendantes (g, p, q) dans le contexte flex serré de la barre — corrigé avec `leading-tight`, taille de texte remontée à `text-xs`, icône 18px→20px, padding vertical légèrement augmenté. Vérifié par capture recadrée sur l'onglet concerné.
- **Système de records simplifié** : sur demande explicite, retrait complet des jalons "tonnage hebdo" et "1RM estimé" (formule Epley/Brzycki) — ne restent que deux types, un vrai 1RM (uniquement quand la série loguée est un vrai essai à 1 répétition dépassant la charge la plus lourde déjà faite sur cet exercice, plus aucune estimation à partir de séries à plus de reps) et un nouveau jalon "objectif de poids atteint". Migration (`20260724000000_simplify_milestones.sql`) : suppression des triggers/fonctions `weekly_tonnage`/`regularity_streak` et de `estimate_one_rep_max()`, `detect_one_rep_max_milestone()` réécrit pour exiger `actual_reps = 1`, nouveau trigger `detect_weight_goal_milestone()` sur `weight_entries` (`AFTER INSERT OR UPDATE` — `logWeightEntry` fait un vrai upsert par date, donc re-loguer le même jour est une UPDATE) comparé à `profiles.target_weight_kg`, idempotent par transition (ne se redéclenche pas si la pesée précédente était déjà sur l'objectif). `social-display.ts`/`social-display.test.ts` et `e2e/social.spec.ts` mis à jour en conséquence (un test de 1RM réécrit, un test d'objectif de poids ajouté de zéro).
- **Feed social simplifié : tout le monde visible par défaut** — l'app n'étant partagée qu'à un cercle fermé d'amis via lien privé, l'opt-in individuel (`social_sharing_enabled`) n'ajoutait que de la friction ; un vrai système d'ajout d'ami est explicitement reporté à plus tard. Migration (`20260724010000_simplify_social_visibility.sql`) : la vue `public_profiles` (point de passage unique dont dépendaient aussi les policies de `storage.objects` sur `progress-photos`, sans avoir à y toucher séparément) redéfinie sans le filtre d'opt-in ; policies SELECT de `milestones`/`posts` simplifiées en `using (true)` pour `to authenticated` ; `can_view_feed_target()` ne vérifie plus que l'existence de la cible. `SocialSharingSection` retirée du Profil, message d'état vide du feed mis à jour. La colonne `profiles.social_sharing_enabled` n'est pas supprimée (juste déconnectée), pour rester réutilisable si le futur système d'ajout d'ami en a besoin.
- **Ajout de photo depuis la galerie du téléphone** : retrait de `capture="environment"` sur l'input file caché de `FeedPage.tsx`, qui forçait l'appareil photo à s'ouvrir directement en sautant le sélecteur natif — celui-ci propose maintenant à nouveau "Photothèque"/"Choisir un fichier" en plus de l'appareil photo.
- **Bug de test trouvé en vérifiant la fiabilité du nouveau test d'objectif de poids** : son étape de nettoyage final (remettre `target_weight_kg` à vide) ne persistait jamais réellement, alors que le test lui-même passait au vert — `click()` de Playwright ne fait qu'émettre l'événement, sans attendre la requête réseau que le clic déclenche ; comme c'était la toute dernière ligne du test, sans assertion après, Playwright fermait la page avant que la requête `PATCH` n'ait le temps de partir. Confirmé par inspection directe de la trace réseau (une seule requête `PATCH /profiles` sur tout le test, celle du début). Une première tentative de correction (`toBeEnabled()` sur le bouton) n'a pas suffi — elle pouvait observer l'état "activé" d'avant clic avant que React ne re-rende en "en cours", donc passait trivialement sans avoir réellement attendu. Corrigé avec `page.waitForResponse()` ciblé sur le `PATCH /rest/v1/profiles`, qui attend la vraie réponse serveur avant de conclure le test. Reproduit et confirmé fiable sur plusieurs runs consécutifs de la suite complète après correction.
- **Vérifié en direct** : `tsc`/ESLint (0 erreur)/tests unitaires (90/90) propres ; suite e2e complète repassée (31/32 — le seul échec, `analytics.spec.ts`, est un flake préexistant non lié à ces quatre corrections, confirmé en le relançant seul où il passe systématiquement) ; compte fixture e2e revérifié propre (`target_weight_kg` bien à `null`) après la suite complète.

## Photos pour les exercices personnalisés du compte réel + bug trouvé au passage (2026-07-24)

Le user a demandé de vérifier les exercices personnalisés créés sur son vrai compte (`arnaud.zeh@gmail.com`) et d'ajouter les photos manquantes, avec la même méthode que les deux passes précédentes sur le catalogue système (matching manuel contre wger.de, jamais d'appel API à la volée depuis le client).

- **6 exercices personnalisés trouvés, tous sans photo** : Cross Body Hammer Curl, Incline Chest Press, Seated Wide Grip Row, Single Arm Chest Press, Single Arm Lat Pulldown, Smith Machine Shrugs. Recherche manuelle dans le dataset complet des traductions anglaises de wger (3286 exercices téléchargés localement, paginé) plutôt qu'un endpoint `/search/` qui n'existe plus dans l'API v2 actuelle (404, confirmé en testant plusieurs variantes).
- **4/6 couverts, migration `20260724020000_exercise_images_custom_and_shrug_fix.sql`** : Incline Chest Press (photo d'un Smith Machine Slight Incline Press, seul l'équipement diffère), Seated Wide Grip Row (réutilise la photo déjà assignée à "Seated Cable Row" au catalogue système, même famille de mouvement), Single Arm Chest Press (réutilise la photo de "Machine Chest Press" — la même machine à bras indépendants sert aussi pour le travail unilatéral), Smith Machine Shrugs (réutilise la photo de shrug à la barre, équipement seul diffère).
- **2/6 volontairement laissés sans photo** : Cross Body Hammer Curl (le seul match de nom exact sur wger n'a aucune image ; la photo générique "Hammer Curls" disponible montre un curl droit classique, pas le mouvement en diagonale devant le corps qui définit visuellement la variante "cross body" — vérifié en regardant l'image avant de trancher) et Single Arm Lat Pulldown (même situation : le match exact n'a pas de photo, et la seule photo de tirage vertical déjà en base est un tirage bilatéral à la barre large, équipement visiblement différent d'une poignée à un bras). Même principe que les passes précédentes : une mauvaise photo est pire que pas de photo.
- **Bug trouvé en cherchant les correspondances, hors périmètre de la demande mais corrigé quand même (le user a confirmé vouloir le fix)** : "Barbell Shrug" et "Dumbbell Shrug" du catalogue système (donc visible par tous les comptes, pas seulement celui-ci) pointaient tous les deux vers la même image wger — en l'ouvrant pour vérifier la pertinence, ce n'était pas une photo d'exercice mais une icône générique de logo d'appli fitness, visiblement une mauvaise contribution communautaire sur wger jamais détectée lors des deux passes précédentes. Corrigées avec de vraies photos de démonstration (shrug barre / shrug haltères, chacune vérifiée visuellement avant application).
- **Vérifié en direct** : chaque image candidate téléchargée et regardée avant d'être assignée (pas seulement le nom qui matche) ; migration poussée ; requêtes SQL de confirmation post-migration montrant les 4 nouvelles URLs et les 2 corrections de shrug en place.

## Espace vide en bas du dashboard mobile, scroll qui ne servait à rien (2026-07-24)

Le user a signalé (capture à l'appui) qu'après avoir scrollé jusqu'en bas sur mobile, la pastille de nav flottante se retrouvait au milieu de l'écran avec un grand espace noir vide en dessous — un scroll possible qui ne menait nulle part.

- **Root cause** : `AppLayout.tsx` faisait scroller tout le document (`min-h-svh` sur le conteneur racine) avec la nav mobile en `position: fixed; bottom: 0` par-dessus, plus un `pb-24` sur `main` pour réserver la place. Ce pattern (scroll de page + overlay fixe + padding de compensation) est connu pour laisser un espace mort sur mobile : les navigateurs mobiles cachent/affichent leur barre d'adresse en scrollant, et un élément `fixed` ne se replace pas forcément de façon cohérente avec `svh` quand la hauteur réelle visible change — la nav restait accrochée à l'ancienne position basse pendant que l'écran réellement visible s'agrandissait, révélant l'espace en dessous.
- **Corrigé structurellement plutôt que rapiécé** : `AppLayout` passe en coquille verrouillée à la vraie hauteur d'écran (`h-dvh flex flex-col`, plus de scroll au niveau du document) — le header et la nav mobile deviennent de vrais éléments flex (`shrink-0`), et seul `<main>` scrolle en interne (`flex-1 overflow-y-auto min-h-0`). La nav n'est plus jamais en `position: fixed` : elle est simplement le dernier élément de la colonne flex, donc toujours exactement au bord bas de l'écran par construction, plus besoin du `pb-24` de compensation qui devenait lui-même un espace mort une fois le vrai contenu épuisé.
- **Vérifié en direct** : mesure JS avant/après (`document.documentElement.scrollHeight` vs `window.innerHeight`) confirmant que le document ne dépasse plus jamais la hauteur d'écran réelle (0px d'écart, contre ~200px d'espace mort avant sur un compte avec peu de contenu) ; page Profil (beaucoup de contenu) vérifiée pour confirmer que le scroll interne fonctionne toujours normalement et ne coupe rien (dernier bouton "Supprimer mon compte" pleinement visible en scrollant) ; `tsc`/ESLint propres ; suite e2e complète repassée (31/32 verts, le seul échec — un test de post photo — confirmé être un flake réseau sans rapport en le relançant seul, où il passe).

## Système d'ajout d'ami : le feed redevient restreint aux amis (2026-07-25)

Le user a demandé de reprendre le système d'ajout d'ami, explicitement différé la veille quand le feed avait été ouvert à tout le monde. Cadré via AskUserQuestion avant d'implémenter (3 questions) : le feed redevient restreint aux amis une fois la fonctionnalité en place (recommandé, redonne du sens au mot "ami" plutôt que d'en faire un simple badge social), modèle demande + acceptation (pas un ajout direct asymétrique), découverte par recherche de nom (pas juste une liste, pour rester scalable).

- **Nouvelle table `friendships`** (migration `20260724030000_friendships.sql`) : `requester_id`/`addressee_id`, `status` ('pending'/'accepted', pas de statut "declined" séparé — un refus supprime simplement la ligne, peut être renvoyée plus tard), paire non-ordonnée garantie unique via deux colonnes générées (`user_a`/`user_b` = min/max des deux id) avec contrainte unique dessus — empêche par construction deux lignes contradictoires entre les deux mêmes personnes, quel que soit qui a demandé en premier. RLS : lecture si on est l'une des deux parties, insertion uniquement en tant que `requester_id`, mise à jour vers 'accepted' réservée au destinataire (l'auteur ne peut pas s'auto-accepter), suppression par l'une ou l'autre partie (couvre annuler/refuser/retirer avec la même opération).
- **Le feed redevient restreint aux amis** : policies SELECT de `milestones`/`posts` repassées de `using(true)` (décision de la veille) à `auth.uid() = user_id or are_friends(auth.uid(), user_id)`, nouvelle fonction `are_friends()` (security definer, même convention que `can_view_feed_target()` déjà en place) réutilisée aussi dans `can_view_feed_target()` et la policy SELECT de `storage.objects` pour les photos de progression. `public_profiles` reste ouvert à tous sans changement — nécessaire pour pouvoir rechercher et trouver quelqu'un par son nom avant de lui envoyer une demande, qui n'est donc pas encore un ami à ce moment-là.
- **`FriendsPage.tsx`** (nouvelle route `/friends`, pas dans la pastille de nav — même pattern qu'Analytics, accessible via un lien "Gérer mes amis →" depuis le Feed) : carte de recherche par nom avec le bon état de bouton selon la relation existante (Ajouter / Demande envoyée / Accepter / Déjà ami), cartes "Demandes reçues"/"Demandes envoyées"/"Mes amis" affichées seulement si non vides. Retrait d'un ami protégé par `ConfirmDialog` (seule action jugée assez consé quente pour le mériter — envoyer/annuler/refuser une demande ne le sont pas).
- **Deuxième compte fixture e2e créé** (`e2e-fixture-2@fitness-app.test`, `PLAYWRIGHT_TEST_EMAIL_2`/`PASSWORD_2` dans `.env.local`) : un flux de demande d'ami a besoin de deux utilisateurs réels et authentifiés séparément pour tester le côté acceptation, ce que le fixture unique existant ne permet pas seul. `auth.setup.ts` étendu avec un deuxième bloc `setup(...)` produisant `e2e/.auth/user2.json` ; `e2e/friends.spec.ts` utilise `browser.newContext({storageState: ...})` pour piloter les deux comptes simultanément dans un même test (pattern standard Playwright pour tester des flux multi-utilisateurs). Les deux comptes fixtures ont reçu un `display_name` distinct ("E2E Fixture 1"/"E2E Fixture 2") pour des assertions non ambiguës.
- **Même classe de bug retrouvée deux fois en écrivant les tests** (déjà rencontrée et corrigée hier sur le nettoyage du test d'objectif de poids) : `click()` de Playwright n'attend que l'émission de l'événement, pas la requête réseau qu'il déclenche — un `page.reload()` ou une navigation juste après peut donc couper une requête `POST`/`PATCH`/`DELETE` en vol. Trouvé en re-testant que le retrait d'ami nettoyait vraiment la base (une ligne "accepted" restait alors que le test passait), pas juste supposé. Corrigé partout dans `friends.spec.ts` avec `page.waitForResponse()` ciblé sur `/rest/v1/friendships`, avant chaque `reload()`/changement de compte qui suit une action de mutation.
- **Deux échecs rencontrés en vérifiant la suite complète, tous les deux confirmés sans rapport avec cette fonctionnalité** : `data-import.spec.ts` a de nouveau timeout à cause de programmes accumulés sur le compte fixture (22, même cause que déjà documentée deux fois cette semaine — nettoyé) ; `analytics.spec.ts` a refait son flake de timing déjà identifié la veille (passe seul de façon fiable). Une fausse alerte notable en cours de route : la page Feed semblait rester bloquée sur "Chargement…" en test manuel dans Chrome — en fait juste ralentie par ma propre session de navigateur manuel tapant sur le même serveur de dev en parallèle des tests Playwright, confirmé en fermant cette session et en relançant proprement.
- **Vérifié en direct** : flux complet testé à la main dans le navigateur avant d'écrire les tests (recherche, envoi, acceptation simulée via SQL, retrait) ; `tsc`/ESLint (0 erreur)/tests unitaires (90/90) propres ; suite e2e complète repassée (34/35 — le seul échec restant, `analytics.spec.ts`, confirmé être l'ancien flake non lié) ; comptes fixtures e2e revérifiés propres (aucune donnée résiduelle sur les deux comptes) après la suite complète.

## Nutrition — plan de phases scopé (2026-08-14)

Nouveau chantier : suivi nutrition (calories/macros), avec cibles personnalisées par profil, log quotidien, recherche OpenFoodFacts et scan code-barres. Scopé via `/phased-dev` + `AskUserQuestion` avant tout code. Wearables explicitement écartés de la V1 (estimation TDEE uniquement).

**Décisions actées avec le user avant implémentation :**
- Log **par repas**, pas un total journalier unique — mais les repas sont **configurables par l'utilisateur** (nombre + noms + ajout/suppression libre), même patron que les séances d'un programme (`session_templates`), pas un enum fixe petit-déj/déjeuner/dîner/collation.
- Nav : "Bien-être" quitte la bottom nav pour devenir accessible depuis Programmes (lien/section), le slot libéré accueille un nouvel onglet "Nutrition" dédié.
- TDEE : **les deux** — facteur d'activité auto-déclaré dès P1 (Mifflin-St Jeor + facteur), affiné plus tard par les séances réellement loguées (phase séparée, P5).
- Périmètre P1 (premier livrable testable) : cibles calculées + log **100% manuel** (l'utilisateur tape lui-même calories/macros) — pas de recherche d'aliments avant P2, pour ne pas faire déraper la taille de la première phase.

**Plan de phases (P0 → P7) :**
- **P0 — Fondations** : nav (retrait Bien-être de la bottom nav → accessible depuis Programmes ; ajout onglet Nutrition), schéma DB (`meal_slots` : repas définis par l'utilisateur, nom + ordre ; `nutrition_targets` : 1:1 par user, niveau d'activité + cibles calories/macros, overridables ; `food_logs` : entrée par repas/jour, nom + calories + macros), RLS owner-only (même patron que `weight_entries`/`coaching_profile`, jamais dans `friend_profile_details`), page squelette `/nutrition`.
- **P1 — MVP** : onboarding "combien de repas par jour" + CRUD `meal_slots`, calcul auto des cibles (Mifflin-St Jeor × activité auto-déclarée + répartition macro par `profiles.goal`, override manuel possible), log manuel dans un repas, dashboard du jour (cibles vs. consommé, barres de progression, répartition par repas).
- **P2 — Recherche OpenFoodFacts** : recherche texte avec pré-remplissage nutritionnel, quick-add "récents" dérivé de `food_logs` (pas de table dédiée).
- **P3 — Scan code-barres** : `BarcodeDetector` natif + fallback zxing/html5-qrcode (Safari iOS), lookup produit par code-barres OpenFoodFacts.
- **P4 — Portions pré-établies** : quick-add pour plats/aliments courants (liste éditoriale de départ).
- **P5 — TDEE affiné par les séances loguées** : ajuste la dépense calorique du jour selon l'activité réelle enregistrée, au-delà du facteur statique de P1.
- **P6 — IA + export + polish** : câblage dans `buildUserProfileContext()` (mêmes 4 fonctions Edge que `coaching_profile`), export dans `data-export-api.ts`, edge cases, responsive 375px.
- **P7 — Sécurité & hardening** : audit RLS sur les 3 nouvelles tables, validation des inputs numériques, `pnpm audit`.

**Risques identifiés** : support `BarcodeDetector` limité sur Safari iOS (mitigé par fallback dès P3) ; CORS/rate-limit potentiel sur l'API OpenFoodFacts en appel direct client (edge function proxy en secours si besoin en P2) ; TDEE reste une estimation, jamais une mesure réelle (communiqué clairement en UI) ; P1 est la phase la plus grosse du plan, à scinder en cours de route si elle dépasse ~3-4 jours en pratique.

**Décisions techniques** : `meal_slots` en table séparée plutôt qu'un enum fixe (voulu configurable par l'utilisateur) ; `nutrition_targets` en table dédiée plutôt que des colonnes sur `coaching_profile` (valeur calculée/recalculée souvent, concern distinct d'une fiche remplie une fois) ; pas de colonnes `source`/`external_id` sur `food_logs` avant que P2 en ait réellement besoin (évite des colonnes mortes en P1) ; favoris/quick-add dérivés de `food_logs` par requête plutôt qu'une table `favorite_foods` séparée.

Prochaine étape : implémenter P0.

## Nutrition — P0 : fondations (nav + schéma) (2026-08-14)

Suite directe du plan scopé ci-dessus. P0 = nav restructurée + les 3 tables de base, rien d'utilisable côté utilisateur encore (page squelette).

- **Nav** (`AppLayout.tsx`) : "Bien-être" retiré de `navItems`, remplacé par "Nutrition" (icône `Utensils`, `/nutrition`) au même endroit dans le tableau — nombre d'items inchangé (5→5), donc aucun nouveau risque de débordement sur la pastille mobile déjà éprouvée à 6 items lors de la phase design de juillet. `ProgramsListPage.tsx` gagne un lien "Bien-être →" en haut à droite du header, même patron que "Gérer mes amis →" sur le Feed.
- **Migration `20260814120000_nutrition_p0.sql`** : `meal_slots` (repas configurables par l'utilisateur, nom+ordre, **jamais seedés par trigger** — contrairement à `session_templates`/ses 7 jours fixes, voulu explicitement par le user comme un CRUD libre) ; `nutrition_targets` (1:1 par user, même patron que `coaching_profile` — ligne auto-créée par `handle_new_user`, jamais d'insert client) ; `food_logs` (une entrée par aliment loggé, rattachée à un repas). Les trois strictement propriétaires (RLS `auth.uid() = user_id`/`id`), jamais dans `friend_profile_details` — décision par défaut alignée sur `coaching_profile` (donnée nutrition/santé), pas sur `weight_entries`/`body_measurements` qui eux sont partagés avec les amis ; pas explicitement redemandé au user, à confirmer si un jour le partage nutrition est souhaité.
- **Leçon déjà apprise appliquée dès le départ** : `meal_slots.archived_at` (nullable) plutôt qu'un hard delete par défaut — évite de reproduire la perte de 15 séries déjà rencontrée avec `session_template_exercises` (voir `20260804214100_archive_session_template_exercises.sql`). Un repas jamais utilisé reste hard-deletable côté client ; un repas avec de l'historique se contente d'un `archived_at` pour ne jamais cascade-supprimer `food_logs`.
- **Backfill inclus dans la même migration** (pas un fichier séparé cette fois, connu à l'avance) : `insert into nutrition_targets (id) select id from auth.users on conflict do nothing` pour les comptes déjà existants.
- **Migration appliquée via l'API Management** (`database/query`, token du Keychain — voir mémoire "Accès Supabase Management API") plutôt que `supabase db push`, qui restait bloqué en attente d'un mot de passe DB interactif (pas de TTY dans cet environnement, `SUPABASE_DB_PASSWORD` non configuré). Ligne insérée manuellement dans `supabase_migrations.schema_migrations` pour rester synchronisé avec un futur `db push`/`migration list`. `database.types.ts` régénéré (`supabase gen types typescript --linked`) sans le bug de bruit CLI déjà rencontré en P3a.
- **RLS vérifiée en direct par requêtes REST croisées** (deux comptes fixtures, tokens obtenus par sign-in password, pas de session navigateur — évite la flakiness réseau déjà documentée) plutôt que Playwright : fixture 2 ne peut ni lire, ni update, ni delete les lignes de fixture 1 sur les 3 tables (0 ligne retournée/affectée à chaque tentative), et une tentative d'insert `food_logs` en usurpant le `user_id` de fixture 1 est rejetée avec `42501` (violation RLS), pas juste silencieusement ignorée. Données de test nettoyées après coup sur le compte fixture.
- **Vérifié en direct dans le navigateur (375px)** : pastille de nav mobile affiche bien Accueil/Programmes/Nutrition/Coach/Feed (Bien-être absent, aucun texte coupé), lien "Bien-être →" sur Programmes navigue vers `/bien-etre`, `/nutrition` affiche la page squelette. `tsc -b`, ESLint (0 erreur, 5 warnings préexistants sans rapport) et Vitest (152/152) propres.

**Gate P0 — à confirmer avant de passer à P1** :
1. Nav mobile et desktop : Nutrition présent, Bien-être accessible depuis Programmes → confirmé visuellement ci-dessus.
2. RLS croisée sur les 3 nouvelles tables → confirmée par tests REST directs ci-dessus.
3. `tsc -b`/lint/tests → propres.

**Gate P0 confirmée par le user (2026-08-14)** : nav OK, nutrition strictement privée OK, go P1.

## Nutrition — P1 : repas configurables + cibles calculées + log manuel (2026-08-14)

Suite du plan de phases. P1 rend le module réellement utilisable au quotidien : plus de squelette, un vrai flux onboarding → cibles → log → progression.

- **`src/lib/meal-slots-api.ts`** : CRUD `meal_slots`. `removeMealSlot` tente un hard delete puis, si Postgres renvoie `23503` (le repas a déjà des `food_logs` rattachés), bascule sur `archived_at` au lieu de laisser l'erreur remonter — même détection d'erreur que `offline-sync.ts` (`isForeignKeyViolation`). **Vérifié en direct contre la vraie base** (pas juste supposé) : suppression d'un repas avec un aliment loggé → `archived_at` posé, la ligne `food_logs` correspondante toujours présente en base, aucune erreur UI.
- **`src/lib/nutrition-calc.ts`** (pur, testé — 11 cas) : Mifflin-St Jeor + facteurs d'activité PAL classiques + ajustement calorique par objectif (perte_de_poids -20%, prise_de_muscle +10%, reste à la maintenance) + protéines en g/kg variables par objectif (2.2 en perte, 1.8 en prise de muscle, etc.) + lipides à 28% des calories, glucides en reste — chaque constante commentée avec sa source (ACSM, Aragon & Schoenfeld 2013, Helms et al. 2014, Morton et al. 2018), même esprit que `DEFAULT_REST_SECONDS_BY_FOCUS`. `computeAge` gère le cas où l'anniversaire n'est pas encore passé dans l'année.
- **`NutritionTargetsCard`** : cibles affichées avec barres de progression (`MacroProgressBar`, pas de dépendance ajoutée — même choix que `ContributionHeatmap`), section "Ajuster mes cibles" dépliable (même patron que les catégories de `CoachingProfilePage`) avec sélecteur de niveau d'activité + bouton "Calculer automatiquement" (désactivé et remplacé par un message si le profil est incomplet — sexe/taille/date de naissance/objectif/poids loggé manquants, avec lien vers `/profile`) + 4 champs éditables à la main dans tous les cas, un seul bouton "Enregistrer" pour les deux chemins (calculé ou tapé à la main).
- **Onboarding repas** (`MealSlotOnboarding`) : "Combien de repas par jour ?" avec presets (3/4/5/6) + nombre personnalisé, crée séquentiellement N `meal_slots` nommés "Repas 1"..."Repas N". `ManageMealSlots` permet ensuite de renommer (édition inline, sauvegarde au blur), ajouter et supprimer à tout moment — pas de seed par trigger, contrairement à `session_templates`, comme voulu explicitement par le user.
- **Log manuel** (`AddFoodLogDialog`, `MealSlotSection`) : formulaire nom+calories (obligatoires)+macros (optionnelles) par repas, suppression d'une entrée derrière `ConfirmDialog` (même prudence que pour les pesées).
- **Vérifié en direct** (compte fixture, 375px) : flux complet — onboarding 4 repas, saisie manuelle de cibles (2400 kcal / 180g protéines), ajout d'un aliment dans un repas avec mise à jour immédiate des barres de progression, renommage d'un repas, ajout d'un nouveau repas, suppression d'un repas déjà utilisé (confirmé archivé et non supprimé en interrogeant directement l'API REST après coup). Données de test entièrement nettoyées sur le compte fixture après vérification (repas, log, cibles remises à zéro).
- **Qualité** : `tsc -b` propre, ESLint 0 erreur (mêmes 5 warnings préexistants sans rapport), Vitest 163/163 (11 nouveaux tests `nutrition-calc.test.ts`).

**Gate P1 — à confirmer avant de passer à P2** :
1. Onboarding + gestion des repas (ajout/renommage/suppression) → vérifié en direct ci-dessus.
2. Cibles calculées automatiquement + override manuel → vérifié en direct ci-dessus.
3. Log manuel + barres de progression du jour → vérifié en direct ci-dessus.

## Nutrition — retours P1 : activité en 2 facteurs + grammage (2026-08-15)

Le user a testé P1 sur son vrai compte et remonté 4 points avant de passer à P2. Vérifié directement en base sur son compte réel (`arnaud.zeh@gmail.com`) plutôt que de deviner : `calories_target=3906` mais `activity_level` sauvegardé="modere" — incohérent (3906 kcal ne correspond mathématiquement qu'à un calcul fait avec "tres_actif" ×1.9, pas "modere" ×1.55). **Vrai bug confirmé** : changer le sélecteur d'activité ne relançait pas le calcul automatiquement, donc les chiffres affichés/enregistrés ne suivaient pas la sélection courante. Les 161g de protéines eux-mêmes étaient corrects (2.2g/kg × 89.5kg pour l'objectif prise_de_muscle), volontairement indépendants du niveau d'activité (ISSN : les besoins protéiques dépendent du poids/objectif, pas du NEAT) — mais ça manquait de transparence sans ce bug à côté.

- **Modèle d'activité repensé en 2 facteurs** (`nutrition-calc.ts`) suite à un cas concret du user (sédentaire au bureau + 4 grosses séances/semaine + week-ends actifs) qui ne rentrait dans aucune case d'un PAL à 5 niveaux classique : `DailyActivityLevel` (3 valeurs : sédentaire/modéré/physique, NEAT hors sport uniquement) + un bonus additif dérivé du volume d'entraînement (`TRAINING_FREQUENCY_BUMP`, paliers par tranche de séances/semaine). Combiné, le multiplicateur reste dans la même plage 1.15-1.9 que l'ancien PAL à 5 niveaux (physique + 7+ séances = 1.9, même plafond que l'ancien "très actif") — une décomposition, pas une nouvelle formule inventée.
- **Le volume d'entraînement n'est plus auto-déclaré** : dérivé automatiquement des `session_logs` réellement complétés (moyenne glissante sur 14 jours, `src/lib/training-frequency-api.ts`) plutôt qu'un champ de plus à tenir à jour à la main — avance une partie de ce qui était prévu en P5 ("TDEE affiné par les séances loguées"), choisi explicitement par le user plutôt que l'option plus simple (menu auto-déclaré). L'activité du week-end (rando, surf) reste volontairement hors scope pour l'instant — rien n'est loggable pour ce type d'activité aujourd'hui ; le user a choisi de s'appuyer sur l'override manuel déjà existant plutôt que d'ajouter un facteur de plus mal calibré.
- **Bug de recalcul corrigé** : `NutritionTargetsCard` relance maintenant le calcul immédiatement quand le sélecteur d'activité change (pas seulement au clic sur un bouton séparé) — les 4 champs affichés/enregistrés suivent toujours la sélection courante. Migration `20260815120000_nutrition_activity_and_grammage.sql` : contrainte `activity_level` resserrée aux 3 nouvelles valeurs (l'unique compte réel avec des données, `modere`, restait valide).
- **Grammage + calcul auto pour 100g** : le user a jugé la saisie calories/macros en valeur absolue trop fastidieuse. Le formulaire (`AddFoodLogDialog`) demande maintenant une quantité (g) + des valeurs **pour 100g** (comme une étiquette nutritionnelle), avec un total calculé et affiché en direct avant validation (`computeFoodLogTotals`, pur, testé). Nouvelles colonnes sur `food_logs` : `quantity_g` + 4× `*_per_100g` (le per-100g reste la source de vérité éditable, les totaux restent stockés pour que le reste de l'app — barres de progression, sommes du jour — n'ait pas besoin de connaître cette forme). Anticipe le format des données P2 (OpenFoodFacts fonctionne aussi en pour-100g).
- **Vérifié en direct** (compte fixture, 375px) : nouveau select 3 options sans crash avec profil incomplet, formulaire d'ajout avec aperçu "Pour 200g : 260 kcal · P 5.4g · G 56g · L 0.6g" (130kcal/2.7g/28g/0.3g pour 100g × 200g), soumission confirmée par une requête réseau réelle (`POST food_logs` 201) et la ligne stockée avec les bons `quantity_g`/`calories_per_100g`. Données de test nettoyées après coup.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 170/170 (18 tests `nutrition-calc.test.ts`, dont `trainingFrequencyBump` et `computeFoodLogTotals`).

## Nutrition — v2 : volume par durée réelle, pas quotidiens, aide week-end (2026-08-15)

Suite directe de l'entrée précédente. Le user a demandé des précisions sur le calcul du volume d'entraînement avant de valider, a remarqué que l'aide "week-end actif" n'était nulle part visible, et a proposé d'exploiter la fiche coaching pour plus de précision.

- **Volume d'entraînement pondéré par la durée réelle, pas juste le nombre de séances** (`computeAverageWeeklyTrainingMinutes`, `nutrition-calc.ts`) : avant, une séance de 20min et une de 90min comptaient pareil. `training-frequency-api.ts` récupère maintenant `started_at`/`completed_at` (pas juste un `count`), une séance oubliée en cours (jamais terminée dans les temps) est plafonnée à 180min pour ne pas fausser la moyenne. **Décision explicite de ne pas aller plus loin** (séries/tonnage/RPE) : aucun de ces signaux n'a de conversion validée vers des calories (le RPE mesure une perception d'effort, pas une dépense énergétique ; le tonnage varie trop par individu) — irait vers de la fausse précision plutôt qu'une vraie amélioration.
- **`avg_daily_steps` de la fiche coaching prioritaire sur la case à 3 choix** quand il est rempli (`dailyActivityMultiplierFromSteps`, paliers Tudor-Locke & Bassett 2004) : un signal continu et objectif plutôt qu'une catégorie auto-déclarée grossière. Le select "Activité quotidienne" se désactive automatiquement dans ce cas (avec un texte expliquant pourquoi) plutôt que de laisser les deux sources se contredire silencieusement.
- **Bouton "Recalculer" réintroduit** en plus de l'auto-recalcul au changement de sélecteur — nécessaire maintenant que le select peut être désactivé (cas `avg_daily_steps`), donc plus de moyen de déclencher un recalcul autrement qu'un bouton explicite.
- **Phrase d'aide ajoutée pour l'activité week-end** (rando, surf...) juste au-dessus des 4 champs manuels de `NutritionTargetsCard` — le mécanisme (override manuel) existait déjà depuis P1 mais n'était pas du tout découvrable, le user ne le voyait "nulle part". Toujours pas de nouveau champ dédié, juste un rappel textuel du chemin existant.
- **Distinction clarifiée avec le user** entre enrichir le calcul déterministe (plus de données structurées de la fiche coaching, ex. `avg_daily_steps` — pas d'appel API, juste des maths) et un raisonnement qualitatif complet sur toute la fiche (aliments préférés, allergies, habitudes...) qui reste le rôle du coach IA déjà branché sur la fiche coaching ailleurs dans l'app (prévu comme P6 "câblage IA" du plan nutrition, pas improvisé ici).
- **Vérifié en direct** (compte fixture, 375px) : nouveau texte d'aide pas-quotidiens/volume-en-minutes/semaine-active tous affichés correctement, select toujours fonctionnel sans `avg_daily_steps` renseigné (pas de régression). `tsc -b`/ESLint (0 erreur) propres, Vitest 175/175 (23 tests `nutrition-calc.test.ts`).

## Nutrition — calories dérivées des macros + purge des tirets cadratins app entière (2026-08-15)

Deux retours du user avant de passer à P2.

- **Calories dérivées des macros, plus un champ indépendant** (`NutritionTargetsCard`) : le user a fait remarquer que modifier protéines/glucides/lipides à la main devait aussi faire bouger le total de calories, ce qui n'était pas le cas (deux champs indépendants pouvaient devenir incohérents entre eux). Le champ Calories est maintenant désactivé/calculé en direct (`caloriesFromMacros`, pur, testé) à partir des 3 macros — plus de champ à synchroniser séparément, donc plus moyen d'avoir un total qui ne correspond pas à la somme réelle. Le calcul automatique (bouton "Recalculer") continue de remplir les 3 macros, les calories suivent automatiquement.
- **Tirets cadratins retirés de tout le texte visible par l'utilisateur** (pas les commentaires de code, qui n'en sont pas la cible) : 19 occurrences trouvées dans des cards/dialogs/menus à travers l'app entière (pas seulement nutrition — `FriendsPage`, `FeedPage`, `CoachingProfilePage`, `FriendProfilePage`, `ExerciseSlotFlow`, `LinkSupersetDialog`, `SubstituteExerciseDialog`, `DataOwnershipSection`, `NotificationsCard`, `HomePage`, `coaching-profile-fields.ts`, en plus des miennes dans `NutritionTargetsCard`/`AddFoodLogDialog`), reformulées avec une ponctuation naturelle (point, virgule, deux-points, parenthèses selon le cas) plutôt que juste supprimer le tiret. Vérifié qu'aucun test e2e n'asserte sur le texte exact modifié.
- **Vérifié en direct** : saisie manuelle de protéines/glucides/lipides (150/200/70g) → calories affichées "2030" en direct sans action supplémentaire (150×4 + 200×4 + 70×9 = 2030, correct). `tsc -b`/ESLint (0 erreur) propres, Vitest 177/177 (2 nouveaux tests `caloriesFromMacros`).

## Nutrition — P2 : recherche OpenFoodFacts (2026-08-15)

- **CORS vérifié directement avant de construire quoi que ce soit** (risque noté dès le scoping initial) : `curl` avec `Origin: http://localhost:5173` sur `world.openfoodfacts.org` confirme `access-control-allow-origin: *` — appel direct client possible, pas besoin d'edge function proxy.
- **`src/lib/openfoodfacts-api.ts`** : `searchOpenFoodFacts` (legacy `cgi/search.pl?json=1`) + `mapOpenFoodFactsResponse` gardée pure et séparée du `fetch` (testable sans mocker le réseau, 7 tests). Filtre les entrées sans code-barre/nom/calories (base communautaire, qualité variable). **Arrondit systématiquement à la source** (calories à l'entier, macros à 1 décimale) — l'API renvoie souvent des valeurs à 12 décimales (converties depuis des kJ, ex. `101.577437858508`), trouvé en testant en direct dans le formulaire avant de corriger.
- **`OpenFoodFactsSearch.tsx`** : recherche avec debounce (400ms, nouveau hook générique `useDebouncedValue`), résultats cliquables (nom · marque · kcal/100g) qui préremplissent le formulaire existant de `AddFoodLogDialog`. Chips "récents" (aliments déjà loggés, dédupliqués par nom, dérivés de `food_logs` — pas de table favoris séparée) affichées quand le champ de recherche est vide. Le formulaire manuel reste toujours visible en dessous ("Ou saisis les valeurs toi-même"), jamais remplacé par la recherche.
- **`fetchRecentFoodLogNames`** (`food-logs-api.ts`) : scanne les 50 dernières entrées, déduplique par nom, garde les 8 premières — pas de requête SQL de déduplication côté serveur, suffisant à ce volume.
- **Vérifié en direct partiellement** : recherche réelle testée dans le navigateur ("poulet" → 5 résultats réels avec marques/calories), sélection d'un résultat préremplit correctement le formulaire (confirmé visuellement, y compris le correctif d'arrondi). **La soumission finale n'a pas pu être confirmée en direct cette session** — l'extension Chrome a signalé une déconnexion en cours de test et les clics n'ont plus été délivrés de façon fiable ensuite (plusieurs tentatives : clic direct, clic via référence, clic JS, touche Entrée, nouvel onglet — aucune n'a abouti), un problème d'outillage du navigateur, pas du code. Le chemin de soumission lui-même est strictement identique à celui déjà validé de bout en bout en P1 (POST réel confirmé 201 + relecture en base) ; seule la nouvelle logique de préremplissage était à vérifier, et elle l'a été visuellement. À reconfirmer par le user ou lors d'une prochaine session.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 183/183 (7 nouveaux tests `openfoodfacts-api.test.ts`).

**Gate P2 — à confirmer par le user, y compris le point non vérifié en direct** :
1. La recherche renvoie des résultats pertinents → vérifié en direct.
2. La sélection d'un résultat préremplit correctement calories/macros → vérifié en direct.
3. La soumission finale enregistre bien l'aliment → **non revérifié en direct cette session**, à confirmer côté user.
4. Le log manuel reste disponible si rien n'est trouvé → toujours affiché sous la recherche, inchangé de P1.

## Nutrition — fix débordement horizontal mobile sur les résultats de recherche (2026-08-15)

Le user a confirmé que l'ajout d'un aliment fonctionne bien, mais a signalé un vrai bug d'affichage mobile : le préremplissage depuis un résultat de recherche permettait un scroll horizontal gauche/droite, non désiré (mobile-first).

- **Cause identifiée** : dans `OpenFoodFactsSearch.tsx`, chaque résultat était un bouton `flex items-center justify-between` avec un `<span className="truncate">` pour le nom+marque et un `<span className="shrink-0">` pour les kcal/100g. Sans `min-w-0` sur le span tronqué, `truncate` (qui repose sur `white-space: nowrap`) n'a aucun effet réel dans une ligne flex — le span garde sa largeur intrinsèque complète et pousse la ligne (donc la dialog, dont `overflow-y-auto` calcule implicitement `overflow-x` à `auto` par les règles CSS) plus large que le viewport dès qu'un nom de produit est long.
- **Corrigé en repassant sur plusieurs lignes plutôt qu'en réparant la troncature** (demande explicite du user) : le bouton résultat passe de `flex-row justify-between` à `flex-col items-start`, nom et kcal/100g empilés au lieu d'être serrés côte à côte — élimine la cause racine (plus de ligne flex à faire tenir sur une largeur fixe) plutôt que de juste ajouter `min-w-0`.
- **Vérifié en direct au pire cas** (le nom le plus long des résultats réels, "Manhattan Oeuf Poulet Rôti Fromage · Salade & Compagnie") : passe proprement sur deux lignes, aucun scroll horizontal, avant et après sélection/préremplissage. `tsc -b`/ESLint (0 erreur) toujours propres.
- Recherche vérifiée à nouveau en direct avec de vraies données (l'API OpenFoodFacts a renvoyé quelques 503 transitoires pendant les tests — confirmé indépendamment que l'API elle-même répondait normalement en parallèle via `curl`, donc un aléa réseau/navigateur de cette session, pas un problème de code).

## Nutrition — la liste de résultats reste affichée après sélection (2026-08-15)

Retour du user avec capture à l'appui : après avoir sélectionné un résultat de recherche, la liste restait affichée en dessous du formulaire désormais rempli.

- **Corrigé** (`OpenFoodFactsSearch.tsx`) : la sélection (résultat de recherche ou chip "récent") vide maintenant le champ de recherche (`selectAndClear`), ce qui fait disparaître la liste immédiatement. `showResults` dépend maintenant de la valeur brute du champ (pas de la valeur débattue qui pilote la requête réseau) pour que la disparition soit instantanée, sans attendre le délai de debounce.
- **Vérifié en direct** : recherche "poulet", sélection d'un résultat → champ de recherche revient au placeholder, liste disparaît, formulaire manuel correctement rempli en dessous.

## Nutrition — 2 nouvelles sources de recherche (liste maison + USDA FDC) (2026-08-15)

Retour du user : trop d'aliments génériques manquants sur OpenFoodFacts (base de produits emballés avec code-barres, pas d'aliments bruts). Diagnostiqué sur sa propre capture : chercher "Blanc de poulet" ne renvoyait que de la charcuterie tranchée de marque, jamais une entrée générique. A choisi les deux options proposées plutôt qu'une seule.

- **Type partagé** (`food-search-result.ts`) : `FoodSearchResult` commun aux trois sources, chaque id préfixé (`off:`, `common:`, `usda:`) pour éviter toute collision de clé React une fois fusionnées dans une seule liste.
- **Liste maison** (`common-foods.ts`) : ~55 aliments génériques courants (viandes/poissons/œufs, féculents cuits, légumineuses, légumes, fruits, laitages, matières grasses/oléagineux) avec des valeurs de référence standard (type CIQUAL/USDA), recherche locale instantanée (pas de réseau), insensible à la casse et aux accents. Toujours affichée en premier — délibérément une petite liste à la main plutôt qu'un gros dataset importé, cohérent avec la philosophie du projet.
- **USDA FoodData Central, proxifié par une edge function** (`usda-fdc-search`) plutôt qu'appelée directement du client : contrairement à OpenFoodFacts (entièrement public, sans clé), une clé FDC est nécessaire et rate-limitée par clé — l'exposer côté client (`VITE_...`) l'aurait rendue récupérable dans le bundle JS public. La clé (fournie par le user, obtenue gratuitement sur le site USDA) est stockée comme secret Supabase (`USDA_FDC_API_KEY`, jamais en clair dans le repo ni affichée dans la conversation après réception), l'edge function ne fait que relayer la recherche. Filtré sur `dataType=Foundation,SR Legacy` (aliments bruts/génériques) plutôt que `Branded` (ferait doublon avec OpenFoodFacts).
- **Vrai piège trouvé en testant en direct avec une clé réelle** (pas en devinant depuis la doc) : certains aliments "Foundation" (ex. blanc de poulet cru brut, fdcId 2646170) n'ont pas de champ `Energy` (nutrientNumber 208) classique, seulement des variantes Atwater (957/958). Le parsing (`usda-fdc-api.ts`) essaie 208 puis se rabat sur 957 — capturé dans un test avec les vraies données de la réponse.
- **Fusion dans `OpenFoodFactsSearch.tsx`** : maison (instantané) → USDA (async) → OpenFoodFacts (async) dans une seule liste, un seul indicateur de chargement/erreur combiné pour les deux sources réseau.
- **Vérifié en direct avec la vraie clé** : "poulet" → résultats maison instantanés + OpenFoodFacts (USDA ne matche pas, normal — base anglophone) ; "chicken breast" → résultats USDA réels ("Chicken, breast, boneless, skinless, raw", 106 kcal/100g — exactement la valeur du cas Atwater testé), sélection préremplit correctement le formulaire.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 194/194 (17 nouveaux tests : mapping USDA avec données réelles trimées, recherche maison, id OpenFoodFacts préfixé).

## Nutrition — virgule décimale rejetée sur les champs numériques (2026-08-16)

Retour du user : sur "Œuf entier", taper "1,1" dans Glucides échouait — seul le point fonctionnait.

- **Cause** : `<input type="number">` est sensible à la locale du navigateur/OS de façon incohérente — sur un appareil en français, le clavier numérique propose la virgule comme séparateur décimal, mais la valeur DOM d'un input `type="number"` n'accepte que le point selon la spec HTML. Résultat bien documenté (surtout iOS Safari) : taper "1,1" laisse le champ dans un état invalide/vide, silencieusement.
- **Corrigé** (`number-input.ts`, nouvelle fonction pure `parseLocaleNumber` testée) : tous les champs décimaux de `AddFoodLogDialog.tsx` et `NutritionTargetsCard.tsx` passent de `type="number"` à `type="text" inputMode="decimal"` (même clavier numérique sur mobile) et parsent via `parseLocaleNumber` (accepte virgule et point) plutôt que `Number()` directement. Le champ "Quantité (g)" est inclus par cohérence même s'il est rarement décimal. Le champ "Autre nombre" de l'onboarding repas n'est pas concerné (nombre entier de repas, pas de décimale possible).
- **Vérifié en direct** : "Œuf entier" avec "1,1" tapé tel quel dans Glucides → accepté sans erreur, aperçu "Pour 100g : 155 kcal · P 13g · G 1.1g · L 11g" correctement calculé.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 200/200 (6 nouveaux tests `parseLocaleNumber`).

## Programmes — focus deload à la duplication + charge de référence réduite (2026-08-16)

Retour du user : à la duplication d'un programme il veut pouvoir changer le focus (force/hypertrophie/endurance), les exercices restant toujours identiques (seuls RPE/séries/reps/charges changent). Question ouverte : pour un deload, un référentiel de charge réduite automatique a-t-il un sens scientifiquement ?

- **Deload comme 4e valeur de `focus`** (migration `20260816120000_deload_focus.sql`, contrainte CHECK étendue) plutôt qu'un flag séparé — cohérent avec le fait qu'un programme de deload a son propre repos par défaut (`DEFAULT_REST_SECONDS_BY_FOCUS.deload = 60s`, entre la récup métabolique de l'endurance et la récup neuromusculaire de la force) et son propre badge/label, comme les 3 focus existants.
- **Coefficient de réduction de charge** (`applyLoadReduction` dans `programs-api.ts`, testé unitairement) : -15% par défaut, ajustable dans la modale de duplication. Choix basé sur la littérature de périodisation (NSCA, consensus Helms/Israetel) : le volume baisse davantage en deload (-40 à -60% typique) que l'intensité/charge (-10 à -50% selon les sources) — 15% est une réduction modérée dans le bas de cette fourchette, pas agressive. Ne touche jamais un poids négatif (assistance machine sur exercice au poids du corps : le réduire en magnitude durcirait l'exercice, l'inverse d'un deload) ni null/0.
- **`DuplicateProgramDialog.tsx`** (nouveau) remplace le bouton "Dupliquer" à clic instantané : nom + focus (select) + champ % de réduction affiché uniquement si focus = deload, 15 par défaut.
- **Bug identique trouvé et corrigé au passage** (`ExerciseSlotFlow.tsx`) : la charge de référence (`target_weight_kg`) et le RPE cible utilisaient encore `type="number"`, même défaut de virgule décimale que la nutrition la veille. Passés à `type="text" inputMode="decimal"` + `parseLocaleNumber`, même correctif que ci-dessus appliqué au même fichier pendant qu'il était déjà ouvert pour ce chantier.
- **Vérifié en direct** : programme Force avec Squat 62,5 kg (virgule acceptée) dupliqué en focus Deload avec 15% → nouvelle charge 53.1 kg (= 62.5 × 0.85, arrondi à 1 décimale, correspond exactement au test unitaire) et repos par défaut 60s. Programmes de test supprimés après vérification.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 206/206 (6 nouveaux tests `applyLoadReduction`).

## Programmes — réduction deload élargie à 30-50% + ajustement scientifique entre focus (2026-08-18)

Retour du user, avant de passer au câblage IA nutrition : le -15% fixe pour le deload était jugé trop faible ; il a demandé une fourchette 30-50% choisissable, et — à charge pour Claude de proposer les valeurs scientifiquement — un ajustement automatique similaire quand on duplique par exemple d'hypertrophie vers force (charge de travail plus élevée en force).

- **`applyLoadReduction` renommé `applyLoadAdjustment`** (`programs-api.ts`) : accepte maintenant un pourcentage signé — positif réduit la charge (deload), négatif l'augmente (ex. hypertrophie → force). Même garde-fou qu'avant sur les poids négatifs (assistance) et null/0, dans les deux sens désormais (augmenter l'assistance en magnitude la rendrait plus facile, pas l'objectif non plus).
- **Zones de charge de travail par objectif** (`FOCUS_REFERENCE_1RM_PERCENT`) basées sur la table NSCA (Haff & Triplett, *Essentials of Strength Training and Conditioning*, 4e éd.) : force ≈90% 1RM, hypertrophie ≈76% 1RM (milieu de la fourchette 67-85%), endurance ≈55% 1RM (sous 67%). `deload` est volontairement absent de cette table — il n'a pas de zone absolue propre, il se définit par rapport à la séance dupliquée.
- **`suggestFocusLoadAdjustmentPercent(source, dest)`** dérive un ajustement suggéré à partir du ratio entre les deux zones (ex. hypertrophie→force : 1 - 90/76 ≈ -18%, soit +18% de charge ; force→hypertrophie : ≈ +16%, soit -16% de charge). Retourne 0 si le focus ne change pas ou si `deload` est source ou destination (pas de zone absolue à comparer).
- **`DuplicateProgramDialog.tsx`** : le champ d'ajustement n'apparaît que si le focus change réellement (avant : uniquement pour deload). Pour un deload, c'est maintenant un `Select` à 5 valeurs fixes (`DELOAD_REDUCTION_OPTIONS` = 30/35/40/45/50%, défaut 40% — milieu de la fourchette, choisie plus prononcée que l'ancien -15% car le but d'un deload est une vraie récupération) plutôt qu'un champ libre, pour rester dans les bornes scientifiquement admises. Pour tout autre changement de focus, c'est un champ texte pré-rempli avec la suggestion de `suggestFocusLoadAdjustmentPercent` (éditable). Une validation bloque toute valeur hors 30-50% si focus=deload.
- **Vérifié en direct** (compte fixture) : programme Hypertrophie avec Squat 100kg → dupliqué en Deload à -50% (choisi dans le select) → 50kg (= 100 × 0.5), repos 60s. Même programme dupliqué en Force → suggestion automatique -18% affichée → dupliqué tel quel → 118kg (= 100 × 1.18), repos 180s (défaut Force). Les deux calculs correspondent exactement aux tests unitaires. Programmes de test supprimés après vérification.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 212/212 (6 nouveaux tests : augmentation de charge signée, `suggestFocusLoadAdjustmentPercent` dans les 3 sens + cas deload/no-op).

## Programmes — repos figé sur le focus de destination à la duplication (2026-08-20)

Le user a remarqué qu'une duplication force→deload faisait passer le repos de 3min à 1min alors que le champ n'avait jamais été explicitement renseigné.

- **Cause** : `copyProgramInternal()` copiait `target_rest_seconds` brut du slot source (souvent `null`), qui se résolvait ensuite au fallback d'affichage `DEFAULT_REST_SECONDS_BY_FOCUS` — contre le focus de *destination*, pas celui d'origine. Un repos jamais écrit passait donc silencieusement de 180s (fallback force) à 60s (fallback deload) sans qu'aucune valeur ne change réellement en base.
- **Corrigé** : `target_rest_seconds` est maintenant figé explicitement sur `DEFAULT_REST_SECONDS_BY_FOCUS[focus]` (destination) à la duplication, comme charge/RPE le sont déjà — mais dérivé du focus plutôt que d'un pourcentage du repos source, sur demande explicite du user ("adapté au type de programme"). S'applique aussi à `copyProgramToMyAccount()` (copie du programme d'un ami), qui partage la même fonction interne.
- **Qualité** : `tsc -b`/ESLint/Vitest (231/231) propres.

## Dashboard — card nutrition réduite à calories/jour seul (2026-08-20)

- Les 3 `MacroProgressBar` (prot/glucides/lipides) de `NutritionTodayCard` poussaient la hauteur de la card assez pour visiblement rétrécir l'anneau de progression au-dessus (`flex-1` ne récupère que l'espace laissé par les cards à hauteur fixe en dessous). Card réduite à une seule ligne calories/cible, même format compact que les autres cards du dashboard — détail macro reste accessible via le popup de l'anneau nutrition et `/nutrition`.
- **Vérifié en direct** sur le compte fixture (cibles + repas loggé ajoutés temporairement via fetch authentifié en contexte page, nettoyés après capture) : anneau reprend une taille normale.
- **Qualité** : `tsc -b`/ESLint/Vitest (231/231) propres.

## Nutrition — P3 : scan code-barres + photo d'étiquette (vision IA) (2026-08-20)

Reprise du plan de phases scopé le 2026-08-14 (P3 jamais fait, le chantier avait sauté direct de P2 à l'IA/dashboard). Le user a demandé d'ajouter au passage une 2e solution caméra (photo de l'étiquette) pour le cas où le code-barres n'est pas référencé — scopée via AskUserQuestion (toujours visible, pas gatée derrière un scan échoué).

- **Scan code-barres** (`BarcodeScanner.tsx`) : `BarcodeDetector` natif (formats EAN/UPC uniquement) quand disponible, fallback `@zxing/browser` (nouvelle dépendance) pour Safari iOS. `fetchProductByBarcode`/`mapOpenFoodFactsProduct` (`openfoodfacts-api.ts`) lookup par code-barres sur l'endpoint produit OFF (`/api/v2/product/{barcode}.json`) — shape réelle vérifiée en direct au curl avant de coder (`{code,product,status,status_verbose}`, pas de clé "product" si status:0). `status:0`/produit incomplet retombe sur `null`, pas une exception — l'utilisateur retombe sur les autres options plutôt qu'un message d'erreur.
- **Photo d'étiquette** (`NutritionLabelPhotoCapture.tsx`) : nouvelle Edge Function `ai-analyze-nutrition-label` + `_shared/ai-nutrition-label-vision.ts`, même infra BYOK que les 4 fonctions IA existantes (clé lue depuis Vault, jamais une clé partagée). Prompt dédié, pas `COACH_PERSONA` (tâche d'extraction mécanique, pas de posture de coach à appliquer). Sortie JSON forcée (`extracted`, `name`, calories/macros pour 100g) — `extracted:false` plutôt qu'inventer des chiffres si la photo n'est pas une étiquette lisible. Compression client réutilisée telle quelle depuis `image-compression.ts` (P9c) avant envoi en base64.
- **`FoodEntryTabs.tsx`** : les 3 méthodes (Rechercher/Scanner/Photo) dans un même strip d'onglets au-dessus du formulaire manuel de `AddFoodLogDialog`, même patron visuel que le sélecteur de provider IA déjà utilisé ailleurs (`AiTrendAnalysisCard`).
- **Vérifié en direct sur le compte fixture** : requête vision bien formée confirmée pour les deux providers via le truc de la fausse clé Vault (401 réel "API key is invalid"/"Incorrect API key", pas une erreur 400 de structure — donc les content blocks image + le schéma forcé passent correctement l'un et l'autre côté Anthropic et OpenAI). Layout capturé à 375px via un script Playwright jetable (storageState du compte fixture) : les 3 onglets tiennent sur une ligne, l'état caméra-indisponible (pas de device dans l'environnement headless) affiche proprement une erreur + bouton réessayer sans casser la mise en page, l'état "pas de clé configurée" et l'état "bouton prêt" avec une clé configurée s'affichent tous les deux correctement. Chemin caméra réel (scan physique d'un vrai code-barres, vraie photo) et chemin vision avec une vraie clé laissés au user, comme à chaque fois pour le reste de la couche IA.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 234/234 (3 nouveaux tests `mapOpenFoodFactsProduct`).

## Nutrition — P4 : plats courants (quick-add) (2026-08-20)

Scopé via AskUserQuestion avant tout code (3 questions : type de contenu, intégration UI, comportement au clic) — le plan d'origine ("quick-add pour plats/aliments courants") laissait plusieurs lectures possibles. Décisions retenues : plats composés à totaux fixes (pas des portions par défaut sur les aliments unitaires existants de `common-foods.ts`), 4e onglet dédié dans `FoodEntryTabs`, ajout direct en un tap (pas de passage par le formulaire).

- **`common-dishes.ts`** : liste éditoriale de 16 plats composés (petit-déj/repas/léger), totaux fixes (calories + protéines/glucides/lipides), pas de per-100g — valeurs estimées à partir des mêmes références de composition que `common-foods.ts`, même précédent "liste plate, faite main, facile à étendre" (voir `feedback_simplicity_over_theoretical_structure`). `searchCommonDishes('')` retourne toute la liste (pas de recherche obligatoire, contrairement à `OpenFoodFactsSearch` — la liste est assez courte pour être parcourue directement).
- **`normalizeSearchText`** extrait dans `text-normalize.ts` (2e usage réel de l'accent-stripping après `common-foods.ts`) et réutilisé aux deux endroits.
- **`FoodLogInput`** (`food-logs-api.ts`) : `quantity_g`/`calories_per_100g`/`*_per_100g` passés en optionnels — la migration du 2026-08-15 avait déjà anticipé ce cas ("a future quick-add/import path that only has the total"), donc aucune migration SQL nécessaire, les colonnes sont déjà nullable.
- **`CommonDishPicker.tsx`** : liste parcourable/filtrable, chaque plat est un bouton qui logge directement (pas de prefill) — `onQuickAdd` distinct d'`onSelect` dans `FoodEntryTabsProps`, le composant reste seul responsable d'exposer son propre état d'erreur/chargement par plat.
- **`AddFoodLogDialog.tsx`** : `handleQuickAdd` insère avec les totaux du plat directement (`calories`/`protein_g`/`carbs_g`/`fat_g`, sans `quantity_g` ni `*_per_100g`) puis ferme le dialog — même mutation `useCreateFoodLog` que le chemin manuel, pas de nouveau hook.
- **Vrai bug mobile trouvé en vérifiant à 375px (pas juste en lisant le code)** : ajouter un 4e onglet a fait déborder la ligne d'onglets hors du `Dialog` (`DialogContent` est en `grid`, dont le comportement de taille minimale automatique laisse un enfant `flex` non-wrappable "faire exploser" la largeur du conteneur — même famille de bug que les débordements mobiles déjà documentés sur ce projet, ex. `ProgramDetailPage`/en-têtes à deux boutons). Corrigé avec `flex-wrap` sur la barre d'onglets (les 4 onglets passent sur 2 lignes à 375px plutôt que de déborder) — vérifié directement via `getBoundingClientRect`/`scrollWidth` avant/après le fix, pas juste visuellement.
- **Vérifié en direct sur le compte fixture** (pas juste en mock) via un script Playwright jetable réutilisant `e2e/.auth/user.json` : onboarding de 3 repas, ajout d'un plat en un tap, vérification du texte loggé et des totaux affichés, suppression du log et des repas créés pour laisser le compte dans son état d'origine.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 240/240 (6 nouveaux tests `searchCommonDishes`).

## Nutrition — P7 : audit sécurité & hardening (2026-08-21)

Proposé via AskUserQuestion après avoir constaté que P5 ("TDEE affiné par les séances loguées") était déjà substantiellement livré le 2026-08-15 (volume d'entraînement dérivé des `session_logs` réels) — seul P7 restait vraiment à faire du plan original. Utilise le skill `owasp-security`.

- **Audit RLS des 3 tables** (`meal_slots`/`nutrition_targets`/`food_logs`) : RLS activée partout, policies owner-only correctes, aucune des 3 ne transite par `friend_profile_details` ni aucune autre vue de visibilité amis (vérifié dans les migrations et la définition de la vue elle-même, pas juste supposé).
- **Vrai trou d'intégrité RLS trouvé et corrigé** : la policy insert/update de `food_logs` ne vérifiait que `auth.uid() = user_id`, jamais que le `meal_slot_id` référencé appartient au même utilisateur — la FK vers `meal_slots(id)` garantit seulement que la ligne existe. Rien dans l'app ne construit une requête pareille (`useMealSlots()` ne renvoie que les repas de l'utilisateur courant), mais une requête forgée à la main aurait pu rattacher un `food_log` au `meal_slot_id` d'un autre compte. Impact réel limité (la lecture du repas d'autrui reste bloquée par sa propre RLS, donc pas de fuite de données), mais fermé par principe. Migration `20260821120000_nutrition_p7_hardening.sql` : `with check` étendu avec un `exists (select 1 from meal_slots where id = meal_slot_id and user_id = auth.uid())` sur les policies insert et update.
- **Bornes numériques ajoutées** : `calories`/`protein_g`/`carbs_g`/`fat_g` (food_logs) et `calories_target`/`protein_g_target`/`carbs_g_target`/`fat_g_target` (nutrition_targets) n'avaient qu'un plancher, jamais de plafond — un `integer` sans borne haute accepte n'importe quelle valeur corrompue qui casserait ensuite les pourcentages/barres de progression du dashboard. Plafonds volontairement très larges (20000 kcal, 2000g par macro) — jamais atteignables par un usage réel, plancher existant inchangé (pas de changement de comportement pour une valeur légitime).
- **`pnpm audit`** : 22 vulnérabilités trouvées, toutes via `shadcn` (CLI de dev, jamais bundlée dans le build) → `hono`/`@hono/node-server`/`@modelcontextprotocol/sdk`. Corrigé en bumpant `shadcn` 4.13.1→4.18.0 (22→9 restantes). Une vraie dépendance de prod restait affectée : `react-router` (faille "RSC Mode CSRF Bypass", non exploitable ici — l'app n'utilise pas ce mode, SPA Vite classique — mais patch trivial déjà couvert par le range `^8.2.0` existant) → bumpé 8.2.0→8.3.0. Les 9 restantes (`nanoid`/`brace-expansion`/`undici`/`postcss`) sont toutes des transitives dev-only de la toolchain vite/vitest/tailwind, jamais expédiées au navigateur — laissées telles quelles.
- **Vérifié en direct sur le compte fixture** (pas juste en lisant le SQL) : requête réseau capturée montrant un insert légitime toujours en 201 après le durcissement de la policy RLS, puis nettoyage complet.
- **Qualité** : `tsc -b`/ESLint (0 erreur, warnings pré-existants non liés) propres, Vitest 240/240.

**Plan de phases nutrition original entièrement clos** (P0→P7). P5 reste noté comme substantiellement couvert plutôt que formellement "fait" — le seul angle non traité est l'activité week-end (rando, surf), explicitement laissée à l'override manuel par choix du user.

## Auth — mot de passe oublié (2026-08-21)

Trou trouvé en auditant l'auth suite à la question du user : aucune fonction de récupération/réinitialisation de mot de passe n'existait dans l'app (ni "mot de passe oublié" sur /login, ni "changer mon mot de passe" une fois connecté) — un compte oublié était bloqué sans intervention manuelle côté dashboard Supabase. Scopé via AskUserQuestion : "mot de passe oublié" seul (pas le changement en étant connecté), construit par-dessus le mailer de test Supabase actuel (2 emails/heure, pas de SMTP dédié — voir `project_smtp_deferred`) plutôt que d'attendre Resend/OVH.

- **`/forgot-password`** (`ForgotPasswordPage.tsx`) : formulaire email seul, `supabase.auth.resetPasswordForEmail(email, { redirectTo: .../update-password })`. Message de confirmation volontairement générique ("si un compte existe...") qu'un email existe ou non — évite l'énumération de comptes, aligné avec le comportement propre de Supabase sur cet appel.
- **`/update-password`** (`UpdatePasswordPage.tsx`) : page où atterrit le lien reçu par email. `redirectTo` déclenche l'échange PKCE automatique côté client (`detectSessionInUrl`, déjà par défaut) puis un événement `PASSWORD_RECOVERY` sur `onAuthStateChange` — écouté ici pour savoir quand afficher le formulaire de nouveau mot de passe (avec un timeout de 4s vers un état "lien invalide/expiré" si rien n'arrive). Volontairement **pas** du pattern `if (status === 'authenticated') redirect '/'` utilisé par Login/SignupPage — `useAuthStore` traite une session de recovery comme n'importe quelle session authentifiée, donc ce garde-fou aurait renvoyé l'utilisateur vers "/" avant même de pouvoir choisir un nouveau mot de passe.
- **`LoginPage.tsx`** : lien "Mot de passe oublié ?" ajouté sous le champ mot de passe.
- **`App.tsx`** : 2 routes ajoutées hors `ProtectedRoute` (même niveau que /login, /signup), chargées eagerly (mêmes raisons que le commentaire existant sur les pages pré-auth).
- **`site_url`/`uri_allow_list` déjà corrects** pour ce besoin (vérifiés en direct via l'API Management) : le wildcard `https://arnaudzeh.github.io/fitness-app/**` couvre déjà n'importe quel sous-chemin, donc `/update-password` est automatiquement autorisé comme cible de redirection sans toucher la config Supabase.
- **Vérifié en direct** (pas juste en lisant le code) via script Playwright jetable : lien visible sur /login, appel réseau réel `POST /auth/v1/recover` capturé en 200, état de confirmation affiché correctement, état "lien invalide" affiché après le timeout sur `/update-password` sans code. Testé avec un email `@example.com` (domaine réservé IANA, jamais délivrable) plutôt que le compte fixture `.test` — Supabase valide plus strictement le domaine sur `/recover` que sur `/signup` et rejette les TLD `.test`, sans rapport avec le code de l'app. Capture 375px sur les 4 écrans (login, forgot-password, confirmation, lien invalide), aucun débordement.
- **Non vérifié, laissé au user comme pour toute dépendance email/externe précédente (signup confirmation, push réel, clé IA réelle)** : le vrai aller-retour "email reçu → clic sur le vrai lien → nouveau mot de passe → connexion avec le nouveau mot de passe" — nécessite une vraie boîte mail, jamais scriptable depuis ces sessions.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 240/240 (pas de nouveaux tests unitaires, ces 2 pages sont uniquement des flux d'intégration avec Supabase Auth, pas de logique pure à isoler).

## Nutrition — duplication de repas (2026-08-21)

Demandé directement par le user (pas de découverte préalable) : quelqu'un d'assidu mange globalement la même chose chaque jour de la semaine (sauf cheatmeal du week-end), retaper chaque aliment tous les jours est une vraie friction. Discuté en exploratoire d'abord (2-3 phrases, pas de code) avant confirmation du user pour les deux formes à la fois : dupliquer un repas seul, et dupliquer toute la journée. Scopé via AskUserQuestion sur le seul point vraiment ambigu — comportement si le repas cible a déjà des aliments logués aujourd'hui : **ignorer les repas déjà remplis** (jamais d'ajout en double ni d'écrasement), choisi par le user, protège automatiquement un cheatmeal déjà saisi.

- **Quel jour dupliquer** : pas "hier" strictement mais **le jour précédent le plus récent qui a réellement quelque chose de loggé** (`fetchMostRecentLoggedDate` dans `food-logs-api.ts`, `lt('logged_date', beforeDate)` trié desc, limite 1) — tranché sans reproposer de question, en réappliquant une leçon déjà apprise sur ce projet (le pré-remplissage "dernière fois" des séances avait eu le même bug : s'arrêter à la séance la plus récente même vide masquait tout l'historique réel).
- **`duplicateFoodLogsForMealSlot(mealSlotId, fromDate, toDate)`** : copie chaque `food_log` du repas source vers `toDate` (nouvel id, tout le reste identique — total et référence pour-100g le cas échéant). Retourne 0 sans erreur si rien à dupliquer ce jour-là pour ce repas (cas normal pendant une duplication de journée qui touche un repas resté vide ce jour précis).
- **Bouton par-repas** (`MealSlotSection.tsx`) : affiché seulement si le repas est vide aujourd'hui ET qu'un jour source existe pour ce repas précis — jamais sur un repas déjà rempli.
- **Bouton journée entière** (`DuplicateDayButton.tsx`, nouveau composant, affiché sur `NutritionPage` juste après `NutritionTargetsCard`) : un seul jour source partagé pour toute la journée (pas un patchwork par repas depuis des jours différents — plus cohérent, se lit comme "les repas d'hier"), n'agit que sur les repas encore vides aujourd'hui **et** qui ont effectivement quelque chose ce jour-là.
- **Vrai bug bouton mort trouvé en testant, corrigé avant de livrer** : le bouton journée restait affiché et cliquable même quand plus aucun repas vide n'avait de contrepartie sur le jour source (ex: un seul repas sur deux avait un historique) — cliquer ne faisait alors rien de visible. Même famille que le bug déjà corrigé sur `generer_programme` (proposal vide qui laissait un bouton sans issue). Corrigé en vérifiant précisément, via `useFoodLogs(sourceDate)`, quels repas vides ont réellement quelque chose ce jour-là avant d'afficher le bouton — `useFoodLogs` gagne un 2e paramètre `{ enabled }` (même patron que `useCycleEntries`) pour ne pas requêter tant qu'aucun jour source n'est connu.
- **Vérifié en direct sur le compte fixture** (pas juste en lisant le code) : 2 repas onboardés, données backdatées via SQL sur un jour avec un vrai trou entre (avant-hier, pas hier) pour prouver que le "saut de trou" fonctionne, un des deux repas jamais loggé pour prouver l'absence de bouton. Bouton par-repas fonctionnel (ajout confirmé, bouton disparaît une fois le repas rempli), bouton journée fonctionnel et respecte bien le "jamais sur un repas déjà rempli" (pas de doublon en cliquant après coup), bouton journée qui disparaît correctement une fois qu'il n'y a plus rien à dupliquer. Capture 375px, aucun débordement. Compte fixture nettoyé (repas + logs) après coup.
- **Qualité** : `tsc -b`/ESLint (0 erreur, warnings pré-existants non liés) propres, Vitest 240/240 (pas de nouveaux tests unitaires — logique entièrement côté requêtes Supabase/filtrage de composant, rien d'assez isolable en fonction pure pour ce périmètre).

## Auth — changer son mot de passe en étant connecté (2026-08-21)

Suite directe du "mot de passe oublié" — le pendant symétrique explicitement laissé de côté à ce moment-là. Scopé via AskUserQuestion sur le seul vrai point ouvert : redemander le mot de passe actuel avant d'autoriser le changement (choisi, protège contre le cas écran/session laissé ouvert sur un appareil).

- **`ChangePasswordDialog.tsx`** : nouvelle section "Sécurité" (Card) sur `ProfilePage`, placée sur sa propre ligne plutôt que dans la rangée d'en-tête "Se déconnecter" (déjà serrée, risque de débordement mobile déjà rencontré ailleurs sur des rangées à 2 boutons). Vérification du mot de passe actuel via un `signInWithPassword` frais (pas de vérification "maison" — réutilise directement le mécanisme d'auth existant), puis `updateUser({ password })` si ça passe. Message d'erreur mappé comme `LoginPage` ("Invalid login credentials" → "Mot de passe actuel incorrect.").
- **Vérifié en direct sur le compte fixture** (pas juste en lisant le code) : mauvais mot de passe actuel → erreur affichée, dialog reste ouvert. Vrai aller-retour mot de passe réel → temporaire → mot de passe réel, confirmé par un login frais après coup (nouveau `auth.setup.ts` relancé à vide) — le compte fixture reste utilisable pour toute la suite e2e après ce test.
- **Qualité** : `tsc -b`/ESLint (0 erreur) propres, Vitest 240/240. Capture 375px de la section et du dialog, aucun débordement.

## Exercices — 2e passe photos manquantes (2026-08-21)

Reprise du backlog "~57% des exercices sans photo" (en fait 36/167 restants au moment de reprendre, les deux premières passes de juillet avaient déjà couvert bien plus que la note de backlog ne le laissait supposer). wger.de vérifié candidat par candidat via `exerciseinfo/<id>/` (pas juste supposé) : sur ~25 candidats identifiés par nom, un seul avait réellement une image — les deux premières passes avaient déjà pris tout ce qui était facile chez wger.

- **2e source ajoutée, scopée via AskUserQuestion** : Free Exercise DB (`github.com/yuhonas/free-exercise-db`), licence Unlicense (domaine public, encore plus permissif que le CC-BY-SA de wger). Couvre ~25 des 36 exercices restants, vérifié un par un (nom + image existante dans le JSON du dataset) avant d'écrire la migration, puis un échantillon vérifié visuellement (qualité correcte, mouvement bien représenté).
- **Migration `20260821130000_exercise_images_free_exercise_db.sql`** : 27 `update` (dont quelques variantes d'équipement tolérées comme suffisamment proches — Arnold Press en kettlebell, Glute Bridge à la barre — même tolérance déjà appliquée en juillet) + 1 réutilisation d'une photo déjà existante dans notre propre catalogue (`Romanian Deadlift (RDL)` reprend celle de `Soulevé de terre roumain`, même mouvement, pas besoin d'une 3e source pour un seul cas).
- **Résultat** : 131/167 → 159/167 exercices système avec photo. Restent 8 sans correspondance dans aucune des deux sources : Bicycle Crunch, Burpee, Curtsy Lunge, Landmine Press, Man Maker, Nordic Hamstring Curl, Toes to Bar, V-Up — gardent l'icône de repli par muscle group.
- **Aucun changement de code** : `ExerciseThumbnail.tsx` consomme déjà `image_url` de façon générique (avec repli automatique sur erreur de chargement), donc un backfill de données pur suffit.
- **Vérifié en direct** (pas juste via curl) : nouvel exercice recherché dans `ExercisePicker` sur le compte fixture, image confirmée réellement chargée (`naturalWidth`/`complete`/opacité vérifiés via JS, pas juste la présence de la balise `<img>` dans le DOM) et visuellement correcte sur une capture recadrée à la vignette.
